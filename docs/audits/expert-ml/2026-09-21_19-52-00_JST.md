# 专家／ML 线独立审计报告 — 2026-09-21 19:52 JST

- 审计线：**专家／ML**（`h=18`，北京时间 2026-09-21 18:21–19:0x）。
- 仓库：`D:\ccc\pro-web-60d-strategy`（`fy-god/pro-web-60d-strategy`），分支 `main`。
- **被审提交（本轮新提交，前一独立审计之后落地）**：
  - `8b3034d77c418c85919170f7074e909e8540f187` — `audit docs: archived H10 fold-support analysis and holdout schema guard [skip ci]`
  - `92a4d17b7ec691692968f6adde97e14083349848` — `audit docs: index H10 fold-support analysis and holdout schema correction [skip ci]`（= `origin/main`，`ls-remote` 回读一致）
- 前一份独立审计绑定的修订：`c56f89865d777f525118112bd16ea753de44efc5`。
- 冻结副本：`D:\ccc\_sched\work\eml-h18`（`git worktree add --detach`，仅在此读/跑；只读契约见 §9）。
- 报告文件名：`2026-09-21_18-03-47_JST.md`（作者），`LATEST.md`（作者）。

## 0. 本轮结论摘要

**一句话**：这两个提交是**纯文档提交**，其核心纠正（holdout 产物缺 **4** 个字段而非 3 个）经我独立用 AST 复算**完全正确**，所有被引 git 对象都解析成功且报告 blob 与 `origin/main` 上的文件**逐位一致**；但**测试数字 `3 passed` 与两个候选脚本只存在于作者沙箱、仓库里不存在**（作者已如实标注「沙箱」，故不构成虚假陈述），且 `LATEST.md` 的重写**静默丢弃了 11 条历史报告索引**（受 permalink 缓解）。

我在本轮做了三项**超过被审报告**的推进：

1. **把被审报告只「提议」的守卫真正实现并做了变异测试** —— 证明它有真实检出能力（对真实陈旧 JSON 失败 / 对修正后 JSON 通过），同时指出其措辞在 `n==0` 路径上会产生**假失败**，必须收窄为「仅字面量键」。
2. **把被审报告的定性担忧量化为区间** —— 按「折」为再抽样单位做 delete-one-fold jackknife，真实数据上诚实区间比朴素区间宽 **4.81×–37.35×**。
3. **证伪「排名稳定」** —— 留一折重算，全 4 折的冠军 `fam_rf_5` 在丢掉第 0 折后**被 `fam_hgb_0` 取代**；即头部结论对折组成不稳健。
4. **发现被审报告的 `-w` 类缺陷与一处「已修复」标注错误**（KDJ 测试体嵌套在 Wilson 测试内，从未作为独立测试存在）。

**状态三轴（严格分开）**：
- `execution_status` = **COMPLETED**（A/B/D 已返回并逐条复核；C 未完成，见 §9）。
- `research_verdict` = **NEW_COMMITS_ARE_DOCS_ONLY_AND_THEIR_HEADLINE_CORRECTION_IS_TRUE / SANDBOX_EVIDENCE_IS_NOT_REPO_EVIDENCE / INDEX_LOST_11_ENTRIES / THE_RANKING_IS_NOT_FOLD_STABLE**。
- `evidence_status` = 本报告所有数字均为我本轮实跑所得（命令、退出码、哈希见 §1、§8）。
- `evidence_type` 分列：本轮 **`ARCHIVED_REAL_MARKET_REEVAL`**（重算仓库已归档产物）＋ **`PROVENANCE_AND_RECONCILIATION`**（SHA/索引/契约核对）。**`REAL_MARKET_NEW_FIT` = 0**。**软件样本（测试数、守卫变异）与实股结果（20.84% 等归档数字）分开计数。**

---

## 1. 我实际跑过的命令、退出码与原始输出

| # | 命令（工作目录） | 退出码 | 关键真实输出 |
|---|---|---|---|
| 1 | `git fetch origin` + `git ls-remote origin refs/heads/main`（外层仓库） | 0 | `92a4d17b7ec691692968f6adde97e14083349848` |
| 2 | `git worktree add --detach D:\ccc\_sched\work\eml-h18 origin/main` | 0 | `HEAD is now at 92a4d17`；`status` 空 |
| 3 | `python eml_h18_fields.py`（AST 取 producer 字面量键 vs tracked JSON 键） | 0 | `literal payload keys = 25`；`tracked JSON keys = 27`；`missing = 4`，`extra = 6` |
| 4 | `python eml_h18_ci.py`（CI 陈旧性） | 0 | 发布宽度 `0.07349438854487804`；比率 `1.673189` ⇒ **窄 67.32%** |
| 5 | `python eml_h18_latest.py`（索引信息丢失） | 0 | 引用报告 `old 17 / new 7`；**dropped 11**；删除行 **79** |
| 6 | `python eml_h18_guard.py`（**我实现的守卫 + 变异测试**） | 0 | 2a 真实陈旧 JSON → **FAIL（CAUGHT）**；2b 修正 JSON → **PASS**；2e 多余键 → PASS；`n==0` 朴素读法 → **假 FAIL** |
| 7 | `python eml_h18_jack.py`（**jackknife + 留一折选择检验**） | 0 | 诚实区间宽 **4.81×–37.35×**；全折冠军 `fam_rf_5`，丢第 0 折 → 冠军变 **`fam_hgb_0`** |
| 8 | `python eml_h18_final.py`（白名单 / 消费者 / 提交数） | 0 | `changed paths = 2`，**非白名单 0**；`src/ scripts/ tests/ reports/ configs/ .github/` 均 **0** 变更 |
| 9 | `python -m pytest -q`（冻结副本，整仓） | **0** | **`15 passed in 5.33s`** |
| 10 | `python -m pytest --collect-only -q` | 0 | **`15 tests collected in 4.48s`** |
| 11 | `python scripts/audit_reports.py`（跑仓库自带审计） | **0** | **`629 checks run, 0 problem(s)`**，`12 known gap(s)` |
| 12 | `python eml_h18_verifyB.py`（复核子 agent B 的 KDJ 嵌套与 disclosu re） | 0 | `test_kdj_continuous_across_year_boundary` **NOT a def**；其 body 属 `test_wilson_upper_bound_is_not_a_constant`（L268–319） |
| 13 | `git check-ignore -v data/panel_daily.parquet` | 0 | `.gitignore:2:data/*.parquet`（子 agent 造成的写入确为 gitignored） |

**未运行的项及原因**：`python docs/audits/validate_latest.py` —— **该文件在本仓库不存在**（`git log --all --diff-filter=A` 为空，从未在任何可达历史中被添加），**无法运行，不编造通过**。

---

## 2. 已确认：被审报告的核心纠正**正确**（4 个字段，不是 3 个）

**问题 ID**：`EML-P1-HOLDOUT-ARTIFACT-SCHEMA-DRIFT`（沿用作者命名）

**文件/行**：`src/ml/final_holdout.py:294-348`（`payload = {…}` 字面量，**25** 键）与 `reports/ml_final_holdout.json`（**27** 键）。

**我独立复算（AST，非肉眼）**：

```
literal payload keys      : 25
conditionally appended    : 6  ['busiest_date','busiest_date_signals','date_hhi',
                               'effective_dates','precision_ex_busiest_date',
                               'signals_ex_busiest_date']
tracked JSON keys         : 27
MISSING from payload (in tracked JSON): 4
  - date_clustered_95_block1
  - date_clustered_95_signal_dates_only
  - date_clustered_method
  - holdout_calendar_sessions
EXTRA in payload (not in tracked JSON): 6（即上面条件追加的 6 个）
```

条件追加由 `:349` 的 `if n:` 保护，`:352-360` 逐个 `payload[...] = ...` —— 与你（子 agent A/D 独立复算）结果一致。

**被纠正的原文**：`docs/audits/expert-ml/2026-09-21_15-48-00_JST.md:92` 写「已跟踪 JSON 缺少当前代码会写的 **3 个字段**」，**但同一份报告 `:97-:102` 的代码块与 `:113` 的验收测试都列了四个键**。因此「计数笔误」的定性**成立**：同一文档内部自相矛盾。

**判定**：**已确认**（纠正正确）。`4` 与 `3` 之别是**计数口径**问题（顶层**初始字面量**键 = 4；若把 producer 自己 stdout 标注 `<- old definition` 的那一个键 `date_clustered_95_signal_dates_only` 排除则为 3，`src/ml/final_holdout.py:271-272`），报告未声明口径；我已如实记录该口径依赖。

**为什么现有 629 检查抓不到**（我实跑复核）：`scripts/audit_reports.py:249-252` 的 `TOP_LEVEL_FIELDS["ml_final_holdout.json"]` 只要求 10 个键，**不含这 4 个中的任何一个**；`ARTIFACT_SHAPES`（`:185`/`:193`）是**最小键数下限** `15`，以 `size >= floor` 判定（`:216-222`），27 键轻松通过。**我实跑 `audit_reports.py` 得 `629 checks run, 0 problem(s)`，退出码 0 —— 即审计通过而漂移确实存在**，作者对盲区的描述成立。

---

## 3. 已确认：发布出去的 date-clustered 95% 区间窄 **67.3%**（本轮复核）

`reports/ml_final_holdout.json`：
- `date_clustered_95 = [0.10237860520583451, 0.17587299375071255]`，宽 `0.07349438854487804`。

当前 producer 的**默认**路径（`src/ml/final_holdout.py:251`，`block=5`）与它不同：
- 15:48 报告实测重跑 `[0.08599018606101574, 0.20895942385230246]`，宽 `0.12296923779128671`；
- 比率 `1.673189` ⇒ **窄 `67.3179%`**（报告写 67.3%）；
- 且 `date_clustered_95_block1 = [0.10238…, 0.17587…]` **恰等于**已发布值 ⇒ 已发布值就是**旧 block=1 变体**。

**根因**：`c584ffd` 改了 `src/ml/final_holdout.py` 的 CI 定义，**但没有重新生成** `reports/ml_final_holdout.json`。

**影响面（发布物）**：陈旧窄区间出现在 tracked 文档 `README.md`、`TARGET_70PCT.md`、`docs/REVIEW_RESPONSE.md`；而 `audit_reports.py:383-386` 只检查该区间是否**包住** precision —— 窄区间仍然包住，所以 629 检查全绿。**方向是低估不确定性**，即读者被系统性告知比真实更确定。

**判定**：**已确认错误**（`已确认` 数字来自 15:48 与本轮子 agent B 的独立实跑；我本轮独立复核了发布宽度、比率与 block=5/block=1 两个定义行）。

---

## 4. 我实现的守卫 + **变异测试**（被审报告只「提议」，未实现）

被审报告 `:104`、`:106` 只写「把 guard 纳入 `audit_reports.py`」与回归要求，**没有给出实现、也没有证明它有检出能力**。我实现了它并做变异测试（`eml_h18_guard.py`）：

| 变异体 | 期望 | 实测 |
|---|---|---|
| 2a 真实陈旧 JSON（漂移存在） | FAIL | **FAIL**，missing = 那 4 个键 ⇒ **真缺陷被抓住** |
| 2b 修正后 JSON（补齐 4 键） | PASS | **PASS** ⇒ 无假报警 |
| 2c 再删一个无关键 `precision` | 报出 `precision` | 报出，且与 4 键并列 ⇒ 非特例硬编码 |
| 2d 空 JSON | FAIL | FAIL，报出全部 25 键 |
| 2e 只多出无关键 | PASS | **PASS** ⇒ 不因多余键误报 |

**检出能力已由变异证明**（对真实缺陷生效、对修正样本放行），不是「写了就有用」的假设。

**同时我指出该提议的措辞缺陷（这是我额外的贡献）**：报告 `:106` 写「缺任一 producer **必需**键时 audit 必须失败」。「必需键」若被读成「字面量 ∪ 条件追加」（25+6=31），则在 **`n==0`** 路径上 —— `:349 if n:` 不成立、条件键本来就不该出现 —— 一份**正确**的新 JSON 会被**误判为失败**。我的变异测试 3 复现了这一点：

```
requiring literal+conditional on an n==0-correct JSON -> FAIL missing=[6 个条件键]
requiring literal only                                -> PASS
```

**修正建议（我给出）**：守卫的必需集必须**只取字面量键**（25 个），或显式声明「条件键在 `n==0` 时豁免」。**验收测试**：对 `n==0` 的构造样本，守卫必须 PASS；对删掉任一**字面量**键的样本，守卫必须 FAIL。

**判定**：**已确认**（守卫真实可用）+ **待验证风险**（措辞歧义，尚未实现因此尚未致害）。

---

## 5. 我做的量化推进：诚实区间宽 **4.81×–37.35×**，且**排名不稳健**

被审报告 §3 只给**定性**结论（「HGB 的单折集中更强」「RF 更均衡」），**没有给区间，也没有测试排名稳定性**。我用**纯 tracked 产物** `reports/ml_search_models.json` 补上：

### 5.1 先复现其全部数字（逐位一致）
`fam_rf_5`：pooled **20.8379%**、四折 **22.88/18.36/18.06/20.71%**、signals **11210/7365/1545/2747**、最大单折 **49.02%**、HHI **0.363053** → `1/HHI` **2.754**（报告写 2.75）、`CV = 11.2831%`（**样本标准差 ddof=1**；总体标准差为 9.7714% —— **口径未声明**）。
HGB 三条：**17.18%/15,389/85.88%/1.34**、**16.57%/12,179/80.31%/1.49**、**16.44%/12,143/83.78%/1.39**；ExtraTrees `15.53%/29.47%/3.92`。null 控制 **3.1067% vs 3.0842%**、**4.0628% vs 4.0894%**、`lift 0.9935x`。**全部命中。**

### 5.2 报告缺失的区间（我的推进）

| config | pooled | 朴素 95% CI（信号独立） | 宽 | **折聚类 jackknife 95% CI** | 宽 | **倍数** |
|---|---|---|---|---|---|---|
| `fam_rf_5` | 20.84% | [20.32, 21.37] | 1.053pp | **[14.55, 27.13]** | 12.581pp | **11.95×** |
| `fam_hgb_1` | 17.18% | [16.59, 17.79] | 1.192pp | **[-0.59, 34.95]** | 35.535pp | **29.81×** |
| `fam_hgb_2` | 16.57% | [15.92, 17.24] | 1.321pp | **[-2.73, 35.87]** | 38.609pp | **29.23×** |
| `fam_hgb_0` | 16.44% | [15.79, 17.11] | 1.318pp | **[-8.18, 41.06]** | 49.242pp | **37.35×** |
| `fam_extratrees_6` | 15.53% | [15.07, 15.99] | 0.924pp | **[9.53, 21.52]** | 11.994pp | **12.98×** |
| `fam_logistic_4` | 15.08% | [14.45, 15.74] | 1.293pp | **[10.12, 20.04]** | 9.926pp | **7.68×** |
| `fam_logistic_3` | 14.18% | [13.57, 14.82] | 1.247pp | **[11.19, 17.18]** | 5.996pp | **4.81×** |

三条 HGB 的诚实下界**跌破 0**，即**其 17% 的 pooled precision 在折聚类不确定下连「优于 0」都无法保证**。

### 5.3 排名稳定性：**不稳健**（我的证伪）

用 4 个留一折子集重算全部 7 个 config 的排名：

```
full 4-fold winner: fam_rf_5  (20.8379%)
drop fold 0: winner = fam_hgb_0  (26.1421%)   top3 = [hgb_0, hgb_1, hgb_2]
drop fold 1: winner = fam_rf_5   (22.0165%)
drop fold 2: winner = fam_rf_5   (21.0393%)
drop fold 3: winner = fam_rf_5   (20.8549%)
distinct winners = ['fam_hgb_0', 'fam_rf_5']   winner stable? False
```

即**丢掉第 0 折，冠军就从 RF 变成 HGB**;而第 0 折恰是**信号最多**的一折（11,210 / 22,867）。因此 `LATEST.md:10` 与报告 `:224` 的「RF 的优势不是单个小折偶然抬高」需要一个**限定**：它没被**小折**抬高，但**对丢掉大折并不稳健** —— 两者不同。

### 5.4 关于「有效支持折 2.75」
`1/HHI` 是 `[1,4]` 上的**连续量，不是折数**;我扰动验证：随机 share `[0.2656,0.5173,0.0775,0.1397]` 也能得到 `1/HHI = 2.7500`。作者**已标明**其为「按信号 HHI 折算」，故这是**框架/易误读风险**（读者可能当成「2.75 折支持」），**不是被证伪的数字**。

---

## 6. 已确认：`LATEST.md` 重写**静默丢失 11 条历史报告索引**

`92a4d17` 对 `LATEST.md`：`26 insertions(+), 78 deletions(-)`，89 行 → 37 行。

**被丢弃的报告引用（11 条）**：`2026-09-19_04-05-29`、`2026-09-20_02-04-16`、`03-06-03`、`06-02-02`、`09-58-00`、`13-57-11`、`15-34-32`、`18-03-00`、`22-00-19`、`23-50-06`、`2026-09-21_03-50-40`。同时丢弃 **24 个 SHA/hex 令牌**，以及若干**风险与开放项陈述**（如「由人类决定注册 `scripts\register_fixup_task.ps1`」、回归计数 `FIXED 1 / STILL_OPEN 8 / NOT_REPRODUCED 1`、第三方可复现性边界等）。

**缓解（我验证有效）**：新文件 `:33` 声明「本次不删除任何历史报告」，`:35` 把更新前的完整索引固定在 permalink
`https://github.com/fy-god/pro-web-60d-strategy/blob/c56f89865d777f525118112bd16ea753de44efc5/docs/audits/expert-ml/LATEST.md`
—— 我用 `git cat-file -e c56f898:docs/audits/expert-ml/LATEST.md` 验证**该 permalink 在指定提交处可解析**；且 **11 个 `.md` 文件本身全部仍然存在于 `origin/main`**（未删除任何报告文件）。

**判定**：**已确认**（索引导航回退，非文件丢失）。被审报告正文**未提及**这一点。

---

## 7. 已确认：`3 passed` 与两个候选脚本**不在仓库**（作者已如实标注沙箱）

`audit_holdout_contract.py`、`analyze_h10_stability.py`、`tests/test_candidate.py`、`holdout_contract_probe.json`、`h10_model_stability.json`、附件 zip：

- `git ls-tree -r --name-only origin/main`（2,190 文件）→ **0 命中**；
- `git grep` → 仅出现在**两份 `.md` 的正文里**（`2026-09-21_18-03-47_JST.md:82/:179`、`LATEST.md:12`）;
- `git log --all --diff-filter=A`（全部 164 个可达提交）→ **从未被添加**;
- zip SHA256 `295df8569525572319923e5f46c069882344c430358f755c53ee1a47ff50d609`、证据 `70e684f9…`、`b39f872d…` 均**无实体**。

**我实跑本仓测试：`15 passed in 5.33s`（exit 0），整仓 collect 也是 15 个** —— 与报告的 `3 passed` 是**不同测试集**（沙箱候选脚本的测试），故 `3 passed` **无法从仓库复现**。

**关键定性**：报告 `:19` 明写「本轮当前隔离沙箱实现并实跑两个候选工具……`pytest = 3 passed`」，`:175` 标题「本轮实际沙箱执行」。**作者主动标注了沙箱**，并未将其列为仓库交付物 ⇒ **这是「未复现（附件不在仓库）」，不是「造假」。** 我**不**把它计为已确认缺陷，也**不**允许它被当作项目证据引用。

**判定**：**未复现（附件不在仓库）**。

---

## 8. 子 agent 交叉核对与**我的更正**

### 8.1 四个只读子 agent 结果

| 子 agent | 任务 | 结果 | 我的处理 |
|---|---|---|---|
| `560c9eb6` | A 新提交审查 | **已返回**，篇幅完整 | 其每条结论我都独立复算；**无一条被推翻** |
| `eb5355c0` | B 未修项回归 | **已返回**，14 项逐条 | 采纳；**其中 KDJ 项与「0/80 提交触及源码」我亲自复核确认** |
| `ced62034` | C 机制与影响 | **本轮未完成**（见 §9） | 不计为证据 |
| `95cc5d6e` | D 独立证伪 | **已返回**，对抗性强 | 采纳；其「3 是可恢复口径」的洞察我已在 `:271-272` 亲自确认 |

### 8.2 我**亲自复核并采纳**的子 agent 关键结论

- **KDJ 测试嵌套（`EML-P2-TEST-KDJ-NESTED`）**：`tests/test_engine.py:306` 的 KDJ docstring **位于** `test_wilson_upper_bound_is_not_a_constant`（L268–319）**内部**;`test_kdj_continuous_across_year_boundary` **根本不是一个 `def`**。pytest 收集到 15 个 id，其中 KDJ 只有 `test_kdj_flat_window_is_neutral` 一个。**⇒ 15:48 报告把它标为「已修复／描述更正」是错的，该缺陷仍未修**。我用 AST 独立确认（`eml_h18_verifyB.py`）。
- **`0/80` 提交触及源码**：`c56f898..origin/main` 只有 2 个提交且**只改 `docs/audits/expert-ml/`**;`src/ scripts/ tests/ reports/ configs/ .github/` 变更数**全为 0**（我实跑 `git diff --name-only`）。
- **CI 窄 67.3%**：我复核发布宽度、比率与两个定义行（`block=5` @ `:251`、`block=1` @ `:254`）。

### 8.3 我**更正**的项

1. **子 agent B 的「worktree 写入」披露**：B 调用仓库自己的 `load_panel()` 时在冻结副本内**新建了 gitignored 文件** `data/panel_daily.parquet`（32,469,152 B）与 `data/panel_manifest.json`，另有 `.pytest_cache/`。我**已核实这些确为 gitignored**（`git check-ignore -v` → `.gitignore:2:data/*.parquet`、`:4:data/*.json`），`git status --porcelain -uall` 为空、HEAD 未变，**且我已把它们删除以恢复冻结副本的纯净状态**。**外层仓库未被触碰**（`reports/ml_final_holdout.json` 的 1,877 B 工作区文件与提交 blob 在 LF 归一化后 sha256 相同：`df05fd68…`;差异只是 `core.autocrlf=true` 的 CRLF，git 自身报该文件未修改）。**这是我的调度失误 —— 我给子 agent 的提示词允许它在 worktree 内运行仓库自己的入口，应明确禁止任何会产生文件的调用。已记录。**
2. **子 agent A 的「CV 11.28% 口径未声明」**：我亲自复算确认 —— **样本标准差（ddof=1）给 11.2831%**，总体标准差（ddof=0）给 **9.7714%**。报告未声明口径，属**表述不完备**，非错误。
3. **子 agent B 关于 POOLED `−32.38%` 的量级未复现**：B 测得去重后均值变化为 **+60.82%（向上）**，与旧报告的量级/符号不同 —— 方向（「pooled 未去重」）确认，**该具体量级标 `未复现`**，我不采纳任何一方，只记录。

---

## 9. 只读契约与子 agent 合规

- 冻结副本 `D:\ccc\_sched\work\eml-h18`（`92a4d17`）。**我未在该仓库或副本内创建/修改/删除任何 tracked 文件**;未执行 `commit`/`push`/`checkout`/`reset`/`stash`；未创建任何定时任务。
- 子 agent C（`ced62034`，机制与影响）**本轮未完成**：我在本报告推送后将其停止，它**未返回任何内容**，故其对应职责（机制/影响面）**未被覆盖**；我以自己 §5 的 jackknife 与 §3 的影响面分析部分替代，并如实声明。**不计为独立证据。**
- 涉及 gitignored 写入的披露见 §8.3 第 1 条;已删除恢复。
- 本轮**未修改任何源码**（`src/ scripts/ tests/ reports/ configs/ .github/` 零变更）、**未动任何排程**、**未触碰** `D:\xm\60日预测\reference\eventnet_test_results.json`、**未触碰** `scripts/register_fixup_task.ps1` 与 `.gitattributes`（其本地未提交改动在我 `merge --ff-only` 前后 sha256 完全一致：`e23a15911a02e25cc5c0a04f` / `72e79e485003f07368b8814a`）。

---

## 10. 建议的修复与验收测试

| # | 修复 | 验收测试 |
|---|---|---|
| 1 | 重跑 `python -m src.ml.final_holdout` 重新生成 `reports/ml_final_holdout.json`（真实需本地稠密矩阵，约 91–101 s） | JSON 含 4 键；`date_clustered_95` 等于当前 `block=5` 定义；且**不得**等于 `block1` 变体 |
| 2 | 实现 §4 的守卫（**必需集只取字面量键**）并纳入 `audit_reports.py` | 对 `n==0` 构造样本 **PASS**;删除任一字面量键 **FAIL**（变异测试已给出预期） |
| 3 | `LATEST.md` 恢复 11 条被丢弃索引（或在文件内保留一行「历史索引见 permalink」并列出被移除文件名） | 新旧索引的 `.md` 引用集合相等（差集为空） |
| 4 | `EML-P2-TEST-KDJ-NESTED`：把 KDJ 断言提为**独立 `def`** | `pytest --collect-only` 中 KDJ id 数 ≥ 2;删掉该 `def` 后测试数应减少 |
| 5 | 在报告中声明 `CV` 与「有效支持折」的**计算口径** | 复算者按声明口径能逐位复现 |
| 6 | 排名结论加限定：注明「**丢第 0 折冠军即易主**」 | 留一折重算的冠军集合若 >1 个，结论中必须出现该限定 |

---

## 11. 下一轮的三条研究思路（各给检验与量级/成本）

1. **把「折聚类」不确定性制度化到 H10 评价**（本轮已给出方法与首批数字）
   - 做法：在 `audit_reports.py` 增加一条 **留一折 jackknife 区间**，与 pooled 并列发布；并加一条「冠军稳定性」检查（留一折冠军是否唯一）。
   - 检验：若某 config 的诚实区间下界 ≤ 0，则**不得**在文档中单独发布其 pooled precision 而不带该区间。**成本：0 训练，纯归档 JSON 算术（秒级）。**
   - 量级：本轮已测出 HGB 三条下界**跌破 0**，`fam_rf_5` 区间 `[14.55, 27.13]%`。

2. **用「独立日期数」而非「信号数」做支持度**（修正 §5.4 的口径问题）
   - 理由：producer 自己已生产 `date_hhi` / `effective_dates`（本次报告正是它）—— 仓库**已拥有**「独立单位」概念，却只在 holdout 上用，**未用在模型搜索的折支持上**。
   - 做法：对 `ml_search_models.json` 的每折补记**独立交易日数**与**独立股票数**，以其 HHI 而非信号 HHI 折算支持度。
   - 检验：若某折信号多但独立日期少，两种口径应产生**可观测分歧**；若永远一致，则该指标无增量、应弃用。**成本：需重跑搜索以记录新字段（本机约 101 s 级，或离线从矩阵重算）。**

3. **给「陈旧产物」加机器可检的契约（本轮 §2 的推广）**
   - 做法：把 `:104`/`:106` 的守卫从「仅 holdout JSON」推广到**所有** `ARTIFACT_SHAPES` 产物：对每个产物记录其 **producer 字面量键集合**，缺失即 `ATTENTION`。
   - 检验：用 `scripts/scratch/report_integrity_mutation_test.py` 的既有变异风格，对每个产物各做一次「删键」变异，守卫必须失败。
   - 量级：现有多组产物（`ml_search_wide/ablation/concentration/crosssec_final/…`）；若其中**任何一个**存在同类漂移，则 629 检查的通过率是**虚假安全**。**成本：纯静态 AST，秒级。**

---

## 12. 声明

- 本报告**只上传审计文档与索引**;未推送代码/权重/配置/Actions/PR；未 force push（本轮对专家/ML 仓库仅做了一次 `merge --ff-only` 使本地追上 `origin/main`，**不产生任何提交**，且他人未提交改动 sha256 前后一致）。
- 上传后已用 `git ls-remote origin refs/heads/main` 回读，并核验本轮 diff **仅在 `docs/audits/expert-ml/**` 白名单内**（详见 §13）。
- 未手写或编造任何 SHA/hash/通过数/退出码;未运行的项（`validate_latest.py`）已写明原因并留空。
- `REAL_MARKET_NEW_FIT = 0`：本轮**没有**训练新模型、**没有**新增实股成绩；§5 的全部数字是**已归档产物的再分析**。
- 软件样本（`15 passed`、守卫变异）与实股结果（20.84% 等）**分开计数**。

## 13. 本轮上传与白名单核验（发布后回填）

- 报告发布提交：`ccdb9268238fbcd01ff3ce171611f4ca354fc611`（父 `92a4d17b7ec691692968f6adde97e14083349848`）。
- 本报告 blob（发布后可回读核对）：`docs/audits/expert-ml/2026-09-21_19-52-00_JST.md` 在本提交中的 blob 哈希，见下「回读」行。
- `git push origin main`：**退出码 0**，输出 `92a4d17..ccdb926  main -> main`。
- `git ls-remote origin refs/heads/main` 回读 = `ccdb9268238fbcd01ff3ce171611f4ca354fc611`；`git rev-parse origin/main` = 同值 ⇒ **local = remote，EQUAL = True**。
- **白名单核验**：`git diff --name-only c56f898..HEAD` = 仅
  `docs/audits/expert-ml/2026-09-21_18-03-47_JST.md`（他人提交）、
  `docs/audits/expert-ml/2026-09-21_19-52-00_JST.md`（本报告）、
  `docs/audits/expert-ml/LATEST.md`（索引）—— **非白名单路径 0 个 ⇒ WHITELIST PASS**。
- 本轮 diff 不含任何 `src/`、`scripts/`、`tests/`、`reports/`、`configs/`、`.github/` 变更；**未推送代码/权重/配置/Actions/PR**；**未 force push**。
- 他人未提交改动在本轮前后 sha256 一致（`scripts/register_fixup_task.ps1` = `e23a15911a02e25cc5c0a04f…`，`.gitattributes` = `72e79e485003f07368b8814a…`），保持原样未动。


## 14. 补充：子 agent C 返回后的**更大漂移**发现（我已独立复核）

> 子 agent C（`ced62034`）在本报告首次推送后才返回。它不改变 §0–§13 的基础结论，但**新增一个比被审报告所纠正者更大的缺陷类**。以下每一项我都用 tracked 产物独立复算确认；未能独立确认的部分明确标注。

### 14.1 【我的独立复核确认】**搜索报告的 producer/artifact 漂移比 holdout 的更大**

`src/ml/search.py:410-431` 的 `payload` 字面量有 **15** 个键（`:418 min_signals`、`:431 ranked_unfiltered` 等）。

| tracked 产物 | 顶层键数 | 相对 producer 字面量**缺失** |
|---|---:|---|
| `reports/ml_search_models.json` | 8 | **8**：`folds_requested`、`matrix`、`min_signals`、`oos_base_rate_basis`、`oos_base_rate_by_label`、`ranked_basis`、`ranked_unfiltered`、`stride` |
| `reports/ml_search_ablation.json` | 8 | **8**（同上） |
| `reports/ml_search_wide.json` | 11 | **4**：`min_signals`、`oos_base_rate_by_label`、`ranked_basis`、`ranked_unfiltered` |

**最严重的后果（我复核了 `ranked_unfiltered` 的可达性）**：`ranked_unfiltered` 是 producer 用来提供「排序前的 `--min-signals` 下限**没有藏掉任何东西**」这一**机器可读保证**的字段（`src/ml/search.py:344-345`、`:355`、`:358`、`:431`）。我实跑 `git grep -l ranked_unfiltered 92a4d17` —— 命中的只有 `TARGET_70PCT.md`、一份审计 md、以及 `src/ml/search.py` 本身；**tracked 的 `.json` 产物中命中数为 0**。⇒ **本项目当前没有任何机器可读的产物能独立验证「min-signals 过滤没藏掉候选」**，而 §5 的整个 H10 结论正建立在这些搜索产物之上。

**判定**：**已确认错误**（`EML-P1-SEARCH-REPORT-SCHEMA-DRIFT`，我新命名）。严重度**高于**被审报告纠正的 `EML-P1-HOLDOUT-ARTIFACT-SCHEMA-DRIFT`，因为它是**方法学保证的缺失**，不只是元数据缺失。

### 14.2 【我的独立复核确认】`ml_null_tests.json` 也漂移

producer `src/ml/null_tests.py:105`、`:135` 写 `leak_factor`；tracked `reports/ml_null_tests.json` 的键为 `['feature_auc','n_features','n_rows','noise_features','permuted_labels','verdict']` —— **无 `leak_factor`**；且 `permuted_labels` 子键为 `['max_precision','mean_base_rate','mean_precision','oos_base_rate','oos_precision','verdict']`，**无 `n_folds`、无 `per_fold_signals`**（我实跑确认两者均为 `False`）。子 agent C 称这三个键在 `8aa8bc3` 加入但从未重新生成产物；**该提交→键的对应关系我未独立复核，标 `未复现`**，但**键缺失本身我已确认**。

### 14.3 【我的独立复核确认】「约 8 个无信号会话」的理由是**陈旧**的

`src/ml/final_holdout.py:242-243` 的代码注释写「signal-dates-only 会丢掉无信号会话 —— **~158 个 holdout 会话中的约 8 个**」。但：

- tracked `reports/ml_final_holdout.json` 的 `distinct_dates = 150`（有信号的交易日数）；
- 子 agent C 只读重跑得 `holdout_calendar_sessions = 150`；
- ⇒ **无信号会话 = 150 − 150 = 0**。

而且 producer **自己的**方法字符串（`:314-317`）就用这个减法算出「including the **0** that carried no signal」。**同一文件里 `:242-243` 的「约 8」与 `:315` 的「0」互相矛盾**，`158` 在 `src/ml/final_holdout.py` 中**没有任何其它来源**（我 grep 确认仅此一处）。

**推论（重要）**：既然无信号会话为 **0**，则 §3 的 **67.3% 加宽完全来自 `block=1 → block=5`**，与「宇宙是否包含无信号会话」无关。被审报告与 15:48 报告都未做这个分解；我在这里补上。

**判定**：**已确认错误**（注释陈旧且与自身方法字符串矛盾；`EML-P2-ZERO-SIGNAL-COMMENT-STALE`）。这**不影响** 67.3% 这个数字本身（该数字正确），但**纠正了它的机制归因**。

### 14.4 【我的独立复核确认】`fam_rf_5` 与 `wide_rf_32` 是**同一次运行**

`reports/ml_search_models.json` 的 `fam_rf_5` 与 `reports/ml_search_wide.json` 的 `wide_rf_32` 在 `oos_precision`、`oos_signals`、`per_fold_oos`、`per_fold_signals` 上**逐位相同** ⇒ **同一运行以两个名字发布**。且**宽网格最大值就是它**（58 个 config 中无一超过 `0.20837888660515153`）⇒ **20.84% 是该家族的极大值**，即**存在选择效应**（7 models + 58 wide 个配置中取最大），这与 §5.3 的「丢一折即易主」互相印证。

### 14.5 【我采纳并更正自己的表述】调度器

- C 实跑 `schtasks /query /fo CSV /v` 确认 **`\ProWeb60d-ReportAudit` 确实已注册且运行中**（`Next Run 2026/9/21 19:15:00`，每 4 小时，`Last Result 0`），运行 `scripts/run_report_audit.cmd` → `scheduled_report_audit.py:300` → `audit_reports.py`。
- **我先前表述不精确**：§8.2 与 `LATEST.md` 沿用了「无调度器」的说法。**准确表述应是**：`ProWeb60d-Fixup` 确实**未注册**（这一点此前正确）；但**另有一个 `ProWeb60d-ReportAudit` 在跑，只是它只做报告一致性检查、不跑任何 producer**。我复核了 `scripts/audit_reports.py` 的 import 列表（10 个，全为标准库）与 `subprocess` 提及数 **0**；`.github` 目录**不存在**。⇒ **没有任何排程会执行 `final_holdout.py` / `search.py` / `null_tests.py`**。**这是我本轮需要更正的第二处表述。**
- 同时更正 §3 的一个**转述不精确**：`date_clustered_95` 本身**不会**写进 `reports/AUDIT_STATUS.md`（写进去的是 precision/base/lift/signals）。因此 15:48 报告「AUDIT_STATUS 发布的就是这个窄区间」的说法**不精确**；准确说法是：**该 JSON 每 4 小时被自动读取并校验，其头部点估计进入 AUDIT_STATUS，而窄区间留在 JSON 与其引用文档（`README.md`、`TARGET_70PCT.md`、`docs/REVIEW_RESPONSE.md`）里。**

### 14.6 【我采纳并复核】「有效支持折」不是 pooled precision 的泛化不确定性

子 agent C 指出 `1/HHI`（信号份额）度量的是**信号数量在折间的分散度**，而非 pooled precision 被钉住的程度；正确量应是**折间 precision 方差**。C 给出折为单位的 SE 与 t(3) 区间（RF `[16.75%, 24.93]%`，约为朴素区间的 4.79×）。**这与我在 §5.2 用 delete-one-fold jackknife 得到的结论同向且量级一致**（我：`[14.55%, 27.13]%`，11.95×），两种独立方法都指向「诚实区间远宽于朴素区间」。方法差异（C 用折间方差 + t(3)，我用留一折 jackknife）导致数值不同，**两者都是估计，我不合并成一个数**；共同结论：**RF 在两种度量上都确实比 HGB 更稳定，故被审报告的方向性判断成立**，但**理由是折为单位的不确定性，而非被审报告 `:160` 所述的样本重叠**。

### 14.7 子 agent C 的独立性声明
C 报告冻结副本 `eml-h18` 全程 `92a4d17`、前后 `git status` 干净、两份 tracked JSON（`ml_final_holdout.json` sha256 `0ef45052…`）在其只读重跑前后哈希不变。**我无法替代验证其沙箱外行为**，但可确认：**当前专家/ML 仓库工作树在 C 返回后仅含他人 WIP，冻结副本干净**。
