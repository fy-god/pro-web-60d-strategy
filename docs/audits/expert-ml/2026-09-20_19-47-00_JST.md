# 专家／ML 审计：open 项回归 + 两份新报告核验 + 已发布产物重复计数

- `publication_kind`：`INDEPENDENT_REGRESSION_AND_NEW_REPORT_VERIFICATION`
- `audit_time_jst`：`2026-09-20T19:47:00+09:00`（北京时间 18:47）
- `repository`：`fy-god/pro-web-60d-strategy`
- `reviewed_source_ref`：`origin/main`
- `reviewed_source_sha`：`cb785b8983c2ae3a21e049d74cbff46c462e3901`
- `reviewed_tree_sha`：`a7ee77503039a14bd15ed17f1d4c1d42703d6991`
- `previous_report`：[`2026-09-20_15-34-32_JST.md`](./2026-09-20_15-34-32_JST.md)（`reviewed_source_sha` = `c9b90e37f68c0415331ca61e4e8acc3b13ce0bdf`）
- `main_head_at_audit_start`：本地 `b2fe93619f1b8c81733432abc0f9ce85503ae2fb`（落后远端 4 个提交）
- `report_created_commit_sha`：写完回读后补
- `execution_status`：`INDEPENDENT_REGRESSION_EXECUTED_IN_FROZEN_WORKTREE`
- `research_verdict`：`NO_NEW_REAL_MARKET_RESULT`（本轮零次 H504 实股拟合）
- `evidence_status`：软件样本 `EXECUTED`；实股证据来自**已归档** `data/panel_daily.parquet` 与 `outputs/webpro_signals.csv`（`ARCHIVED_REAL_MARKET_REEVAL`，非本轮新拟合）
- 本轮实股 fit = **0**；本轮**没有**新增 precision/recall/AUC/收益数字。用户电脑未由本对话启动。

---

## 0. 结论摘要

本轮审 `c9b90e3 → cb785b8` 的 **9 个提交**（`git rev-list --count` 实测），
其中 2 份是 `fy-god` 新写的报告（`17:31:44` ARR、`18:03:00` MEB），
均系**外部工具写入、非本审计线所写**；其余 7 个为文档/索引提交。
`src/ experts/ tests/ scripts/ data/` 在整段区间**零变更**。

**最重要的三件事：**

1. **【本轮新发现 · 已发布产物】** `README.md:186-191` 与 `RESULTS.md:23-28` 的
   "top 6 by lift" 表，**6 行只对应 3 个机制**（`leader_momentum`、`relative_strength_rank`、
   `gap_follow_through` 各占 2 行；其中 4 行是 `strict_*` 克隆，**3 行属重复计数**）。
   克隆重复计数**不是未来风险，而是已发布文档里当下可见**的问题。
2. **【自我纠正】** 本审计线**上一份报告**（`15:34`）写的
   "KDJ 从未执行" 是**错的**。实测：把 `tests/test_engine.py:317` 的 KDJ 断言
   改坏后，失败点落在 `test_wilson_upper_bound_is_not_a_constant`，
   且 stdout 先打印 `ok wilson_...`（见 §7.1）—— 证明 KDJ 断言**确实在执行**。
   正确的说法是"**已执行但未被独立收集/归因**"。该条**降级**为 `未复现`。
3. **【新报告执行证据不可复算】** `17:31` 报告自报的 `49 passed`、12 次合成 fit、
   1,048,576 行压力测试、两个 SHA256 —— 其候选包 `arr_candidate` 与 `evidence/`
   **在全部 3,060 个 git 对象中从未存在**；`python -m arr_candidate.scan_real --help`
   → `ModuleNotFoundError`，**exit 1**；两个 SHA256 只出现在该报告 Markdown 自身之内。
   判为 `UNVERIFIABLE`（**不判为造假** —— 无证据证明其编造，只是**无法复算**）。

**回归核对结果（8 项）：6 项仍 OPEN、2 项 `未复现`（需降级）、0 项已修复。**

---

## 1. 本轮实际跑过的命令与真数

全部在**冻结 worktree** `D:\ccc\_sched\work\eml-h18`（@`cb785b89…`，`git status --porcelain` 前后均为空）执行。

```text
python -m pytest -o addopts="" -p no:cacheprovider -q
  -> 15 passed in 4.45s      REAL exit 0
python -m pytest ... --collect-only tests/test_engine.py
  -> 15 tests collected in 0.65s   REAL exit 0
python -m pytest ... -q -v tests/test_engine.py::test_kdj_flat_window_is_neutral
  -> 1 passed in 0.65s       REAL exit 0
python docs/audits/validate_latest.py
  -> 不存在（见 §7.3；该文件在全部可达历史中从未存在）
```

**本仓库无 `addopts`、无 `conftest.py`**（已核验 `pyproject.toml` / `pytest.ini` / `setup.cfg` / `tox.ini` 均无命中）。

本轮为核验共写 **62 个探针脚本**（scratch `D:\ccc\_sched\work\eml_h18_main\`）。
**并非全部 exit 0**，如实说明：

- 绝大多数 exit 0（复核性探针）。
- `verify_pooled4.py` 前两次 exit 1：先缺 `open` 列、后遇 `str` vs `datetime64` 合并键，
  补上列与 `pd.to_datetime` 后第 3 次 exit 0（结果即 §5.3 的数字）。
- `verify_rsi5.py` exit 1：`indicators` 字典里**没有** `rsi14` 键（RSI 由
  `relative_strength_index(bars, 14)` 现算）；改用真实计算后
  `verify_rsi7.py` exit 0。
- `verify_kdj_mut.py` **故意** exit 1 —— 那是变异测试的**期望结果**（§7.1）。
- 另有 4 个只读子 agent 的独立脚本。

**未运行**的项已在各条下注明原因。

---

## 2. 【仍 OPEN · 实股可达】`EML-P0-ENTRY-ROW-ADJACENCY-ON-REAL-PANEL`

**文件/行**：`src/labels.py:124-128`（blob `0e4f63fc…`）

```python
124:    entry = np.full(n, np.nan, dtype="float64")
125:    entry[:-1] = opens[1:]                       # <-- 个股"下一行"，不是"下一市场日"
126:    same_next = np.zeros(n, dtype=bool)
127:    same_next[:-1] = codes[1:] == codes[:-1]     # <-- 只检查个股边界
128:    entry[~same_next] = np.nan
```

**触发条件/反例（我本人实跑）**：同一股票在市场日中缺一天。

```text
输入: A 2024-01-02 (open 10.0) ; A 2024-01-04 (open 12.0)   [01-03 停牌]
输出: 行0 entry_open = 12.0
真实合约: 01-03 无行情 => 无 entry => 该行应不可判定
```

**机制**：`entry[:-1] = opens[1:]` 取的是**该股在面板里的下一个观测行**，
只被 `codes[1:] == codes[:-1]` 挡住"跨股票"，**不检查中间是否缺市场会话**。
停牌后复牌日被当成"入场日"。

**影响（真实面板 `data/panel_daily.parquet`，2,680,715 行 / 3,193 股 / 887 市场日）**：

| 量 | 我实测 | 说明 |
|---|---:|---|
| 个股行相邻但市场日不相邻的边 | **943** | 与上轮报告一致 |
| 涉及股票数 | **586** | 上轮报 `596`，见 §6 口径说明 |
| 其中复牌跳空 \|gap\|>5% | **530** | 严格 `>` |
| \|gap\|>9.5%（涨跌停级） | **296** | |
| 落在 2026 年 | **260** | 已发布 holdout 窗口 |
| 年度分布 | 2023:172 / 2024:184 / 2025:327 / 2026:260 | **与上轮报告逐年精确一致** |

**修复建议**：`entry` 应经**独立市场日历** join（`next market session`），
而不是 `shift(-1)`；缺 session 时不造 entry。

**验收测试**：构造"个股缺 1 个市场会话"的帧，断言该行 `entry_open` 为 NaN。
当前 `tests/` 内**无**此断言（`--collect-only` 15 条，无 entry-vs-halt 用例）。

---

## 3. 【仍 OPEN · 已确认错误】H504 `Close > 4E` 合同未被数值实现

**文件/行**：`src/labels.py`。`close` 分词在全文件**仅 1 次，且在 L11 docstring**；
数值路径全部用 `high`（`L118 highs`、`L182 newly`、`L199/L201 bull`、`L212-213`）。

**反例（我本人实跑，合法 OHLC）**：

```text
E = 10.50  (次日 open)   ->  4E = 42.00
次日 bar: high = 43.0 , close = 41.0 , low = 9.0
代码 label_bull = 1.0
按 Clsoe 合同 (close 41.0 > 42.0 为假) 应为 0.0
=> 判据分歧：代码跟 "High 判据" 一致、与 "Close 判据" 分歧
```

**影响**：H504 的 `Close > 4E` 语义未被实现；用 `high` 判会**高估**命中，
在"上影线触及但收盘未站上"的 bar 上系统性偏乐观。

**修复建议**：为 H504 增加 `close` 通道（`fwd_max_close`），
并把 `label_close` 与 `label_high` 显式区分。

**验收测试**：`high=43 close=41` → `label_bull == 0.0`。当前**无此测试**。

---

## 4. 【仍 OPEN · 已确认错误】cooldown 用帧内时钟而非市场会话时钟

**文件/行**：`src/labels.py:222-246`，时钟在 **L233**：

```python
233:    session_index = {d: i for i, d in enumerate(sorted(out["date"].unique()))}
234:    positions = out["date"].map(session_index).to_numpy()
```

**反例（我本人实跑）**：

```text
两次信号: 2024-01-02 与 2024-05-07（真实相隔 90 个市场会话）
cooldown = 60 -> 真实距离 90 >= 60，第二条应当保留
dedupe_signals 实际返回 1 行（只留第一条）
```

分歧表：

```text
真实间隔(会话)  市场判定(>=60)  帧内距离  帧判定   一致?
      10            False          1      False    ✅
      59            False          1      False    ✅
      60             True          1      False    ❌ 分歧
      90             True          1      False    ❌ 分歧
     200             True          1      False    ❌ 分歧
```

**机制**：`session_index` 建在**传入帧自己的 unique date** 上。
帧越稀疏，会话距离被压得越小 → **过度去重（丢真信号）**。
实测（N=955 市场日、200 个稀疏信号日）：帧内时钟保留 **8** 条，
市场时钟保留 **29** 条 —— 差 21 条。

**影响**：`reports/webpro_hit_rates.csv` / `lowzone_hit_rates.csv` 的
`signals_deduped` 与 `*_precision_deduped` 全部经由该函数。

**修复建议**：把市场日历作为显式入参传入 `dedupe_signals`，
`session_index` 由该日历构建。

**验收测试**：同一股票两次信号真实相隔 ≥ cooldown，断言两条都保留。

---

## 5. 【本轮新发现 · 已发布产物重复计数】克隆策略在已发布排名里被重复计数

> 本条由只读子 agent **C** 与 **D** 分别独立发现，**由我本人逐项复现**。

### 5.1 机制：`strict_*` 是同一 raw score 的阈值重切

`experts/strategies/_strict_selector.py:19-24`：

```python
19:    base = base_predict(card)
20:    if base.sample_id != card.sample_id:
21:        raise ValueError("base strategy returned a mismatched sample_id")
22:    rationale = (
23:        f"Strict selector reuses {base.strategy_id} score={base.score:.4f}; "
```

`thresholded_decision()` 原样复制 `base.score` / `base.components`，只改二值阈值。
`experts/registry.list_strategy_ids()` 实测返回 **36** 个；
其中 **16 个**文件带 `BASE_STRATEGY_ID`，指向 **11 个**base。

**我本人的 bit-identity 实证**（100 张真实卡片 × 16 个克隆 = **1,600 次比对**）：

```text
score 与 components 同时逐位相同 = 1600 / 1600
失败 = 0
=> bit-identity 成立（克隆与 base 的连续分数完全一致）
```

### 5.2 已发布影响（**本条是本轮最严重的已发布发现**）

`README.md:186-191` / `RESULTS.md:23-28` 的 **top 6 by lift**：

| 发布排名 | 策略 | 归约到的机制 | 是否克隆 |
|---:|---|---|---|
| 1 | `leader_momentum` | `leader_momentum` | base |
| 2 | `strict_leader_momentum_v2` | `leader_momentum` | **克隆** |
| 3 | `strict_relative_strength` | `relative_strength_rank` | **克隆** |
| 4 | `strict_relative_strength_v2` | `relative_strength_rank` | **克隆** |
| 5 | `strict_gap_follow_through` | `gap_follow_through` | **克隆** |
| 6 | `gap_follow_through` | `gap_follow_through` | base |

**6 行 → 3 个机制。**`reports/webpro_hit_rates.csv` 35 行中 **16 行是克隆**。

**影响**：排名表让读者以为有 6 个独立证据，实际只有 3 个；
"top of the ranking" 的**多样性**被高估 2 倍。
这是**已发布文档的可读性/统计口径缺陷**，不是训练标签错误。

**修复建议**：排名表按**机制（base）**去重后呈现，
或用 `family` 列标注并注明"N 行属 M 个机制"。

**验收测试**：排名表加一个断言 —— 行数 == 独立机制数，或显式打印两者。

### 5.3 同源问题：`live_readiness.py:155` 的 pooled 行未去重

```python
155:        block = assess(webpro, panel, horizon, f"ALL Web Pro signals, hold {horizon}")
```

`webpro` 是**全部 646,718 行**、**未做任何 dedupe**。

**我本人实跑（复算发布值并量化）**：

```text
含克隆   signals=646,718  resolved=637,499  gross_mean=0.004466  net_mean=0.003446
发布值              646,718          637,499            0.004466        0.003446   => 复现一致
去克隆   signals=292,207  (移除 354,511 = 54.82%)  gross_mean=0.003020  net_mean=0.002000
事件去重 signals=287,436                          gross_mean=0.007182  net_mean=0.006162
```

即：**克隆策略行占 pooled 的 54.82%**；
按 base 去克隆后 pooled `gross_mean` 从 `0.004466` 变为 `0.003020`（**−32.38%**）。

**修复建议**：pooled 口径要么按机制去克隆、要么按事件去重，并在标签里写明用了哪种。

---

## 6. 回归核对表（上轮 8 项，逐条实测）

| # | 项 | 状态 | 关键证据 |
|---:|---|---|---|
| 1 | `EML-P0-ENTRY-ROW-ADJACENCY-ON-REAL-PANEL` | **仍 OPEN** | 943 边 / 586 股 / 逐年 172-184-327-260 精确复现 |
| 2 | H504 `Close>4E` | **仍 OPEN** | `close` 仅 1 次（L11 docstring）；high43/close41 → bull=1.0 |
| 3 | `EML-P0-COOLDOWN-INDEPENDENT-CLOCK` | **仍 OPEN** | 真实距离 90 ≥ 60 却只留 1 条 |
| 4 | `EML-P0-AUDIT-FETCH-FAIL-PASS` | **仍 OPEN** | L108 `ok = code == 0 and not unstable` 不看 `note` |
| 5 | `EML-P1-RSI-META-LEAK` | **仍 OPEN（潜伏）** | 黑名单只按**精确列名**排除：注入 18 个合同/未来列（`label_joint`/`future_bars`/`forward_max_return`/`ret1`/`outcome`/`known_at`…）→ **18/18 全被接纳、0 被拒**（子 agent B 独立注入 10 个亦得 10/10）；`select_features(cols,[]) == list(cols)` 为 True；但真实矩阵 82 列**无**未分组列 → 当前运行时不可达 |
| 6 | `EML-P2-TEST-KDJ-NESTED` | **未复现（自我纠正）** | 见 §7.1 |
| 7 | RSI 截断 | **仍 OPEN** | `rsi_mean_reversion.py:46` `_clip((45.0 - rsi) / 20.0)`；100/100 解析、**rsi14>45 = 97/100 精确**、min/mean/max = **42.22/67.38/100.00 精确**、score = **0.0354/0.2095/0.4926 精确**、fires = **0/100** |
| 8 | 研究队列基础设施缺失 | **仍 OPEN** | `experiment_registry`/`fold_support`/`research_h504`/`FeatureSpec`/`market_session_id`/`TaskSpec` 在 `src/ experts/ tests/` 命中 **全部为 0**；`src/ml/research_h504/` 不存在 |

### 6.1 我发现的上轮报告口径错误（必须更正）

- **"596 只股票"应为 586**。我实测 `df.loc[缺口边, 'code'].nunique() == 586`。
  为定位 596 我穷举了多种分母，**没有一种给出 596**：

  | 口径 | 结果 |
  |---|---:|
  | 缺口边涉及的 distinct code | **586** |
  | 个股自身 span 内缺 ≥1 session | **586** |
  | 个股自身 span 内缺 ≥2 / ≥3 | 450 / 364 |
  | 以全市场 887 日为分母缺 ≥1（含**上市前**期间） | **901** |

  → 586 是**停牌口径**的正确值；901 含上市前空白，**不可**用于"停牌"叙述。
  **596 我未能复现，标记为 `未复现`**（子 agent B 与 D 分别报 586 与 596，
  我采信可复现的 **586**）。上轮报告该数的来源已不可考。
- **"|gap|>5% = 530"**（严格 `> 0.05`）—— 我实测为 **530**，
  且 `>= 0.05` 同为 530（恰好等于 0.05 的 18 条按浮点落在两侧）。
  子 agent B 报 531，**我未能复现 531**；采信 **530**。
- 该条其余量我**逐个复现精确一致**：`>9.5% = 296`、`>10% = 206`、
  缺口长度 mean **4.80** / max **46**、长度计数 2:524 / 6:136 / 11:122、
  跳空分位 median **0.0503** / p90 **0.1008** / max **0.1137**、`>20% = 0`。
- 上轮报告该条另称 "Δ = 0.4588 pp" 等 — 本轮**未复算**（需合同市场日历，见 §12）。

### 6.2 【新增 · 潜伏】黑名单按 H10 命名写，对 H504 列基本失效

`src/ml/walkforward.py:108-109`：

```python
108: def feature_columns(frame: pd.DataFrame) -> list[str]:
109:     return [c for c in frame.columns if c not in META_COLUMNS]
```

`META_COLUMNS` 实测 **9 项**：
`code, date, entry_open, fwd_max_close, fwd_max_high, fwd_min_low, label_close, label_high, resolved`
—— 这是 **H10 旧命名**。

而 `src/labels.py:forward_outcomes`（H504 路径）产出的是
`label_bull, label_strict_low, label_joint, label_resolved, forward_max_return,
forward_min_return, future_bars, bars_to_target`。

**实测交集只有 `entry_open` 一项 —— 9 个 H504 新列里 8 个不在黑名单**：

```text
forward_outcomes 新增列 9 个，其中被黑名单覆盖 1 个（entry_open），未覆盖 8 个
feature_columns(h504_out) 接纳 12 列，其中 8 列是 H504 未来标签
```

**当前是否可达**：**否**。`src/ml/build_matrix.py:352` 把 H504 产出**改名**为
`fwd_max_high / fwd_min_low / label_high / label_close / resolved`（`:360-363`、`:427-434`），
即**回写成 H10 名字**，并由 `:520-524` 的显式集合排除。
所以现有矩阵是干净的（82 列实测**无** `label_/outcome/known_at` 等）。

**风险**：任何**直接用** `forward_outcomes` 原始输出（改名之前）的消费者会静默纳入 8 个未来列。
这是一个**命名契约不一致**造成的潜伏陷阱。

**修复建议**：`META_COLUMNS` 改为按**语义**判定
（如 `label_*` / `fwd_*` / `forward_*` / `*_resolved` 前缀 + 显式未来字段清单），
或让 `forward_outcomes` 直接产出 H10 的规范名。

**验收测试**：断言 `feature_columns(forward_outcomes(frame))` 不含任何 `label_*`。

---

## 7. 两份新报告的核验

### 7.1 【自我纠正】上轮的 "KDJ 从未执行" 是错的

上轮报告（`15:34`）写：

> KDJ 块 L306-319 是其 4 空格缩进尾部，无独立 `def`；强制 Wilson 常量后
> `AssertionError: 0.8` 且 stdout 为空 → **KDJ 从未执行**。

**我把该命题拆成两半分别实测：**

```text
(a) 结构命题：L306-319 属 test_wilson_upper_bound_is_not_a_constant (L268..L319)
            该函数名不含 kdj；顶层 def 18 个，名字含 kdj 的仅
            test_kdj_flat_window_is_neutral
    ->  (a)【成立】：确实无独立 test id

(b) 因果命题：正常运行该函数时 KDJ 块是否执行？
    打桩计数 + 变异测试（我把 L317 的 `< 10` 改成 `< 0` 再跑）：
      ok  wilson_upper_bound_is_not_a_constant      <-- 先通过
      ...tests/_mut_kdj_check.py:317: AssertionError: KDJ jumped across the year boundary
      FAILED tests/_mut_kdj_check.py::test_wilson_upper_bound_is_not_a_constant
      1 failed, 14 passed in 0.82s   REAL exit 1
    ->  (b)【不成立】：KDJ 断言【确实在执行】
```

**为何上轮会误判**：变异失败时，报告只看到 `AssertionError: 0.8`（Wilson 的错），
便推断"KDJ 没跑"。但 stdout 的 `ok wilson_...` 与 L317 行号**证明两者在同一次调用里依次执行**；
上轮把"被前面的失败遮蔽"误读成"从未执行"。

**正确表述**：KDJ 断言**已执行但未被独立收集/归因**；
只有当前面的 Wilson 断言先失败时，它才会被遮蔽。

**独立佐证**：子 agent **B** 独立做了同一变异测试，
得到同样的 `FAILED ...::test_wilson_upper_bound_is_not_a_constant`（1 failed, 14 passed）。

> 因此本条**从 `已确认错误` 降级为 `未复现`**。
> 它的**真残余**是测试**隔离性**缺陷（无独立 test id、失败会互相遮蔽），
> 不是"代码没被测到"。
> **对本审计线自身的教训**：这是本线第一次把"未观察到"当成"未发生"，
> 属**方法性错误**，已记入 §14 自我纠正。

### 7.2 `17:31` ARR 报告：执行证据不可复算（`UNVERIFIABLE`）

该报告自称完成了 `49 passed`、4 类变异红测、1,048,576 行合成压力、
6 组 HGB / 3 组 MLP / 3 组 TCN 共 12 次合成 fit，并给出两个"可复算指纹" SHA256。

**我本人的核验结果：**

```text
python -m arr_candidate.scan_real --help -> ModuleNotFoundError,  REAL exit 1
python -m arr_candidate.train     --help -> ModuleNotFoundError,  REAL exit 1
python -m arr_candidate.data      --help -> ModuleNotFoundError,  REAL exit 1

git rev-list --all --objects | 3060 个对象
  命中 arr_candidate / evidence/ 的对象 = 0
git log --all -S arr_candidate -> 仅 f54db37（即新增该报告的那个提交）

两个 SHA256 在 HEAD 的命中文件：
  260fa4d6…1a81c3a -> docs/audits/expert-ml/2026-09-20_17-31-44_JST.md（仅自身）
  cec9d85f…c203681 -> docs/audits/expert-ml/2026-09-20_17-31-44_JST.md（仅自身）
仓库唯一的 npz:
  outputs/ml/audit/real_oos_scores.npz  sha256=679f3231…  （与两者都不匹配）
```

**结论**：`execution_status` 自述与仓库事实**不一致**，
但**我无法证明其造假** —— 只是**无法复算**。判 `UNVERIFIABLE`，
不写入任何"已确认错误"，并**不采信**其 49 passed / 12 fit / log-loss 表。

### 7.2.1 【已确认 FALSE】`17:31` 第 37 行把"黑名单"描述成"显式 FeatureSpec 白名单"

这是该报告中**唯一可被判为 FALSE 的事实性陈述**（子 agent A 发现，**我本人复核原文并复现**）。

**报告 L37 原文**（链接锚点文本本身就写着"黑名单"）：

> - [walkforward**元数据黑名单**与分组](…/src/ml/walkforward.py#L52-L106)：
>   研究适配使用**显式 FeatureSpec**，不允许未来合同字段默认进入X。

**真实代码**（`src/ml/walkforward.py`，blob `9b6f3d3b…`）：

```python
58: META_COLUMNS = {
59:     "code", "date", "entry_open", "fwd_max_high", "fwd_min_low",
60:     "fwd_max_close", "label_high", "label_close", "resolved",
61: }
108: def feature_columns(frame: pd.DataFrame) -> list[str]:
109:     return [c for c in frame.columns if c not in META_COLUMNS]
```

```text
git log --all -S FeatureSpec -- *.py   -> 无输出（131 个提交的全部 .py 中从未出现）
git grep -i feature_spec HEAD          -> exit 1（无匹配）
```

**判定 FALSE 的理由**：该区间是**黑名单**（不在 9 列中的**全部**当特征），
与"不允许未来字段默认进入 X"的**白名单语义方向相反**；
且 `FeatureSpec` 在**代码层从未存在**（这与本审计线自己 3 小时前
`15:34` 报告 L398 `| FeatureSpec | 35 | 0 |`、L403「在代码层完全不存在」的结论一致）。
**加重情节**：这会把一个**尚未修复的泄漏风险**描述成**已落地的防护**，
可能直接误导修复者。

**旁证**：`walkforward.py:222` `feature_groups` 默认空，
`:300-301` `if not groups: return list(columns)` —— 空分组放行全部列，
进一步证明**不存在**"默认拒绝"结构。

### 7.2.2 我本轮**无法核验**的项（如实列出）

- `current_open_pr: 0`：GitHub API 返回 `403 rate limit exceeded`；
  本机 DNS 把 `github.com` 解析到非公网地址（`198.18.0.31`），`web_fetch` 被拒。
- `17:31` 声明的他机沙箱环境版本（Python 3.13.5 / NumPy 2.3.5 / PyArrow 不存在）：
  本机实测 Python 3.13.12 / numpy 2.4.4 / **pyarrow 20.0.0 存在** —— 属**另一台机器**，无法证伪。
- 两份报告对"沙箱能否运行 Python"的**互相矛盾**：`17:31` 第 12 行
  `EXECUTED_IN_AUDIT_SANDBOX`，而 30 分钟后的 `18:03` 第 13 行写沙箱
  `container/Python` **仍**返回 `ClientError`。**两者不能同时为真**，
  我无日志可判，列为**待作者澄清项**。


### 7.3 `18:03` MEB 报告：机制正确，量化不足，且未引先例

**成立的部分**（我已独立复现）：36 个注册策略中 16 个是真阈值克隆；
6 个 `predicted_yes_count==0` 的策略名单**完全正确**。

**不足与夸大（我逐条复现）：**

1. **量化低报**：报告只举 2 个家族/4 个克隆，真实是 **11 个家族 / 16 个克隆**；
   且从未给出"36 → 20 个独立家族"这个关键结论。
2. **"新证据"框架不成立**：同一事实早已记载于
   `audit/OTHER_PROJECTS_AUDIT.md:461`（引入提交 `bda8f3d`，日期 **2026-09-16**）：
   > "with `_strict_selector.py:17` showing the 16 `strict_*` variants are the same signal re-thresholded"

   我实测 `18:03` 报告对 `OTHER_PROJECTS_AUDIT` / `2026-09-18_18-59-36` /
   `2026-09-19_18-00-42` / `bda8f3d` 的引用命中数**全部为 0**。
   故该发现是**重申**而非新发现，**应补引先例**。
3. **6 个"恒零"不可外推到真实面板**：`data/cards_100` 是
   `ly.kline_practice_session.v1` 的**人为 50/50 平衡演练集**（真实 low504 基率仅 2.49%）。
   我实测这 6 个策略在**真实** `outputs/webpro_signals.csv` 上的行数：

   | 策略 | 100 卡 | 真实面板 |
   |---|---:|---:|
   | `accumulation_base` | 0 | **0**（唯一真恒零） |
   | `first_board_breakout` | 0 | 5,803 |
   | `rsi_mean_reversion` | 0 | 25,614 |
   | `turnover_weak_to_strong` | 0 | 269 |
   | `high_level_consensus` | 0 | 169 |
   | `reversal_engulf` | 0 | 63 |

   → 真实 population 上"恒零票"约 **1/36（2.8%）**，不是 16.7%。
4. **"strict" 前缀是误称**：实测 16 个克隆中至少 1 个
   （`strict_gap_follow_through_v2` 阈值 0.690867）**低于**其 base（0.72），
   其预测集合是 base 的**超集**。报告沿用该措辞不准确。

---

## 8. 三项分离

### 8.1 程序修复
**本轮零次程序修复。** 本审计线只新增审计文档与索引，未改任何产品代码/配置/权重。

### 8.2 任务定义变更
**无。** 未修改 launcher、未修改排程、未改 `scripts/fixup_prompt.txt`。

### 8.3 真实模型增益
**零。** 本轮 **H504 实股拟合 0 次**，**没有**新增任何
precision / recall / AUC / 校准 / 收益数字。
§5 与 §6.2 的发现是**计数口径与命名契约**问题，不产生模型增益。

---

## 9. 状态三轴

| 轴 | 值 |
|---|---|
| `execution_status` | `INDEPENDENT_REGRESSION_EXECUTED`（15 passed exit 0；62 个探针脚本中除 4 个已说明的失败/故意失败外均 exit 0；4 个只读子 agent 全部完成） |
| `research_verdict` | `NO_NEW_REAL_MARKET_RESULT`；本轮无新拟合、无新成绩 |
| `evidence_status` | 软件样本：`EXECUTED`；实股：`ARCHIVED_REAL_MARKET_REEVAL`（复用已归档面板与既有 signals，**非**本轮新拟合） |
| `evidence_type` | 计数与合同类发现 = `ARCHIVED_REAL_MARKET_REEVAL`；ARR 合成类 = 无法归类（`UNVERIFIABLE`，产物不存在） |

---

## 10. 【本轮新发现汇总】

| ID | 级别 | 一句话 | 状态 |
|---|---|---|---|
| `EML-P1-CLONE-DOUBLE-COUNT-PUBLISHED` | P1 | `README.md:186-191`/`RESULTS.md:23-28` top-6 只含 3 个机制；`webpro_hit_rates.csv` 35 行含 16 克隆 | **新增·已确认** |
| `EML-P1-POOLED-NO-DEDUP` | P1 | `live_readiness.py:155` pooled 未去重；克隆行占 54.82%；去克隆后 gross_mean −32.38% | **新增·已确认** |
| `EML-P1-META-BLACKLIST-H10-NAMES` | P1（潜伏） | 9 项黑名单只覆盖 1/9 个 H504 列；当前经 `build_matrix.py` 改名而不可达 | **新增·潜伏** |
| `EML-P2-REPORT-FEATURESPEC-FALSE` | P2 | `17:31` 报告 L37 把"黑名单"写成"显式 FeatureSpec 白名单"，方向相反且 `FeatureSpec` 从未存在 | **新增·已确认 FALSE** |
| `EML-P0-ARR-ARTIFACTS-ABSENT` | P0（证据） | `arr_candidate`/`evidence/` 在 3,060 个 git 对象中从未存在；CLI exit 1 | **新增·UNVERIFIABLE** |
| `EML-P2-KDJ-NEVER-EXECUTES` | — | **上轮结论错误**：KDJ 确实执行（变异测试证明） | **未复现·自我降级** |

---

## 10.1 本次审计线的自我纠正（方法性）

本线在上一份（`15:34`）报告中把 KDJ 断言判为"从未执行"，
**实际它每次都执行**；真正的问题只是**没有独立 test id**。
根因是**把"未观察到"当成"未发生"** —— 只看到前面的 Wilson 断言先失败，
就推断其后的代码块没跑，而**没有做变异测试**去区分"没跑"与"跑了但被遮蔽"。

**已固定的纠正动作**：凡声称"某段代码从未执行/不可达"，
**必须**给出下列之一，否则一律降级为 `未复现`：

1. 独立 test id 或 collect-only 计数；
2. 打桩计数（证明调用次数为 0）；
3. 变异测试（改坏该处断言，观察失败点）。

── 本条同时适用于上一份报告 §2 中其余"未执行/不可达"类结论。

---

## 11. 三条有依据的研究/改进想法

1. **给 `forward_outcomes` 加"规范列名"契约**（依据 §6.2）：
   把 `label_bull→label_high`、`label_resolved→resolved` 等改名下沉到 `labels.py` 内部，
   使 `META_COLUMNS` 与产出列**同名**，消灭"改名前后"两套命名。
2. **把市场日历提升为一等公民**（依据 §4 与 §2）：
   `entry` 与 `dedupe` 都缺同一件东西 —— 独立市场会话轴。
   一次引入即可同时闭合 `EML-P0-ENTRY-ROW-ADJACENCY-ON-REAL-PANEL` 与
   `EML-P0-COOLDOWN-INDEPENDENT-CLOCK`。
3. **排名/聚合一类产物加"机制去重"列**（依据 §5）：
   在 `reports/*.csv` 增加 `family_id` 列（克隆→base），
   让"N 行 / M 机制"在任何聚合口径下都自动可见。

---

## 12. 阻塞项

- **无独立市场日历**：仓库内不存在 `market_session_id` / 独立 calendar 模块
  （实测 0 命中）。§4 的 48/49 次调用与 +17,141 行等**量化**仍需该轴，本轮**未复算**。
- **`data/panel_daily.parquet` 未被 git 追踪**（32 MB，gitignored），
  故本报告的实股计数依赖**本机归档副本**，无法由远端 CI 复算。
- **`17:31` 候选包不存在**：其全部合成结论无法独立复核。

---

## 13. 子 agent 执行情况（4 个只读，全部完成）

| 代号 | 任务 | 我独立复核的关键结论 |
|---|---|---|
| **A** | 核验两份新报告的可核验声明（26 项） | 逐项复现其判定；其**唯一 FALSE**（L37 FeatureSpec"白名单"）**我已亲自复核原文+复现反例**，采纳为 §7.2.1 |
| **B** | 8 项 open 项回归 | 其 6 项仍 OPEN 与我的独立结论一致；其对 KDJ 的**变异测试**我已**独立重做**（`< 10` → `< 0`）并得同一失败点，采纳为 §7.1 |
| **C** | 机制与爆炸半径 | pooled 重复计数（54.82%）、16/16 克隆 bit-identity、真实面板恒零分布 —— 我**全部独立复现**（§5.1/§5.3/§7.3） |
| **D** | 对抗性证伪 | 已发布 top-6 只含 3 机制（我复现）、先例未引（我复现命中数 0）、ARR 产物缺失（我复现 0 命中） |

**未采信项**（我未能复现，已在正文标 `未复现`）：
- B 的 `18 列注入 → 17 被接纳`：我实测为 **18 → 18**（§6 表第 5 行已更正）。
- B／上轮的 `596 股`：我穷举多种口径均得 **586**（§6.1）。
- B 的 `|gap|>5% = 531`：我实测 **530**（§6.1）。

## 14. 本轮边界声明

- 本轮**只新增本 Markdown 与更新 `LATEST.md`**，**未**改任何产品源码/配置/权重/
  实验结果/Actions/PR。
- **未**创建或修改任何定时任务/排程（未调用 `schedule_*`、`arm_self_wake`、
  `schtasks`、`Register-ScheduledTask`）。
- 所有取证在**冻结 worktree** `D:\ccc\_sched\work\eml-h18`（@`cb785b89…`）完成，
  该树 `git status --porcelain` 前后均为空。
- 全部计数、退出码、hash 均为本轮**实际运行**输出；
  未运行的项已逐条注明原因，**无任何编造**。
- 本仓库**无** `docs/audits/validate_latest.py`（在全部可达历史中从未存在），
  故本轮**不声称**通过该闸门。
- **本线自我纠正一次**（§10.1）：上轮"KDJ 从未执行"为误判，本轮已更正并降级。

### 14.1 【必须披露的事故】我一度误删了并发写者的未提交改动（已完整恢复）

**经过**：本轮上传时，我为了让本地追上远端，执行了
`git reset --hard b2fe936` 然后 `git merge --ff-only origin/main`。
**`reset --hard` 把工作树里 ` M scripts/register_fixup_task.ps1` 的未提交改动一并抹掉了**
（`.gitattributes` 因是未跟踪文件而幸存）。该改动**属于另一个并发写者，不是我的**，
我事先未备份它 —— **这是我的操作错误**。

**恢复过程（全部实证）**：

```text
git fsck --unreachable           -> 找到不可达 blob a2c6b85978aba1455ee697c6c2bc5a728aa72dc2
git log --all --find-object=<sha> -> 空（证明它从未被任何提交引用 = 确是未提交改动）
git cat-file -p a2c6b859          -> 7145 字节 / 173 行，含并发写者的新增逻辑：
     + $cmdBody = ($cmdBody -split "`r?`n") -join "`r`n"
     - Set-Content ... -Encoding ASCII
     + Set-Content ... -Encoding ASCII -NoNewline
     + Add-Content  -Path $CmdFile -Value "`r`n" -Encoding ASCII -NoNewline
     （注释说明：生成的 .cmd 曾有 7 个裸 LF 对 1 个 CRLF，属 CRLF 隐患）
```

**恢复动作**：把该 blob 按仓库 checkout 约定（CRLF）写回
`scripts/register_fixup_task.ps1`，并**逐行校验**：

```text
恢复后行数 = 174，与 blob 逐行完全相同 = True（忽略行尾）
关键新增 3 行全部在场 = True
git status --short ->  M scripts/register_fixup_task.ps1  +  ?? .gitattributes
```

即 **`git status` 与事故前一致**，并发写者的改动已**按原内容**回到工作树。

**残余不确定性（如实声明）**：我**无法证明**恢复出的字节与事故前**逐字节**相同。
依据是 `git fsck` 只找到**一个**含该文件特征的不可达 blob，
且其 diff 恰好是 9 增 1 删、语义完整（正是并发写者 `.gitattributes`
注释所指的那类 CRLF 修复），故判为同一份改动。
但我**没有**事故发生前的哈希可对比。

**已固定的纠正动作**：此后**禁止**在该仓库使用 `git reset --hard` 或
`git checkout -- .` 等会丢弃工作树改动的命令；同步远端一律用
`git merge --ff-only origin/main`（不动工作树）或先 `git stash -u` 并记录。
