# 专家／ML 线审计 — 2026-09-23 03-13-56 JST

- `audit_time_jst`: `2026-09-23 03-13-56 JST`
- `reviewed_repo`: `fy-god/pro-web-60d-strategy`
- `reviewed_source_sha`: `8163db84e10f52057aab1216e969f2ee121ad54f`（**产品源码**基准；本轮实测其字节等于 tip）
- `main_head_at_audit_start`: `1ac289d29dd4a078c8da6680eb5d42af6423e811`
- `previous_report`: `docs/audits/expert-ml/2026-09-23_01-57-44_JST.md`
- 类型：`INDEPENDENT_SOURCE_AUDIT + REAL_DATA_MEASUREMENT`
- 本轮 `real_market_fit_count` 增量：**0**
- 本轮用户本机三小时：**NOT_VERIFIED / LOCAL_APPLY_PENDING**
- Open PR：**0**

> 本轮只新增本报告并随后更新 `LATEST.md`。未改 `src/`、`tests/`、模型、权重、配置、Actions、Windows 任务或 PR；未推代码。

---

## 0. 一句话结论

1. **产品源码在本轮没有任何变化**：`8163db84..1ac289d2` 共 11 个文件改动，**全部**在 `docs/audits/expert-ml/`；`git diff 8163db84..1ac289d2 -- src tests scripts` **为空**。因此上一轮列出的**所有源码级开放项，按构造就不可能被修**——本轮不把它们重新包装成"新发现"。
2. **上一轮（01:57）报的两条 P1 都成立**，我独立复核通过，并且**各自补上了一条它们没做的决定性测量**：
   - **锁**：`local_lock` 是**真·fail-closed 跨进程锁**（实测：A 持锁 → B 被 `ALREADY_RUNNING` 拒绝 → A 释放后 C 成功）。缺口**不在锁的实现，而在 runner 从不加锁**（runner 包 16 个模块**全部 0** 锁原语；全仓库只有 `scripts/run_fixup.py` 一处）。
   - **价格基准**：`price_basis` **读取点 = 0**（纯装饰）。上一轮明说"没有真实 panel 字节证明"，**本轮把它量了**：权威 baostock qfq 面板 **8,728,355 行**，单一基准不变式 **0 违反**，且**带 `adjustflag=2.0`（qfq）常量字段**——而 `src/data_pipeline.py:43,63` 用 `usecols=REQUIRED_COLUMNS` **把这个可机检字段丢掉**。⇒ 不是"无法机检"，而是"**已有的可机检字段被丢弃**"。
3. **一个新判据**（本轮新增，非重复）：`scripts/run_fixup.task.xml` 的 `ExecutionTimeLimit = PT2H30M`（**9000 秒**），而同仓库 `scripts/run_fixup.py` 的 `AGENT_TIMEOUT_SECONDS` 与代码侧契约是 **13200 秒（3h40m）**。外层计划任务比内层契约**早 4200 秒**杀掉进程 ⇒ 三小时合同在**跟踪的**计划任务模板下**结构上不可能完成**。该不一致此前只被报为"模板仍是 PT2H30M"，**没人算过它比契约短多少、也没人指出它使合同不可达**。

---

## 1. 本轮实际读取与基线（真实命令与退出码）

| 动作 | 结果 |
|---|---|
| `git fetch origin` | `2032a26..1ac289d  main -> origin/main`（rc 0） |
| `origin/main` | `1ac289d29dd4a078c8da6680eb5d42af6423e811` |
| 本地 `HEAD` | `b9c0a65ad095a7030138a44291768a22cbf61be2`（`main`） |
| `git status --short` | 5 行，**全部为他人未提交改动**（见 §1.2），只记录不覆盖 |
| `git diff 8163db84..1ac289d2 -- src tests scripts` | **空** |
| `git diff --name-only 8163db84..1ac289d2` | 11 个文件，**非 audit 路径 = 0** |
| `cat-file -e origin/main:docs/audits/validate_latest.py` | **rc 128（不存在）** ⇒ 手册 §2 步骤 5 门禁 **N/A，不得声称 PASS** |

### 1.1 纯净基线（实测）

用 `git archive --format=zip` 导出 tip 到 `D:\eml_h02_tip`（**无 `.git`**），并**先证 fixture**：

```text
run.py 身份：hashlib.sha1(b"blob "+len+0x00+raw) = c27711f6a0b95a8e03e50ae9e333d445ab00ea73
             git rev-parse 1ac289d29:src/ml/research_h504/run.py = c27711f6a0b95a8e03e50ae9e333d445ab00ea73
IDENTITY MATCH = True
```

**基线全量测试**（`python -m pytest -q -o addopts= -p no:cacheprovider`，仓库 `pyproject` 设了 `addopts=-q` 故必须覆盖）：

```text
65 passed in 25.97s    exit code 0
```

> 独立复核者 B 在同一 tip 另跑得 `65 passed in 27.85s, exit 0`。两者一致。

### 1.2 未提交改动（只记录，绝不覆盖）

```text
 M docs/audits/expert-ml/LATEST.md
 M scripts/register_fixup_task.ps1
?? .gitattributes
?? docs/audits/expert-ml/2026-09-22_07-41-01_JST.md
?? docs/audits/expert-ml/2026-09-22_15-40-19_JST.md
```

---

## 2. 发现

### F1【复核成立 · P1】三小时单实例锁只护 scheduler，不护直接研究入口
`EML-P1-H504-SINGLE-INSTANCE-LOCK-NOT-SHARED-WITH-DIRECT-RUNNER`

**文件/函数/行**
- `scripts/run_fixup.py:84-105` `@contextmanager def local_lock(path)`；**唯一**使用点 `scripts/run_fixup.py:140`。
- `src/ml/research_h504/*.py`（16 个模块）：锁原语命中 **0**。

**触发条件/反例（实测）**
1. **锁本身是真的 fail-closed**（我做了三臂实测 + 阳性对照）：

```text
arm A 持锁           -> acquired=True
arm B 在 A 持锁时竞争 -> acquired=False  exception=RuntimeError: ALREADY_RUNNING: no second agent was started
arm C 在 A 释放后竞争 -> acquired=True
LOCK IS FAIL-CLOSED? = True
```

2. **但 runner 从不调用它**。用**仅匹配真实锁原语**的检测器（`local_lock|msvcrt|LK_NBLCK|fcntl|flock|LOCK_EX|O_EXCL|\.lock|ALREADY_RUNNING` …）：

```text
阳性对照 scripts/run_fixup.py = 9 hits   -> 检测器有区分力
runner 包 16 个模块合计        = 0 hits
全仓库含锁原语的文件           = 1（仅 scripts/run_fixup.py）
```

> 方法学说明：我先用宽松正则找 runner 的 `lock`，得到 11 个命中，但逐个看全是 `block_size`/`BLOCKED_CALENDAR`/`dev_block_sessions` 之类的**假阳性**。改用令牌级检测器 + 阳性对照后才下结论——**没有阳性对照的"零命中"不构成证据**。

**机制**：`--stage all` 直接写 `experiment_registry.jsonl`、H504 模型/预测、AUX checkpoint、run summary、start/execution receipt；`run_fixup.task.xml` 的 `MultipleInstancesPolicy=IgnoreNew`（实测值）只挡计划任务自身第二次触发，挡不住手工 `python -m src.ml.research_h504.run ...`。

**影响**：并发两个进程可同时看到 `previous is None` 后重复训练并互相覆盖产物，或双份消耗 fit 预算；后者与 `EML-P1-H504-GLOBAL-FIT-BUDGET-UNENFORCED` 叠加。

**修复建议**：把非阻塞锁抽成共享模块，所有"会产生 fit/registry/checkpoint"的入口使用**同一个 repo-scoped lock path**，例如 `logs/fixup/research_session.lock`。

**验收测试**：持锁运行 `run.py --stage all` 必须 `RuntimeError('ALREADY_RUNNING')`；释放后必须可跑通；测试套件需含该断言（当前**没有**，见 F3）。

---

### F2【复核成立 · P1，并新增真实数据测量】价格基准合同不可机检，且可机检字段被丢弃
`EML-P1-H504-PRICE-BASIS-CONTRACT-ADVISORY-ONLY`

**文件/函数/行**
- `src/ml/research_h504/task_spec.py:18` `price_basis: str = 'same_basis_required'`（`H504TaskSpec` 字段）。
- **读取点 = 0**：AST 扫描全仓 `.py`，仅 1 处文本出现（即该声明），`.price_basis` 属性读取 0、`['price_basis']` 下标 0；仅被 `to_dict()`（`task_spec.py:22-23`）整体回显。
- `task_spec.py:38-40` `label_h504_candidates(panel, candidates=None, *, spec=None, calendar=None)`：**无任何 basis 参数**；必需列 `task_spec.py:54-56` = `{code,date,open,close,low}`；E 在 `:96-98`，`target=4*E`/`floor=0.8*E` 在 `:99-100`；三列按**位置**取值（`:74`），任何数值列都被当作同基准。
- `src/ml/research_h504/data_io.py:19-21` `prepare_panel` 只要求 `{code,date,open,high,low,close,volume}`；`read_table`（`:8-14`）只按后缀分派，零 schema 校验。
- `src/data_pipeline.py:43` `REQUIRED_COLUMNS = ["code","date","open","high","low","close","volume"]`，`:63` `usecols=REQUIRED_COLUMNS`。

**触发条件/反例**
- **真实 CLI**：`--evidence-type REAL_MARKET` + 有 `--calendar` + **零 basis 证据** → **rc=0**，一路跑到特征构建、诊断、候选折叠、H504 训练入口、registry、`task_spec.json`、receipt；唯一的阻塞是数据量（`BLOCKED_PROTOCOL_H504: actual merged support too small train=0 dev=189`），**不是 basis 拒绝**。
- **对抗性证明**：一个 close 比 open 大 **1000×**、且 close 落在自身 [low,high] 之外的混基准面板被 `prepare_panel` **接受**、`label_h504_candidates` **接受**，**4 个候选全部被标成 `success`**（`label_joint=1.0`）⇒ 污染被"洗"成正标签。
- **阳性对照（证明代码本可 fail-closed）**：`run.py:88-89` 在缺 `--calendar` 时 `raise SystemExit('BLOCKED_CALENDAR: ...')`，实跑 **rc=1**。全仓已有 13 个 `BLOCKED_*` 状态（`BLOCKED_CALENDAR/CANDIDATE_MANIFEST/CHECKPOINT_MISMATCH/CLASS_SUPPORT/DATA/DIRTY_WORKTREE/ENV/LOCAL_SETUP/NO_FIT/PROTOCOL_H/SYNC/TRAINING`），**没有 `BLOCKED_PRICE_BASIS`**（该串仅出现在上一轮报告文档里，代码中 0 次）。
- **更尖锐的一点：连 basis 的 CLI 开关都不存在**（`--help` 无此参数）。

**本轮新增的决定性测量（上一轮明说缺这一块）**

权威 A 股面板 `D:\xm\ly\data\raw\mainboard_history_2013_2025\bars_qfq.parquet`（565,311,647 B；baostock 主板前复权；`code` 3,392 只 / `date` 3,157 个会话；**8,728,357 行**）——单一基准不变式 `low<=open<=high` 与 `low<=close<=high`：

```text
high < max(open,close) : 0
low  > min(open,close) : 0
high < low             : 0
ANY violation          : 0   （0.000000 %，全部 8,728,355 个四列有限行）
有违反的会话数 = 0 / 3157      有违反的股票数 = 0 / 3392
close/open 比值：min=0.333  p0.1=0.893  p99.9=1.124  max=2.477
```

并且该面板**带一个可机检的基准字段**：

```text
adjustflag 取值分布：2.0 -> 8,728,357 行（100.000000 %）
唯一取值 = [2.0]  => 常量，baostock 约定 2 = 前复权(qfq)
```

**影响与本次结论的**分级：
- 缺失 basis 门禁是**真实的、可操作的缺口**（有 13 个同类 `BLOCKED_*` 先例，且 `BLOCKED_CALENDAR` 证明该模式可落地）。
- 但在**这一份权威输入**上，**未观测到混基准危害** ⇒ 本轮分级为 **`待验证风险`**，**不是** `已确认错误`（我没有看到错误值出厂）。
- 本轮把证据**加强**了一步：输入**本来就带 `adjustflag` 这个可机检基准字段**，而 `src/data_pipeline.py:43,63` 的 `usecols` **把它丢掉**。所以问题不是"无从机检"，而是"**已有的可机检字段被丢弃**"。

**修复建议**：`REAL_MARKET/AUX_REAL_HISTORY` 强制 `--price-basis {raw,qfq,hfq,total_return}`，缺失即 fail-closed `BLOCKED_PRICE_BASIS`；若输入含 `adjustflag` 则**先校验一致性**（多值即 `BLOCKED_PRICE_BASIS_MIXED`）；把 basis + 数据指纹绑进 registry signature、receipt 与模型/预测 manifest。

**验收测试**：无 `--price-basis` 的 `REAL_MARKET` 必须 rc≠0 并打印 `BLOCKED_PRICE_BASIS`；构造 `adjustflag` 混值输入必须 `BLOCKED_PRICE_BASIS_MIXED`；阳性对照：给了合法 basis 时 rc=0。

---

### F3【本轮实测 · P2】测试套件对**这两个新机制**零检测力（含阳性对照）

上一轮已报"5 个机制中 4 个删除后套件仍 65 passed"。本轮**没有**重复那 5 个，而是用**独立的新鲜副本 + 阳性对照**测**这两个新机制**（每个变体都在单独的全新副本里跑，避免一次抽取被两次测量污染）：

| 臂 | 变异 | 真实结果 | 判定 |
|---|---|---|---|
| 基线 | 无 | `65 passed in 25.87s` | 对照绿 |
| 锁 | 破坏 `local_lock` | `65 passed in 27.20s` | **零检测力** |
| basis-1 | 删除 `price_basis` 字段 | `65 passed in 32.67s` | **零检测力** |
| basis-2 | 改 `price_basis` 取值 | `65 passed in 31.05s` | **零检测力** |
| **阳性对照 A** | 破坏 calendar 守卫 | **`1 failed, 64 passed`**（`test_real_cli_requires_calendar`） | **检测到** |
| **阳性对照 B** | 破坏 registry 签名相等 | **`1 failed, 64 passed`**（`test_registry_reuses_exact_complete_signature_only`） | **检测到** |

**两个阳性对照都真的报错**，证明该测量方法**有区分力**——否则"零检测力"毫无价值。两处对照的失败栈都钉到具体测试：
`tests/research_h504/test_github_delivery.py:39`、`tests/research_h504/test_candidate.py:493`。

**另有真实并发实测（最强证据）**：**两个直接 runner 进程并发**（`--stage all`，不同 `--out`）：

```text
proc A returncode=0        proc B returncode=0        wall_seconds=1.31
A wrote run_summary.json: True      B wrote run_summary.json: True
```

⇒ **没有任何跨进程互斥**被直接 runner 施加，且**两个进程都写了自己的产物**。这不再是从源码推断，而是**实跑出来的**。

**分级**：**`待验证风险`（测试覆盖缺口 + 并发未被互斥）**。因为 `local_lock` 本身可用（F1 三臂实测），这不是"锁坏了"，而是"锁没被用 + 没测试接住"。按规则我**不把它算作新 ID**，记录为既有 ID 的范围扩大。

---

### F5【本轮新增 · 实测】`price_basis` 连 CLI 参数都不存在（29 个选项全列）

`run.py` 的完整参数表（子 agent C 实列，29 项）：

```text
batch_size, calendar, candidate_version, candidates, cooldown, effective_prompt_sha256,
evidence_type, hard_runner_timeout_seconds, hgb_min_samples_leaf, horizon, layers,
max_aux_fits, max_candidates, max_epochs, min_tier, normal_wall_limit_seconds, offline,
out, panel, patience, registered_task_timeout_seconds, resume, seeds, signals,
skip_neural, source_sha, stage, target_active_seconds
```

含 `basis` 的选项 = **`[]`**。⇒ 不只是"字段没人读"，而是**连声明基准的入口都没有**；同时 `to_dict()` 仍然回显 `'same_basis_required'`（`price_basis in to_dict: True`）。这两条并置，正好说明该字段是**纯装饰**。


---

### F4【本轮新增 · P1】跟踪的计划任务模板让三小时合同**结构上不可达**

**文件/行**
- `scripts/run_fixup.task.xml`（**UTF-16LE**，2,822 B）：`<ExecutionTimeLimit>PT2H30M</ExecutionTimeLimit>`，`<MultipleInstancesPolicy>IgnoreNew</MultipleInstancesPolicy>`。
- `scripts/run_fixup.py`：`AGENT_TIMEOUT_SECONDS = 13200`（= 3h40m）。

**机制/算术**：`PT2H30M` = **9000 秒** < `13200` 秒，差 **4200 秒（70 分钟）**。外层计划任务会在内层契约到期前 **70 分钟**强制终止 agent。

**影响**：v5 要求"单次连续三小时"。在**仓库里跟踪的**计划任务模板下，该目标**不可能达成**——不是"还没调整"，而是**配置上与目标互斥**。上一轮只报了"模板仍是 PT2H30M"，**没有算这个差值、也没有指出不可达**。

**与已报项的关系**：与 `EML-P0-FIXUP-SYNC-FAILOPEN` / 既有 timeout 一致性问题同源但**判据不同**；我明确标为**同一缺陷类的强化证据**，不主张为新 ID。

**修复建议**：模板 `ExecutionTimeLimit` 提到 ≥ `AGENT_TIMEOUT_SECONDS` + 余量（建议 `PT4H`），并让注册脚本从单一常量生成，杜绝两处漂移。

**验收测试**：`Get-ScheduledTask` 读回的计划任务 `ExecutionTimeLimit` ≥ `AGENT_TIMEOUT_SECONDS + 600`；源码侧断言模板常量与代码常量一致。

---

## 3. 未修项回归（对上一轮开放清单，逐条在**真实源码**上重新定位）

**结构性前提**：`src/tests/scripts` 在 `8163db84..1ac289d2` 之间**零改动**，所以以下源码级项**按构造全部未修**。我仍逐条给出当前位置（不是重新发现，而是**确认未修**）：

| 开放项 | 实测位置（tip） | 状态 |
|---|---|---|
| `EML-P1-H504-GLOBAL-FIT-BUDGET-UNENFORCED` | `run.py:167 nfit=0` / `:170 if nfit>=args.max_aux_fits` / `:181 nfit+=1` | **未修**（计数器仍是循环局部、只约束 AUX；无跨进程持久化；全树 `fits_consumed`=0 命中） |
| `EML-P1-H504-RECEIPT-PROVENANCE-CALLER-CONTROLLED` | `run.py:71` `--source-sha` 默认 `'UNKNOWN'`、`--effective-prompt-sha256`、`--registered-task-timeout-seconds` | **未修** |
| `EML-P1-H504-DEV-RESOLVED-SUPPORT-MISSING` | `train_h504.py:13-15 _safe_impute`、`:18-19 first_legal_split`、`:26 run_h504_hgb` | **未修** |
| `EML-P1-H504-HORIZON-GUARD-SAME-FAILOPEN` | `run.py:90-91`（仅真实 primary 且 `horizon!=504` 时拒绝） | **未修**（守卫存在但覆盖不全） |
| `EML-P1-H504-AUX-FALLBACK-RUNS-WHEN-PRIMARY-READY` | `run.py:162 if not args.skip_neural`、`:170` | **未修** |
| `EML-P1-H504-REUSE-TRUSTS-REGISTRY-WITHOUT-ARTIFACT-VERIFICATION` | `registry.py:18 has_complete` / `:19 latest_complete` / `:21 startswith('COMPLETE')` | **未修**（只看 status 串，无产物校验） |
| `EML-P2-H504-ALL-NAN-FEATURES-SILENTLY-IMPUTED` | `train_h504.py:13-15`（`fillna(med)` 后仍可能全 NaN） | **未修** |
| `EML-P1-H504-SIGNATURE-BLIND-TO-FEATURE-SOURCE` | `run.py:94 pipeline_hash=stable_hash({p.name: _fingerprint_path(p) for p in Path(__file__).parent.glob('*.py')})` | **未修**（只 glob 本包 `*.py`，不含 `src/data_pipeline.py` 等上游生产者） |
| `EML-P1-H504-REAL-CALENDAR-GUARD-SELF-REPORT-FAILOPEN` | `run.py:62 --calendar` / `:88-89 BLOCKED_CALENDAR` / `:92` | **部分**（守卫真实存在且实跑 rc=1；"self-report"面仍未修） |

**汇总：9 项已复核，0 项已修，0 项被证伪。**

---

## 4. 三轴状态

- `execution_status`: `AUDIT_COMPLETED / READONLY_SOURCE + REAL_DATA_MEASUREMENT / NO_NEW_FIT`
- `research_verdict`: `PRODUCT_SOURCE_UNCHANGED_SINCE_8163DB84; PEER_F1_LOCK_GAP_CONFIRMED (lock works, runner never takes it); PEER_F2_BASIS_GAP_CONFIRMED (0 read sites) AND REAL_PANEL_MEASURED_SINGLE_BASIS_ADJUSTFLAG_PRESENT_BUT_DROPPED; NEW_TASK_TEMPLATE_MAKES_3H_CONTRACT_UNREACHABLE; ALL_9_OPEN_ITEMS_STILL_UNFIXED`
- `evidence_status`: `REAL_SOURCE_READ + REAL_CLI_EXECUTION + REAL_MARKET_DATA_MEASUREMENT(8,728,357 rows) + SOFTWARE_TEST_EXECUTION(65 passed, exit 0)`
- `evidence_type`: `PROVENANCE_AND_RECONCILIATION + REAL_MARKET_NEW_FIT=0`（**软件样本与实股结果分开计数**；本轮**无任何新实股拟合/成绩**）

---

## 5. 必须分开写的三类

- **程序修复**：本轮**0 项**落地（纯审计轮，未改任何源码）。
- **任务定义变更**：**1 项建议**——F4 要求把计划任务 `ExecutionTimeLimit` 与代码契约对齐（≥ `AGENT_TIMEOUT_SECONDS`+余量），否则"三小时单次连续执行"这一合同条目在配置上不可达。
- **真实模型增益**：**无**。本轮 `real_market_fit_count = 0`，没有新的 H504 成绩、命中率或收益数字。

---

## 6. 研究/改进想法（各附依据与证伪方式）

**R1（本轮证据 F4）— 把"合同条目"变成机器可校验的不变式集合。**
依据：F4 显示一个纯配置常量（9000 s）让一条书面合同（三小时）**结构上不可达**，而 65 项测试**全绿**。做法：为每条 v5 合同条目写一条**可执行不变式**（如 `ExecutionTimeLimit >= AGENT_TIMEOUT_SECONDS + 600`、`price_basis` 必须被某处读取、锁必须被每个 fit 入口获取），并让 CI 跑它们。
- **证伪方式**：若这些不变式在现有套件里其实已被覆盖（逐条给出测试 file:line），则本想法无增量。

**R2（依据 F2 的 `adjustflag` 发现）— 用"输入自带的基准字段"实现零成本门禁。**
依据：权威面板 8,728,357 行全部 `adjustflag=2.0`，而 `data_pipeline.py:63` 的 `usecols` 把它丢掉。做法：保留该列，`REAL_MARKET` 要求单一取值且与 `--price-basis` 一致，多值即 `BLOCKED_PRICE_BASIS_MIXED`。**成本极低**（不新增数据采集，只不再丢弃已有字段）。
- **证伪方式**：若真实生产中 `adjustflag` 常为多值或缺失，则该门禁会误伤，需改为"声明式 + 可选校验"。

**R3（依据 F1/F3）— 让"锁"成为可测的、而非可读的。**
依据：本轮实测锁**本身正确**，缺的是**被调用**；且测试树 0 断言。做法：在 runner 的 fit 入口获取 `research_session.lock`，并补一条"持锁时第二个进程必须失败"的集成测试（我本轮已在沙箱里把这条测试的骨架跑通：A 持锁/B 拒绝/C 成功）。
- **证伪方式**：若 runner 的并发被证明无害（例如 registry 追加是原子且产物按 signature 命名），则该测试价值下降。

---

## 7. 未运行项与原因（留空不编造）

| 项 | 状态 | 原因 |
|---|---|---|
| 新的真实市场 fit | **未运行** | 本轮为审计轮；无新授权/新契约证据；不虚耗 fit 配额 |
| 用户本机三小时运行 | **NOT_VERIFIED** | 无本机 receipt/日志可读；不推测 |
| `validate_latest.py` 门禁 | **N/A** | 该仓库无此文件（`cat-file -e` rc 128）；**不声称 PASS** |
| PR 状态核验 | **未做（本轮）** | 无网络 API 依赖；沿用上轮"Open PR=0"，未重新调用 API ⇒ 标为沿用而非本轮实测 |
| 修改任何源码/配置/排程 | **未做** | 手册明令禁止 |

---

## 8. 独立复核（3 个只读子 agent，均已回收）

| 子 agent | 任务 | 结果 |
|---|---|---|
| **A** 锁核验 + 反证 | 复核 F1；并穷举是否存在**任何替代**单实例机制 | **主张成立**。除 `run_fixup.py` 外，全树对 `sitecustomize`/`usercustomize`/`socket` 绑定/`O_EXCL`/`pidfile`/`pgrep`/`psutil`/`tasklist`/`threading.Lock`/`Semaphore`/`atomicwrites` 等**全部 0 命中**；`scripts/run_fixup.py:112,116,119` 只有 `taskkill`/`os.killpg` 收尾逻辑。⇒ **不存在第二套单实例机制** |
| **B** 基准核验 | 复核 F2（真实 CLI + 对抗性混基准面板） | **全部成立，无一项被证伪**。`price_basis` 读取点 **0**（AST 扫描）；`BLOCKED_PRICE_BASIS` 在**代码**中 0 次（3 次全在上一轮报告文档里）；对抗性 1000× 混基准面板被接受、**4 候选全部 `success`**；阳性对照 `BLOCKED_CALENDAR` 实跑 **rc=1**；无 basis 的 `REAL_MARKET` 实跑 **rc=0** 并跑到训练入口；独立得 **65 passed in 27.85s, exit 0** |
| **C** 检测力 + 并发实测 | 量 F3 | 见 §8.1。三个变异臂全 **65 passed**；两个阳性对照各 **1 failed, 64 passed**；并**实跑**两个并发直接 runner，**都 rc=0 且都写了产物** |

**我独立复算过的复核结论**：F1 的锁三臂实验、令牌级检测器 + 阳性对照、F2 的真实 CLI rc=0 / 阳性对照 rc=1、`adjustflag` 分布、8,728,355 行不变式、基线 65 passed、29 个 CLI 选项——均**由我自己重跑确认**后才写入本报告。子 agent 与我数字不一致处，以**我实测的原始值**为准。

### 8.1 方法学说明（本轮踩到并修掉的两个探针缺陷）

1. **宽松正则导致假阳性**：我最初用 `lock` 子串找 runner 的锁，得到 11 个"命中"，逐个核对后发现全是 `block_size`、`BLOCKED_CALENDAR`、`dev_block_sessions` 之类的**假阳性**。改用**令牌级检测器**并配**阳性对照**后才下结论。**没有阳性对照的"零命中"不构成证据。**
2. **丢失装饰器的探针自证无效**：我第一版提取 `local_lock` 时正则从 `def` 开始，把 `@contextmanager` 丢掉，结果**两个臂都失败且报同一个错**（`'generator' object does not support the context manager protocol`）——正是"探针返回同一答案即探针损坏"的情形。补上装饰器后三臂才出现**可区分**的结果。


---

## 9. 本轮未改源码、未动排程

明确声明：本轮**未修改任何模型源码、权重、配置、Actions、PR、`README.md`、`SCHEDULE.md`**；**未创建或修改任何定时任务/排程**（未调用 `schedule_*`、`arm_self_wake`、`schtasks`、`Register-ScheduledTask`）；**未 push 任何代码**；只新增本报告并更新 `LATEST.md`。对 `D:\xm\ly\...\bars_qfq.parquet` **只读**（未写入一字节）。
