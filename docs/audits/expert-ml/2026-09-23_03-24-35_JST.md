# 专家／ML 线审计补遗 — 2026-09-23 03-24-35 JST

- `audit_time_jst`: `2026-09-23 03-24-35 JST`
- `reviewed_repo`: `fy-god/pro-web-60d-strategy`
- `main_head_at_audit_start`: `1ac289d29dd4a078c8da6680eb5d42af6423e811`（= `origin/main`，**未移动**）
- `previous_report`: `docs/audits/expert-ml/2026-09-23_03-13-56_JST.md`（同轮正报告）
- 类型：`ADDENDUM / CORRECTION_OF_MY_OWN_PROBE + MACHINE_STATE_MEASUREMENT`
- 本轮 `real_market_fit_count` 增量：**0**

> 本补遗只做三件事：**(1)** 更正我自己正报告里一处**探针缺陷**；**(2)** 补上三个子 agent 交回、但我**尚未写入**正报告的决定性事实；**(3)** 更正一处**对 v5 文档的字面引用**。不新增源码级缺陷主张（源码本轮零改动）。

---

## 0. 三句话结论

1. **我正报告里"fixture 证明"的布尔值是错的，但结论不受影响**：`pyproject.toml` 在**本 tip 根本不存在**（2232 个跟踪文件里没有），我却把它列进探针文件集，于是探针报 `ABSENT` 并把 `allok` 置 False。**7 个真实存在的文件在 LF 归一化后全部逐位匹配**（含 `run_fixup.py 26a61796…`、`run.py c27711f6…`、`task_spec.py 74359c22…`）。⇒ **探针缺陷，不是 fixture 缺陷**。
2. **正报告的 F4 只写了两方不一致，实际是五方**，而且**部署状态比"超时太短"更糟**：三个不同来源写着 90 分钟／PT2H30M／PT3H50M，而本机**根本没有任何 `ProWeb60d-Fixup` 任务**（213 个任务里只有 `ProWeb60d-ReportAudit`）。
3. **v5 文档"明确要求"的那句原话，在 v5 里不存在**：`单实例锁跨 runner/手动启动生效` 只在**上一轮报告自己**里出现（`2026-09-23_01-57-44_JST.md:23,85`）。v5 只写了 `单实例锁`（`:56`）与 `锁`（`:66`/`:102`）。**工程结论完全成立**，但**引用出处要改**。

---

## 1. 更正：我自己的探针缺陷（fixture 证明）

**现象**：我运行 fixture 证明时得 `FIXTURE IS THE TIP = False`。

**根因（实测）**：本 tip 的 **2232** 个跟踪文件里：`pyproject.toml` / `pytest.ini` / `setup.cfg` / `tox.ini` / `.gitattributes` / `conftest.py` / `tests/conftest.py` **全部不存在**（`requirements.txt` 存在）。我跟文件的顶层条目只有 `.gitignore LICENSE README.md REPRODUCIBILITY.md RESEARCH_QUICKSTART.md RESULTS.md TARGET_70PCT.md audit data docs experts outputs reports requirements.txt scripts src tests`。**我先前报告里提到"仓库 `pyproject` 设了 `addopts=-q`"因此也是错的**——本仓库没有 `pyproject.toml` 也没有 `pytest.ini`/`setup.cfg`/`tox.ini`/`conftest.py`。`-o addopts= -p no:cacheprovider` 仍然每次通过，但它**不是**为了覆盖某个已存在的配置——**没有那个配置**。子 agent C 独立指出过这一点（"no pyproject.toml/pytest.ini/setup.cfg/tox.ini exists in this tip; `-o addopts=` still passed every run"），我复核后确认它对。


**修复后的证明**（LF 归一化，`core.autocrlf=true` 故**必须**归一化，否则原始字节比对必然失败）：

| 文件 | 盘上 CRLF 对数 | LF 归一化 blob | git 目标 | 匹配 |
|---|---|---|---|---|
| `scripts/run_fixup.py` | 166 | `26a617966277028e74b1eed56a5e5e04ba21f397` | 同 | **True** |
| `src/ml/research_h504/run.py` | 197 | `c27711f6a0b95a8e03e50ae9e333d445ab00ea73` | 同 | **True** |
| `src/ml/research_h504/task_spec.py` | 123 | `74359c222b3e8b6ad2b14fcf8627de4e3ee46440` | 同 | **True** |
| `src/ml/research_h504/registry.py` | 25 | `82b1987b346fcf94f92bdd5efd235c0dddfa0991` | 同 | **True** |
| `tests/research_h504/test_github_delivery.py` | 110 | `1c430b20a3ac15cda71f81f6e31d87c3bba43b6c` | 同 | **True** |
| `tests/research_h504/test_candidate.py` | 515 | `5952758ad1a2f1e24435512e7ba554593b1940a5` | 同 | **True** |
| `README.md` | 572 | `c1da2e3108bd5a9fd01430338621ff32d90d41d3` | 同 | **True** |

⇒ **fixture 有效**；正报告 §1.1 的基线 `65 passed in 25.97s, exit 0` 与所有基于该提取的测量**全部有效**。

**另有两处我自己的探针错误，一并更正**（都在同一次尝试里，已重做）：
- 我用 `b'\\r\\n'` 当 CRLF 探针——那是**四个字面字符**（反斜杠 r 反斜杠 n），不是 CR LF。所以那次报告"CRLF present = False"是**探针错**，实际 `run_fixup.py` 盘上有 **166** 对 CRLF。
- 我"还原 pristine"时走了 `read_text()`/`write_text()`，**通用换行翻译会静默改写行尾**，所以那句"restored pristine: identity match"**不可信**。**重做时不再做还原声明**——直接丢弃提取目录，用全新提取做测量。

---

## 2. F4 强化【本轮新增 · P1】三小时合同在本机**不具备运行条件**，且超时值有**五方**不一致

### 2.1 五个互相矛盾的数值（全部实测）

| 来源 | 值 | 换算 | 出处 |
|---|---|---|---|
| `register_fixup_task.ps1` 注释 | "up to 90 minutes (run_fixup.py allows 5400s)" | **5400 s** | `scripts/register_fixup_task.ps1:94` |
| `register_fixup_task.ps1` 变量 | `$timeLimit = "PT2H30M"` | **9000 s** | `:96`（并写入 `:115`） |
| `run_fixup.task.xml`（UTF-16LE，**已跟踪**） | `<ExecutionTimeLimit>PT2H30M</ExecutionTimeLimit>` | **9000 s** | `scripts/run_fixup.task.xml:16` |
| 代码常量 | `AGENT_TIMEOUT_SECONDS = 13200` | **13200 s** | `scripts/run_fixup.py:23` |
| v5 合同要求（Windows 任务上限） | `13800秒／PT3H50M` | **13800 s** | `docs/audits/expert-ml/2026-09-21_01-43-18_JST.md:39` |

v5 文档自身另有两处**目标值**：`10800 秒`（`:16`「本合同以单次10800秒有效研究为目标」）与单次 wall 上限 `210 分钟`（`:43`，**我换算为 12600 秒**，文档原文是分钟）。⇒ 同一件事上共有 **5400 / 9000 / 10800 / 13200 / 13800** 五个**字面**数值（另加一处 210 分钟需换算），**没有任何两处被强制一致**。

**最关键的一处**：`ps1:94` 的注释声称 `run_fixup.py allows 5400s`，而**同一仓库**的 `run_fixup.py:23` 是 **13200**。注释与代码相差 **7800 秒**（2h10m）。⇒ 模板作者与代码作者对"agent 能跑多久"的认知**相差 2 小时以上**，且**没有任何测试**接住（见 §3）。

### 2.2 更硬的一步：本机**根本没有**这个任务

我按 v5 文档自己的要求（`:30`"仓库XML只是模板，不证明本机当前注册值，**必须实际查询**"）实际查询了本机：

```text
Get-ScheduledTask                       => 213 个任务
Get-ScheduledTask -TaskName 'ProWeb60d-Fixup'       => ''      （found = False）
Get-ScheduledTask -TaskName 'ProWeb60d-ReportAudit' => 存在    （found = True）
```

⇒ 本机注册的任务里**只有 `ProWeb60d-ReportAudit`，没有 `ProWeb60d-Fixup`**。

**影响（这是本轮相对正报告的实质推进）**：上一轮说"`IgnoreNew` 只能挡另一个计划任务实例"——**但连那个实例都不存在**。所以 v5 期望的"三小时执行器"在本机**从未被注册**：既没有外层 3h50m 上限，也没有那条 `IgnoreNew`。v5 文档 `:41` 规定"未解决时标 `BLOCKED_RUNTIME_CONFIG`，**不能宣称三小时已具备运行条件**"。按该规则，本轮实测结果**正好落在该状态**：
- 外层任务**不存在** ⇒ 无外层期限（谈不上"太短"或"够长"）；
- 模板值 9000 s **比 v5 要求的 13800 s 短 4800 s**，且**比代码 13200 s 短 4200 s**；
- 注释值 5400 s **比代码短 7800 s**。

**分级：`待验证风险` → 就"配置可运行性"这一面而言应记为 `BLOCKED_RUNTIME_CONFIG`（v5 自己的状态名）**；这不是模型缺陷，是**运行前提未满足**。

### 2.3 修复建议（比正报告更具体）

1. **单一真源**：把超时定义收敛到一个常量文件，`ps1`、`task.xml` 模板、代码**全部从它生成**；删除 `ps1:94` 那句与代码矛盾的注释。
2. **对齐到 v5**：模板与代码都提到 v5 规定的 **13800 s**（外层），内层 13200 s 保持"比外层短、留余量"的关系。
3. **注册并核验**：注册 `ProWeb60d-Fixup` 后，`run_fixup.py` 的 `--dry-run` 已经把 `registered_task_timeout` 硬编码成 `'NOT_CHECKED_BY_THIS_SCRIPT'`（`run_fixup.py:133`）——应当**真的查**（`schtasks /query /xml`）并写入 receipt，否则 v5 的"必须实际查询"永远无法机检。

**验收测试**：`(a)` 一条断言"模板常量 == 代码常量 == v5 要求值"；`(b)` 一条断言 `ExecutionTimeLimit >= AGENT_TIMEOUT_SECONDS + 600`；`(c)` 一条集成测试真的查询本机任务并断言存在且值正确。当前**三条全无**。

---

## 3. 检测力：锁的函数体在测试下是**死代码**（正报告的 F3 说得太弱了）

正报告 F3 只写了"没有任何测试断言直接 runner 会拿锁"。子 agent C 给出**更强的机制**，我独立重做后确认：

**我的独立哨兵实验**（全新提取，fixture 已按 §1 证明，基线先绿）：

```text
基线（未打补丁）      : 65 passed in 25.81s, exit 0
在 local_lock 函数体首行插入哨兵后 : 65 passed in 26.34s, exit 0
哨兵文件存在 = False        <== 测试套件从头到尾没有进入 local_lock
补丁确实生效 = True（且文件通过 ast.parse）
```

**调用图证据**（比哨兵更根本，不依赖一次观测）：

```text
整棵测试树里 run_fixup.main(…) 的调用点总数 = 1
   tests/research_h504/test_github_delivery.py:83| assert m.main(['--dry-run'])==0
全部都是 --dry-run? True
```

`run_fixup.py:134-135` 在 `--dry-run` 时直接 `return`，**早于 `:140` 的取锁**。另外两个用 `load_runner()` 的测试（`:88`、`:99`）只调用 `sync_before_agent`，**根本不进 `main()`**。

⇒ 正确表述是：**`scripts/run_fixup.py:85-105` 在测试下是不可达的**（不是"production 里的死代码"——production 的 `:140` 确实会取锁）。
⇒ 所以"把锁改成 no-op 仍 65 passed"**不只是"缺测试"**，而是**套件根本执行不到那段代码**。

**一并确认的第三个机制**：子 agent C 用**常驻哨兵 + 438/438 存活采样**实测两个并发直接 runner：`rcA=0, rcB=0`，**两者全程同时存活**，wall 22.08s vs 单跑 20.24s（**不是 2 倍**）⇒ **完全重叠，无互斥**。我自己的并发实跑（`A returncode=0 / B returncode=0 / 都写了产物`）与之一致。

**分级：`待验证风险`（测试覆盖缺口）**，不新增 ID。

---

## 4. 更正：对 v5 文档的一处**字面引用**

上一轮报告两处写「v5 明确要求"单实例锁跨 runner/手动启动生效"」。我实测：

```text
git grep -F '单实例锁跨'  <tip>  --  .
1ac289d…:docs/audits/expert-ml/2026-09-23_01-57-44_JST.md:23   <== 只在上一轮报告自身
1ac289d…:docs/audits/expert-ml/2026-09-23_01-57-44_JST.md:85
```

**该字符串在 v5 文档中不存在。** v5 实际只写：

- `:56` 「…实际研究应使用核验后的隔离源码、失败即停止的前置检查和**单实例锁**。…」
- `:66` 「…真实期限／prompt／预算、**锁**」
- `:102` 「同一session：核预算和**锁**→…」

**判定**：这是**引用出处不准**（over-attribution），**不是**假发现——v5 `:56` 确实要求单实例锁，且 §2.1/§3 证明的工程缺陷**完全成立**（锁存在、可真 fail-closed、但直接 runner 不取它、且测试下不可达）。因此：
- **不改结论、不降级**（按 `bbbb2`：同伴文档里的既有表述问题不是我的缺陷，我只做事实校正）；
- **但引用应改为** `2026-09-21_01-43-18_JST.md:56/:66/:102`，不要引用那个不存在的字面串。

---

## 5. 三个只读子 agent 的最终判定（全部回收）

| 子 agent | 判定 | 与正报告关系 |
|---|---|---|
| **A** 锁核验 + 反证 | **CONFIRMED TRUE**，无一子项被证伪。除 `run_fixup.py` 外全树 0 套替代单实例机制（`O_EXCL`/`O_CREAT`/`os.open`/`socket.bind`/`win32file`/`pidfile`/`psutil`/`threading.Lock`/`Semaphore`/`sitecustomize`/`*.pth` 全 0）；AST 全 180 个 `.py` 中具备加锁能力的 import **恰好 2 个，都在 `run_fixup.py`**（`:92 msvcrt`、`:95 fcntl`）。**唯一子项 6 为 PARTLY TRUE**（即 §4 的引用问题）。另：`ProWeb60d-Fixup` 本机未注册 | 强化 §2.2 |
| **B** 基准核验 | **全部成立**，无一项被证伪。`price_basis` 读取点 0；`BLOCKED_PRICE_BASIS` 在代码中 0 次；对抗性 1000× 混基准面板被接受且 4 候选全 `success`；阳性对照 rc=1；无 basis 的 `REAL_MARKET` rc=0；独立 `65 passed in 27.85s, exit 0` | 与正报告 F2 一致 |
| **C** 检测力 + 并发 | **两个机制均 NO_TEST_COVERAGE**；基线绿；**锁函数体在测试下不可达**（哨兵从未写入）；并发 438/438 双活；并指出本 tip **无 `pyproject.toml`** → 促成我的 §1 更正 | **强化并更正值报告 F3** |

**我独立复算后才采用**：哨兵实验（§3）、调用图（§3）、fixture 归一化证明（§1）、本机任务查询（§2.2）、五方超时值（§2.1）、v5 引用核对（§4）——均**我自己重跑**。子 agent 与我数字不一致处，以我实测原始值为准。

**环境事实如实记录**：`D:\eml_h02_tip` 并非被"外部"删除——那是**我本轮收尾清理**所致；A 与 C 都在报告中把它标为"由会话外因素删除"。根因是我在子 agent 仍在运行时就删了提取目录。**这是我的流程错误**，已以此补遗更正，并改为**先确认子 agent 全部回收、再清理**。

---

## 6. 三轴状态（本补遗）

- `execution_status`: `AUDIT_ADDENDUM_COMPLETED / READONLY + MACHINE_STATE_QUERY / NO_NEW_FIT`
- `research_verdict`: `MY_PROBE_BUG_CORRECTED(fixture valid; no pyproject.toml at tip); F4_UPGRADED(timeout disagreement is FIVE-way 5400/9000/10800/12600/13200/13800 AND ProWeb60d-Fixup is NOT REGISTERED on this host => BLOCKED_RUNTIME_CONFIG); F3_UPGRADED(local_lock is UNREACHABLE from the test suite, not merely unasserted); V5_QUOTATION_CORRECTED`
- `evidence_status`: `REAL_SOURCE_READ + MACHINE_STATE_QUERY(213 tasks) + SOFTWARE_TEST_EXECUTION(sentinel experiment, 65 passed both arms)`
- `evidence_type`: `PROVENANCE_AND_RECONCILIATION + SOFTWARE_TEST_EXECUTION`（**无任何新实股拟合**）

---

## 7. 三类分开

- **程序修复**：**0 项**（纯审计，未改源码）。
- **任务定义变更**：**1 项**——§2.3 要求把超时收敛到单一真源并对齐 v5 的 13800 s，同时把 `registered_task_timeout` 从 `'NOT_CHECKED_BY_THIS_SCRIPT'` 改成**真的查询**。
- **真实模型增益**：**无**（`real_market_fit_count = 0`）。

---

## 8. 未运行项

| 项 | 状态 | 原因 |
|---|---|---|
| 变量并发的实股 fit | 未运行 | 审计轮；且会虚耗 fit 配额 |
| 注册/修改 `ProWeb60d-Fixup` 任务 | **未做** | 手册明令**禁止创建或修改任何排程**；本轮只**读取**机器状态 |
| `validate_latest.py` 门禁 | **N/A** | 本仓库无此文件（`cat-file -e` rc 128）；**不声称 PASS** |
| 修改源码/权重/配置/PR | **未做** | 手册禁止 |

---

## 9. 声明

本补遗**未改任何源码、权重、配置、Actions、PR**；**未创建或修改任何排程**（只**查询**了本机任务列表）；**未 push 代码**；只新增本补遗并更新 `LATEST.md`。并发实跑**只写 `--out` 指向的暂存目录**，未触碰 `logs/fixup/agent.lock` 之外的仓库状态，未写任何受审仓库工作区。
