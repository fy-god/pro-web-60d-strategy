# 专家／ML：本地至少60分钟真实研究执行任务书

- publication_kind: LOCAL_EXECUTION_CONTRACT_AND_CANDIDATE_TESTS
- issued_at_jst: 2026-09-20T22:10:00+09:00
- repository: fy-god/pro-web-60d-strategy
- reviewed_source_sha: 908a41334eaf2f38570a7fb93954a63ebb80ad52
- reviewed_tree_sha: 34273d2bb4cf275036ae57770858463ed61721cc
- batch: EML-EXP-KDJ-PATH-001-REAL
- delivery_id: LOCAL-60M-01；接续RSI/PDC/MEB，不再为没有结果的同一研究改名。
- 本报告是执行合同修订、局部源码核验和候选测试，不是完整新源码审计或实股训练成绩。没有管理定时任务。

## 1. 直接改变验收，不再把两份Markdown当研究完成

用户明确要求本次本地运行至少一小时。截图仅显示两份Markdown变化和14分30秒用时，不能据此证明本地训练时长。当前固定源码的 `scripts/fixup_prompt.txt` 仍只处理P0/P1，同报告即 `NO_NEW_REPORT`；`scripts/run_fixup.py` 仍对子agent使用5400秒超时。四小时间隔不是单次可用四小时，也不是后台持续运行的证据。

本地执行者收到这份新指令后，终点改为真实数据、训练、预测和分析产物。不要再把工作交付变成一份新审计报告。云端助手仍只发布本报告和LATEST；不在远端替换控制脚本。已有本地独立授权允许时，在隔离副本中接入执行合同及研究代码；否则只交候选diff并明确未启动，不能假装本机已接入。

原参考批次的“保存计划后立即执行、表格训练、神经网络、消融和有依据的修改”只作为执行结构参考，不使用另一个仓库的SHA、V26状态、路径和实验编号。

## 2. 本次验收合同

```json
{
  "min_active_local_seconds": 3600,
  "min_real_training_seconds": 1800,
  "min_completed_real_fits": 6,
  "max_slice_seconds": 4500,
  "max_new_fits_this_delivery": 18,
  "max_parallel_heavy_jobs": 1,
  "count_llm_thinking": false,
  "count_markdown_or_git_time": false,
  "count_idle_or_retry_wait": false,
  "count_synthetic_training": false,
  "reset_old_budget": false
}
```

这里的一小时是本地真实数据处理、因子计算、训练和分析的有效经过时长，不是多核CPU时间相加，不是多个并行任务时间重复相加。至少30分钟用于真实数据上的模型优化或预训练。每个fit是模型×配置×种子×时间块的一次实际拟合，不把epoch当fit。

先读取旧预算台账。75分钟和18次是本次切片上限，同时服从已确认的剩余总预算及外层剩余时间，预留至少10分钟保存和退出。不重新把过去用量清零，不从历史文档中随意选择72/96等互相冲突的上限。余额无法核验时保存预算冲突和已见证据，不能编造0已用量。

在未达3600秒而还有预登记、依赖满足且有研究意义的任务时，继续下一项。禁止用sleep、反复下载同一文件、重复同hash拟合、扩大无用网络或取消合理早停来凑时长。安全、真实数据、合法协议、预算或入口阻塞必须停止受影响分支并报告未达标；工作队列全部有效完成但不足一小时，应记 `WORKLOAD_COMPLETE_BELOW_TIME_TARGET`，不能冒称满足用户时长。

`hour_verdict` 与 `market_result_verdict` 必须分开。一小时达标不证明选股有效；辅助预训练完成不证明H504增量。

## 3. 本次不再堆新名词：只推进一条主线

主假设：在相同KDJ、长期位置和价量背景下，去除专家阈值克隆后的连续家族分数及MEB摘要，是否增加信息；RSI恢复、PDC事件原始量作为预定义单项对照，不同时改标签、样本和网络。

当前82列元数据记录2,680,715行、3,193股、887市场日，但只是元数据，并不证明本轮沙箱拥有全部行情。本地先实际读取和核验。100张cards只能用于接口与数值等价检查，不能作为本次大量真实数据训练替代品。

MEB先按显式 `family_map` 去克隆，再用训练期经验CDF冻结各家族的分位。保留family原始分数、分位向量、覆盖率、支持数和dominance。训练期恒值/支持不足家族不转换成虚假的0.5分位支持；原始量另存。开发期不重新拟合CDF，未知不作为反对票。完整家族名单由当前源码及数值比对产生，不从旧报告手抄20个名字。

先做clone×5不变性、恒值和缺失检查，再训练。已有合格同指纹结果复用，不每轮从头扫。

## 4. 八个工作包：完成一个后继续，不在补测试后收工

### WP1：真实面板盘点、历史补齐、数据可用性

实际读取 `src/data_pipeline.py`、PWS_DATA_ROOT所指授权目录、现有panel和更早CSV/Parquet。读取现存数据时不要调用会自动写旧缓存的入口。一次扫描全部已授权行情，按股票/年份输出行数、首末日期、重复、缺市场日、零量、OHLC合法性、价格口径和来源hash。不同来源冲突单列，不用“保留最后一行”隐去差异。

需要补齐历史时只调用已经授权、经检查的只读抓取器和已确认接口，增量缓存、遵守来源限速、记录失败、重试最多两次；没有接口/权限则写 `BLOCKED_SOURCE`，不猜端点、不付费、不登录绕过限制。重点寻找2023年之前历史，而不是反复下载已有2023—2026资料。新增候选文件为 `research_h504/data_inventory.py`、`history_adapter.py`；输出 `data_inventory.json`、`history_gap.csv`、`source_manifest.json`。验收是覆盖真实面板或明确记录受限分片，不能只列目录或复述旧JSON。只回滚新run目录，不覆盖源行情。

### WP2：H504资格和可执行分支

新建/复用 `research_h504/task_spec.py`、`calendar.py`、`labels.py`、`fold_support.py`。锁定t收盘、t+1市场日Open、不延期、504市场日Close严格>4E、此前含达标日Low>=0.8E、同日风险优先。保存label_end、known_at、deadline；完整成熟队列用于BCE。合法OHLC输入必须先验证，不能用Close>High的非法bar当主合同成功用例。

回归包括Close==4E不成功、High过线但Close未过不成功、下一市场日无Open不顺延、同日风险优先、未来volume改变不改价格标签、未来结果不改候选。用标量参考函数与向量结果抽样交叉核验。输出 `task_spec.json`、`label_audit.json`、`fold_support.json`、候选/结果表。

H504成熟训练→成熟后续开发不存在时，明确阻塞H504评价，而不是随机切成熟行。以128日预热和严格先成熟再开发的最宽松一行训练/一行开发口径，最低需要128+2×504+1=1137个市场日；887不够。本地若有更早资料则按真实数据重新计算，不由旧元数据决定结论。若不足，进入WP6历史自监督分支，仍做真实计算，不把它称为H504结果。

### WP3：全历史特征与专家分量

实现/复用 `research_h504/feature_spec.py`、`rsi_factors.py`、`pullback_events.py`、`family_features.py`。显式白名单：known_at/deadline/label_end/entry/未来结果不能进入X。保留KDJ、长期位置、实际收益/幅度；只在已完成回踩上输出PDC。RSI水位、变化、新增/滚出贡献及原始价格损伤分别保存，不把其合成摘要当概率。

先在现有cards上逐策略核验raw score和clone等价，再对全部真实历史向量化/分块计算。不要逐行创建几百万个Python card对象；训练用分片须预先按代码hash固定，所有对照共用同一候选。市场日缺口用mask和间隔表示，不拼成连续bar。输出 `factor_catalog.json`、`feature_schema.json`、分片缓存和 `family_map.json`；输入源hash或公式变动则缓存失效，不改候选来绕过OOM。

### WP4：有针对性的全量诊断，不再只跑25个toy反例

在真实面板输出各因子的缺失/恒值/饱和/覆盖分布、按年和历史长度分层、相关性家族、RSI滚出主导频率、两次PDC原始损伤与ATR分母分离、家族CDF漂移。至少对32个预先固定股票分片做前缀扰动，抽取200条事件核查available_at和完成时点。

同hash诊断不重跑。CDF、分位边界和方向选择只来自训练/开发允许时点；健康诊断可以无标签开展，但不能把缺少未来结果的“频率”说成误报率。输出 `factor_health.json`、`rolloff_diagnostics.csv`、`pdc_denominator_audit.csv`、`family_health.csv`、异常原始行键。失败时定位输入/时钟/计算环节，不用全fillna(0)掩盖。

### WP5：真实H504表格训练（协议允许时立即开始）

第一合法开发折、固定seed17，六组顺序如下：T0纠错后实际快照基准；T1=T0+家族raw；T2=T0+训练期family-q；T3=T2+MEB/coverage/dominance；T4=T3+RSI恢复原始组；T5=T4+PDC事件完整原始组及PDC。所有组候选、标签、日期、样本权重和发布政策相同。去KDJ必须同时去掉KDJ定义的事件及派生，不做伪消融。保留按同长度原始历史对照检验表达增益的待运行项。

HGB起点leaves7、min_leaf300、l2=10、lr=.03、100轮，关闭默认随机验证早停；有合法内层时间块才做早停。自然采样未加权基准，不为凑时长增加轮数。每次fit保存全部开发候选预测、配置/数据/代码hash、模型、加载一致性及原始日志。零信号折保留自然分母。附件中的 `train_prepared.py` 已能跑六个家族对照接口，但默认smoke用60轮/min_leaf30，正式批次须使用本段配置；它验证输入字段，不替代独立标签认证。

### WP6：神经网络真的优化，不以forward通过收工

有合法H504折时：T0/T3的同容量MLP分别用17/29/43，共6个fit；再做T0/T3的128市场日TCN各1个fit。MLP64→32，TCN宽32、kernel3、六个双卷积残差块、dilation1/2/4/8/16/32；只左填充，感受野253。训练样本按索引懒加载。AdamW lr1e-3、weight_decay1e-3、最多30epoch、合法时间验证早停patience5、clip1。保存best/last、优化器/RNG、逐epoch损失/梯度、全预测。小样本overfit和forward只叫smoke。

没有合法H504折但有真实行情时，改走明确独立的 `AUX-HISTORICAL-REPRESENTATION-01`：训练同容量TCN预测历史下一市场日收益与随后5市场日已实现波动，目标都必须在fit_asof之前已知。用固定的历史train/validation块、窗口128、原始价量输入与增加连续因子两臂、三个种子，共6个fit。标准化只用训练块；缺失路径不伪标签。输出真实训练日志、下一步预测和checkpoint。它研究输入表达/优化与缺失处理，不证明H504选股增量，也不改H504目标。未来有合法H504资料后比较随机初始化与预训练初始化的迁移，不能用辅助指标择定正式交易政策。

此分支需本地实现 `self_supervised.py`、`sequence_dataset.py` 和训练循环；附件没有冒充它已在仓库存在。缺PyTorch则标该分支BLOCKED_ENV，继续可执行表格/因子任务，不能用合成训练补足真实一小时。

### WP7：误差分析与一次真正的配对修改

在完整开发预测中按年份/股票簇/流动性/KDJ状态/家族dominance划分错误；有H504标签才称TP/FP/FN/risk/timeout，辅助分支报告回归残差/波动误差，不混名称。对至少三个明确失败切片输出样本量、预测、候选键和机制假设，避免只挑好看案例。

只从有支持的切片选一项修改：如排除恒值家族、修正PDC分母混淆、增加mask-only负控制。固定基准和修改版各重训一次；原先基准同hash可复用，不盲目重跑。必要时扩第二合法时间块或预先固定未见股票组。不得在最终测试挑方向/阈值。输出 `error_slices.csv`、`paired_metrics.json`、`retrain_decision.json`，负面结果可以完成研究。

### WP8：台账、观测、验收与退出

新增/复用 `run_hour.py`、`experiment_registry.jsonl`、`resource_observer.py`，只管理当前已授权的本地子进程，不动排程。任务是有依赖的队列，注册源码/数据/候选/标签/特征/分割/配置hash后执行；同一报告已经读过不是退出条件。子进程使用参数数组而非shell拼接。锁不允许第二个重训练任务，终止只处理本次自己创建的进程树。

观察器每2—5秒记录PID、经过时长、CPU/I/O或训练step增量。有效窗口取并集，不乘核数；没有进展的等待不计。原始命令、stdout/stderr、模型/预测文件、行数和hash互相核验。手工写一个passed=true不是证据。附件的 `observe_command.py` 和 `research_components.py` 可用于观测与一致性验收；仍不是防篡改证明，真实数据来源和工作价值要复核。

退出写 `execution_receipt.json`：active_local_seconds、train_seconds、fits_attempted/completed、rows_scanned、unique_stocks、候选数、未知/无入场数、checkpoint/prediction hash、hour_verdict、market_result_verdict、剩余队列与精确resume命令。时长不达标、少于6个真实fit、缺模型或全预测，不得记 `COMPLETE_LOCAL_1H`。

## 5. 优先级与时间分配

这些是规划区间，不是伪造实测耗时：数据/日历5—12分钟；特征/全量诊断10—18分钟；实际训练至少30分钟；误差分析/配对修改10—20分钟。总执行切片最多75分钟；前置开发若消耗了外层余量，就如实标BOOTSTRAP或PARTIAL，下一次从检查点继续，不谎称已经完成一小时训练。

标准H504队列最多六个HGB、六个MLP、两个TCN、两个错误配对和两个第二块验证，共18次新fit上限。六个确定性HGB不机械重复同配置三种子凑数。预算不足优先T0/T3配对与真实诊断。辅助分支先六个TCN预训练，再按错误支持增加MLP基准、mask-only或第二历史块；只做预登记且有价值的任务，不无限扫参。

## 6. 本轮助手实际做了什么

实际读取了当前仓库元数据、HEAD、根/递归tree、open PR（0）、LATEST、数据入口、旧本地执行prompt、launcher和矩阵元数据。没有声称全面重审标签或训练主链。根tree未见.github目录；不推断外部触发器一律关闭。

本轮隔离沙箱能执行Python，已新写并测试：

- MEB训练期冻结ECDF、克隆等价校验、常量/缺失家族处理和重载。
- 一小时证据一致性检查：16分钟不能通过，多核CPU秒不当经过时间，重复区间不相加，sleep无进展/合成数据/Markdown不计真实研究时长，缺产物或hash不符拒绝。
- 本地单命令观察器；实际启动一个人工算术子进程验证stdout/收据/退出和锁释放。
- 准备矩阵训练接口：六个HGB人工接口fit，每个4096训练行、1024开发行，64个未知结果仍保留，均保存模型/完整预测并重载，最大差0。

最终30项候选pytest通过（1.52秒）；第一次测试有一个共享数组视图fixture错误，已改独立copy并保留原失败日志。时间验收单元测试里的3600秒是人工时间戳夹具，不是本轮真实执行一小时。六次fit全部为SYNTHETIC，真实市场fit=0，用户本机运行=未发生，local_hour_completed=false。本轮没有把快速软件测试冒称大量市场训练。

候选包随本对话附件提供完整源码、测试、模型、预测和运行记录，不要求把二进制权重提交GitHub。没有附件时，本地可按本报告的接口和公式实现，但必须把“尚未实现”作为状态，不伪造成功CLI。附带命令只适用于已取得并检查的候选文件：

```text
python train_prepared.py --matrix PREPARED.npz --manifest MANIFEST.json --out NEW_RUN
python observe_command.py --job JOB.json --root RUN_ROOT --timeout 4500
python research_components.py --plan hour_plan.json --receipts RUN_ROOT/receipts.jsonl --root RUN_ROOT --out RUN_ROOT/execution_receipt.json
```

## 7. 直接给本地agent的起始指令

你现在是本项目的本地研究执行者，不是新审计Markdown的撰写者。读取本报告，保存HEAD、dirty清单、数据库存和旧预算。已有授权允许时在隔离副本把旧“只修P0/P1、同报告即退出”合同替换为研究队列；不得修改排程、权限模式、Actions或其他项目。不要再次嵌套启动run_fixup.py。

先建立最短可运行数据→特征→训练链，然后立即开始真实任务。读取历史结果和checkpoint，有READY/INTERRUPTED/待分析预测就继续。所有源行情和旧结果只读，候选代码和产物写本次run，产品push沿用独立授权，没有就只交diff。不要为了等所有报告一致性问题修完而阻塞有效开发训练。

本次目标有效本地研究>=3600秒，其中真实训练>=1800秒、完成>=6个不同有效fit，并有完整预测/模型/错误分析。最多75分钟、18次新fit，服从外层和已确认总预算。H504无合法折时执行已明确命名的历史自监督分支，不缩短504、不用H10或随机切分冒充正式成绩。任务提前完成但不满一小时，继续下一预登记探索；没有有价值任务则报告时长未达标，禁止凑时。

结束不要只回复“修改两份文档”。回传真实处理行数/股票数、训练日志/命令/退出码、模型与预测hash、elapsed/CPU/GPU观测、完整台账、hour_verdict、market_result_verdict和剩余队列。源码缺失、数据缺失、预算不足分别报具体阻塞；文档发布不等于本机执行。

## 8. 固定源码证据与边界

- [旧执行合同](https://github.com/fy-god/pro-web-60d-strategy/blob/908a41334eaf2f38570a7fb93954a63ebb80ad52/scripts/fixup_prompt.txt)：仅P0/P1及同报告NO_NEW_REPORT。
- [launcher](https://github.com/fy-god/pro-web-60d-strategy/blob/908a41334eaf2f38570a7fb93954a63ebb80ad52/scripts/run_fixup.py#L227-L258)：5400秒上限，不能因四小时触发间隔假定单次四小时。
- [数据入口](https://github.com/fy-god/pro-web-60d-strategy/blob/908a41334eaf2f38570a7fb93954a63ebb80ad52/src/data_pipeline.py#L1-L123)：本地源文件与可能自动写盘的load_panel行为。
- [现有矩阵元数据](https://github.com/fy-god/pro-web-60d-strategy/blob/908a41334eaf2f38570a7fb93954a63ebb80ad52/outputs/ml/matrix_h10_t30_s1_meta.json)：2,680,715行、3,193股、887日；H10，不是H504训练结果。
- [发布前完整索引](https://github.com/fy-god/pro-web-60d-strategy/blob/908a41334eaf2f38570a7fb93954a63ebb80ad52/docs/audits/expert-ml/LATEST.md)：保留此前独立复核与MEB记录。不同轮次工具环境与证据分开，本报告不复用它们的测试数。

云端权限仍只限本目录新增报告和LATEST。报告/索引发布提交与reviewed_source_sha分别记录；不改远端产品代码，不创建PR，不启用训练或交易触发器。本次排程和模型设置不变。下一轮优先核验研究源码diff、真实数据产物、checkpoint及一小时执行收据，而不是再写一个新因子名字。
