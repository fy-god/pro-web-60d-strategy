# 专家／ML 线审计 — 2026-09-22 03:34:01 JST（h=2）

- `audit_time_jst`: `2026-09-22T03:34:01+09:00`（本地 CST `2026-09-22 02:34:01`）
- `reviewed_repo`: `D:\ccc\pro-web-60d-strategy`
- `reviewed_source_sha`: `9bb1011d46a23718925bcab36f42d3494e71e060`（**实际 `git rev-parse HEAD` 读出**）
- `main_head_at_audit_start`: `8287b33da26978f1d513b7880baf06a9103d2879`（`origin/main`；本地 HEAD 是其祖先，落后 2 个**纯文档**提交）
- `previous_report`: [`2026-09-21_23-44-53_JST.md`](./2026-09-21_23-44-53_JST.md)
- `publication_kind`: `INDEPENDENT_REGRESSION_AND_NEW_REPORT_VERIFICATION`
- `open_pr_count`: **不可验证**（见 §7；`git ls-remote origin 'refs/pull/*/head'` 返回空，只证明无 advertised pull ref，不等于 0 个 open PR）
- `report_created_commit_sha`: `05556a9a5d4d95b52605357c1c1dceedf309b0fc`（`git ls-remote` 回读 = 本地 HEAD，**EQUAL**；`8287b33..05556a9 main -> main`；whitelist PASS，仅 2 个 `docs/audits/expert-ml/` 文档）
- 本轮 `REAL_MARKET_NEW_FIT = 0`；**没有新增实股结果**。

---

## §0 必须首先披露：我在本轮造成并已恢复的一次工作树事故

**这不是被审方的问题，是我的操作错误。必须写在最前面。**

**经过**：清点阶段我执行了一个命令块，其中**第一行**是 `git -C $R checkout origin/main -- .`，而我本意是"不要这么做、只读"——那句中文说明被我写成了**该命令之后的独立字符串**，于是**命令真的执行了**。后果：

1. `scripts/register_fixup_task.ps1` 的**并发写者未提交改动**被 `origin/main` 版本覆盖（即被还原成 HEAD 版本）；
2. 两个本来干净的文档路径被**意外 stage**：`docs/audits/expert-ml/2026-09-22_01-57-29_JST.md`（`A`）与 `LATEST.md`（`M`）。

**这直接违反了手册 §1 的"绝不覆盖他人未提交改动"，也违反了我自己在 `2026-09-20_19-47-00_JST.md:646-648` 写下的禁令**（"此后禁止在该仓库使用 `git reset --hard` 或 `git checkout -- .`"）。**我破坏了自己上轮立的规矩。**

**恢复（全部实证，可复核）**：

| 步骤 | 命令/依据 | 结果 |
|---|---|---|
| 定位正确 blob | `git fsck --unreachable` 枚举 36 个不可达 blob；以**文件谱系**而非长度判定 | `a2c6b85978aba1455ee697c6c2bc5a728aa72dc2`（7145 B / 173 行） |
| 谱系证据 | 上轮事故报告 `2026-09-20_19-47-00_JST.md:619-627` **逐字记录了同一 blob**，及其 +9/−1 的 diff | 与本次实测 `only_in_candidate=9 / only_in_HEAD=1` **完全一致** |
| 未被提交 | `git log --all --find-object=a2c6b85…` → **空** | 证明它确是"从未提交的改动" |
| 写回 | 按本仓约定（`core.autocrlf=true`）以 CRLF 写入 | 7318 B / 173 行 |
| 校验 | LF 归一后与 blob **逐字节相同** = `True` | 关键 3 行（`$cmdBody = ($cmdBody -split "\`r?\`n") -join "\`r\`n"`、`-Encoding ASCII -NoNewline`）**全部在场** |
| `git status` | 恢复后 | ` M scripts/register_fixup_task.ps1` + `?? .gitattributes` = **与事故前逐字一致** |
| 其它路径 | `git diff -- ':(exclude)…ps1'` → **空** | 未误伤任何其它文件 |

**我犯的第二个错误（同一次事故内，已改正）**：第一版恢复脚本我用"**候选里行数最长**"当启发式，结果选中了 `0e527c3955db`（605 行）——那其实是一份**提到 `schtasks`/`Register-ScheduledTask` 字样的 Markdown 审计报告**，我把它写进了 `.ps1`。我随即写了第二个脚本，改为**按谱系判定**并加入防护断言（候选必须 **不含** Markdown 标记 `publication_kind`/`audit_time_jst`/`## `，且**含** `param(`/`$cmdBody`），并**先校验后写**，才得到上表的正确结果。

**残余不确定性（如实声明）**：我**无法证明**恢复出的字节与事故前**逐字节**相同——事故前我没有取该文件的哈希。依据是：`git fsck` 只找到**一个**与该文件谱系相符的不可达 blob，其 diff 恰好 +9/−1、语义完整（正是 `.gitattributes` 注释所指的 CRLF 生成修复），且 `git status` 已与事故前一致。**我判定为同一份改动，但这是推断，不是字节级证明。**

**对子 agent 的影响（必须更正他们的归因）**：子 agent B 与 C 都在报告里写下"**工作树在会话中途被外部进程改动**"，C 还推测是"A/B 两个 sibling agent 或第二个自动化写的"。**这个归因是错的——那是我。** 我已把这一点写入本报告，避免错误归因被下一轮继承。

**已加固的纪律**：本轮之后，本仓一切同步只用**不触碰工作树**的方式（`git merge --ff-only` 或只读 `git show`）；**任何** `checkout/reset/restore/stash/clean` 一律禁用；子 agent 提示词中的"只读"约束**不能**保护工作树，主 agent 自己必须遵守。

---

## §1 待审范围：新增提交全部为文档，无源码推进

`4a4910e..origin/main` 共 **4 个提交**，`git diff --name-status` 全部路径：

```
A  docs/audits/expert-ml/2026-09-21_23-44-53_JST.md
A  docs/audits/expert-ml/2026-09-22_01-57-29_JST.md
M  docs/audits/expert-ml/LATEST.md
M  reports/AUDIT_STATUS.md
```

- **零个** `src/`、`scripts/`、`tests/`、`experts/`、配置或 Actions 改动 ⇒ 本轮**不存在**"某个源码缺陷被修好了吗"的问题；`HEAD` 与 `origin/main` 的**源码逐字节相同**，故本轮所有源码结论在两者上等价。
- 被审的新文档是 [`2026-09-22_01-57-29_JST.md`](./2026-09-22_01-57-29_JST.md)（300 行，`publication_kind=CANDIDATE_IMPLEMENTATION_AND_EXECUTION_PLAN_UPDATE`，自称"v5 candidate ledger 接入训练链"）。

## §2 被审报告（v5）核验：仓库状态声明为真，但"code 变更"全部不在本仓

子 agent A 逐条抽取 **56 条可检验声明**并实测。分布：`TRUE 23 / FALSE 2 / PARTIAL 4 / ABSENT-FROM-REPO 18 / UNVERIFIABLE-FROM-REPO 8 / CONTRADICTED 1`。

**我亲自复核的关键项（不是采信）**：

| 声明 | 实测 | 结论 |
|---|---|---|
| `reviewed_tree_sha = 20958632…` | `git rev-parse 9bb1011^{tree}` = `20958632f92e2dee6ad03dc9190031a51f74d2a0` | **TRUE（逐字节）** |
| `src/ml/research_h504/` 不存在 | `git ls-tree origin/main src/ml` → 13 个文件，无 `research_h504`；全仓 96 处 `research_h504` **全在 `docs/`**，`src|scripts|tests|experts` 内 **0** | **TRUE** |
| `scripts/fixup_prompt.txt` 会 `NO_NEW_REPORT` | `origin/main:scripts/fixup_prompt.txt:27` | **TRUE** |
| `run_fixup.py` 仍 `timeout=5400` | `:237` | **TRUE** |
| `src/lowzone.py` 的 V00 `min_tier=1` | `:208`；因果性 docstring `:7-9` | **TRUE** |
| `a00bf2f` / `8287b33` 各只改 1 个文档路径 | `git show --stat` | **TRUE** |

### §2.1 `已确认错误` P1：§8.1「`40 passed in 31.85s`」为**假**

- **文件/行**：被审报告 `2026-09-22_01-57-29_JST.md:182`（`40 passed in 31.85s`），其 §12 第 286 行还以"40 tests"为依据排下一轮优先级。
- **反例/实测**：在本机对 **`origin/main` 的完整解包副本**（2195 个文件）跑
  `python -m pytest -o addopts= -ra` → **`collected 15 items` / `15 passed` / 退出码 0**。
  我另在**真实工作树**独立复跑一次：**`15 passed in 1.50s`，退出码 0**（两次一致）。
- **机制**：`tests/` 只有 `__init__.py`（0 B）与 `test_engine.py`（527 行）；`git grep -c 'def test_'` → **15**。**任何收集路径都不可能得到 40。**
- **影响**：该数字是报告 §8.1 唯一的"测试证据"，并被 §12 当作验收依据；它**在本仓不可复现**（与作者自标"沙箱"一致——**不是造假指控**，但**不能作为本项目证据**）。
- **修复建议**：沙箱数字必须标注 `[SANDBOX-ONLY, NOT REPRODUCIBLE IN REPO]`，且不得进入验收优先级。
- **验收测试**：`python -m pytest -o addopts= -ra` 在 `origin/main` 上必须恰好 `15 passed`；报告若再引 40，即为回归。

### §2.2 `已确认错误` P2：§7 的前提被**证伪**（CONTRADICTED）

- **声明**（报告 `:160-166`）：v4 的 T0 把含 `vir*`／`vp_*`／`kdj_low_*` 的 `feature_cols` 全量送进 HGB，故基线偷含研究增量。
- **实测**：本仓唯一的 `feature_cols` 在 `src/ml/build_matrix.py:520-524`，是一个**列黑名单**；`outputs/ml/matrix_h10_t30_s1_meta.json` 的 82 个 `feature_names` 中 **无** `vir*`、**无** `vp_*`、**无** `kdj_low*`。全仓非文档命中：`vir_` = **0**、`vp_` = **0**、`kdj_low` = **0**（`kdj_low_z` 仅出现在 `audit/STRATEGY_INVENTORY.md:286` 的散文里）。`_baseline_feature_cols` 全仓 **1** 处命中，就是报告自己。
- **影响**：报告 §7 所述"纠正"的对象**在本仓不存在**，故该"修复"不可验证亦不可回归。
- **修复建议**：区分"沙箱内代码库"与"本仓"；对本仓的声明必须给出本仓 file:line。

### §2.3 `待验证风险` P2：§3–§7 全部符号在本仓**不存在**，§10 的 CLI 是虚构的

`fold_support_summary`、`fold_support_calendar_structural.json`、`calendar_structural_one_date_per_session_reference`、`min_train_candidates`／`max(100,2*min_samples_leaf)`、`Registry.latest_complete`、`REUSED_COMPLETE`、`task_scope`／`H504_JOINT`／`AUX_HISTORY`、`experiment_registry.jsonl`、`run_h504_hgb`、`_baseline_feature_cols` —— **每一个都只出现在该报告与其 `LATEST.md` 条目里**。
§10 的 CLI 参数 `--layers`/`--candidates`/`--evidence-type`/`--source-sha`/`--hgb-min-samples-leaf`/`--target-active-seconds`/`--normal-wall-limit-seconds`/`--hard-runner-timeout-seconds`/`--registered-task-timeout-seconds`/`--effective-prompt-sha256` → 本仓**各 0 命中**；`--stage` 只在不相关的 `scripts/audit_ml/audit_ml_nulls.py:625` 出现。
**判定**：报告 §11 自己承认候选包是"对话附件、不在 GitHub 产品树"，**这一点是诚实的**；但 §2 标题"**真正接入训练链**"与 §8"本轮实际沙箱执行"极易被下游读成"仓库已接入"。**建议**：`publication_kind` 已标 `CANDIDATE_IMPLEMENTATION…`，但正文标题与 `LATEST.md` 索引条目应显式加 `SANDBOX-ONLY`。

### §2.4 `未复现`：§8.2 合成链计数与 §9 的三个 SHA256 摘要

`1180 sessions / 12 stocks / 2664 candidates / 156 dev predictions / real_train_seconds=0` 与三个 SHA256：本仓**无** fixture、生成器、产物或附件文件 ⇒ **不可复现**，判 `未复现`，**不作为缺陷**（作者已自标沙箱）。

## §3 回归核对：上轮 8 个未修项 —— **8/8 全部仍坏**（子 agent C 独立复现，各自带反例）

| # | 项 | 现 file:line | 本轮实测反例（关键数） | 判定 |
|---|---|---|---|---|
| 1 | `EML-P0-FIXUP-SYNC-FAILOPEN` | `run_fixup.py:180`（fetch 返回值**被丢弃**）、`:183` `sync_code=…`、`:184`（只写日志，`sync_code` 全文件仅 2 次出现、**无分支**）、`:256-259` | 不可达 origin（`file:///…NO_SUCH_REMOTE.git`）→ `git fetch` **exit 128**，脚本**退出码 0**、stdout `fix-up ok` | **CONFIRMED-STILL-BROKEN** |
| 2 | `EML-P0-AUDIT-FETCH-FAIL-PASS` | `scheduled_report_audit.py:84-86`、`:108`、`:344`、`:376` | fetch **128** → `changed=[]`，脚本 **exit 0**，发布 `Verdict **PASS**` + `Remote drift \| git fetch failed (exit 128)…` | **CONFIRMED-STILL-BROKEN** |
| 3 | `EML-P1-AUDIT-DRIFT-PASS` | `scheduled_report_audit.py:88-89` | 3 路径漂移被 pathspec 收敛为 **1**；**2121/2195** 个受跟踪文件对漂移**不可见** | **CONFIRMED-STILL-BROKEN** |
| 4 | `EML-P0-AUDIT-STALE-ATTEST` | `reports/AUDIT_STATUS.md:11` vs `:88-89,:94,:108,:141` | 实际 2 路径漂移，已发布文本仍为 `0 path(s) differ`；文件内**无 commit SHA、无 dirty 标记** | **CONFIRMED-STILL-BROKEN** |
| 5 | `EML-P0-COOLDOWN-INDEPENDENT-CLOCK` | `src/labels.py:233,242` | 97 天间隔的一对：单独 → 保留 **1/2**；加入无关填充股 → **2/2**（**帧内局部**，非墙钟；**标签更正**） | **CONFIRMED-STILL-BROKEN** |
| 6 | `EML-P1-POOLED-NO-DEDUP` | `src/live_readiness.py:72,87` | 全文**无** `drop_duplicates`/`duplicated`；本地输入 646,718 行 / 35 策略 / 重复 `(code,date)` **55.5547%** | **CONFIRMED-STILL-BROKEN** |
| 7a | `EML-P1-HOLDOUT-ARTIFACT-SCHEMA-DRIFT` | `src/ml/final_holdout.py:294-348` | producer 字面量 **25** 键 vs 受跟踪 **27**；缺的正是 4 个 `date_clustered_*` | **CONFIRMED-STILL-BROKEN** |
| 7b | `EML-P1-SEARCH-REPORT-SCHEMA-DRIFT` | `src/ml/search.py:410-433` | producer **15** 键；受跟踪报告 8/8/11；`ranked_unfiltered` 在 **0/15** 份报告中 | **CONFIRMED-STILL-BROKEN** |

**我亲自独立验证的两条承重结论**（不采信子 agent 结论）：
- `scheduled_report_audit.py:108` `ok = code == 0 and not unstable` —— **实测该行源码不含 `changed`**；而 `:344` `needs_attention = code != 0 or bool(changed) or bool(unstable)` —— **实测该行含 `changed`**。⇒ **漂移永远无法把已发布判决定性为红色，尽管控制台会打印 ATTENTION**。这是 #2/#3/#4 的共同根因，**一行之差**。
- `run_fixup.py` 中 `sync_code` 全文件**仅出现 2 次**（赋值 + 日志），**从无分支读取**；唯一决定退出码的是 `:256` `ok = completed.returncode == 0`（**只看 agent**）。⇒ 同步失败在**两个方向**都不可见。

## §4 本仓自带审计「629 checks / 0 problems / PASS」——**数字为真且可复现，但覆盖面被高估**

- **实测**：`python scripts/audit_reports.py` → **`629 checks run, 0 problem(s)`，退出码 0**（我亲自跑）。
  `--verbose` 分节：`arithmetic=8, cross_report=327, frontier=162, markdown_structure=7, orphan_reports=1, pooling=84, prose=33` = 622 节内 + 7 census = **629**。
  **控制实验**：注入真实问题 → 退出 **1** → `ATTENTION` ⇒ 检查器本体是 **fail-closed** 的。
- **`SECTION_FLOORS` 计数上限确实存在且有效**（`audit_reports.py:135-143`，合计 **620**）：注入 `{}` 使某节少跑检查会触发 `FAIL section 'pooling' ran at least 999 checks (ran 84)` → 退出 1。**但它只防"缩水"，且 620 vs 实际 622 仅 2 条余量，并与检查代码同文件**（同一次提交里改两处即可静默放行）。
- **`已确认错误` P1（fail-open 在发布器，不在检查器）**：`scheduled_report_audit.py:108` **不含 `changed`**，故三条路径可发布 PASS：
  1. **fetch 失败**（`:84-86`）→ `changed=[]` → `Verdict PASS`（子 agent B 用**真实 fetch exit 128** 端到端产出该文本；且 `run_report_audit.cmd` 不传 flag、任务 XML `RunOnlyIfNetworkAvailable=false`，离线必走此路）；
  2. **无诊断输出** → `summary={}`（`:116-122`）→ `Checks \| see log` 仍 PASS；
  3. **零检查** → `\| Checks \| 0 checks run, 0 problem(s) \|` 仍 PASS（`audit_reports.py:3390` `return 1 if f.problems else 0`，**无全局下限**）。
  4. **漂移无法致红**（`:108` 漏 `changed`，`:344` 却读）——文件写 PASS 而控制台打印 `ATTENTION REQUIRED`。
- **中途变更防护有后缀与范围双重限制**（`:72-73` 只哈希 `reports/**` 的 `.json`/`.csv`）：审计期间新增 `reports/NEW_MID_AUDIT.md` → PASS 且**无 unstable 段**；`README.md` 中途被改 → PASS、drift 0。而 `audit_reports.py` 的断言恰恰硬依赖 `README.md`/`TARGET_70PCT.md`/`RESULTS.md`/`src/*`/`outputs/ml/*`/`data/cards_100` —— **全部在哈希范围之外**。控制实验：新增 `.json` **会**被抓到（说明机制本身没问题，是**范围**问题）。
- **头条数字**：仅**内部一致性**被验证（`precision==hits/signals`、`lift==precision/base_rate`、Wilson 与 date-clustered CI 包住点估计、README/TARGET_70PCT 散文与 JSON 必须一致，`:1792-1814`）。**未被独立验证**：`base_rate 2.90%` 没有独立锚（而它正是 4.70x 的分母）；`6,202`/`844` 从未从数据重算；`ml_crosssec_hgb0.json` 仅作 provenance 读取（其"Known gaps"已自认）。

## §5 状态三轴与分类计数

| 轴 | 值 |
|---|---|
| `execution_status` | `COMPLETED`（子 agent A/B/C 全部交付；主 agent 亲自复核承重结论；未 `NO_NEW_REPORT`） |
| `research_verdict` | `已确认错误` **3**（v5 §8.1 假通过数 P1；发布器 fail-open 三路径 P1；§7 前提被证伪 P2）、`待验证风险` **2**（v5 沙箱符号/CLI 不在本仓、中途变更防护范围）、`已经修复` **0**、`修复后回归` **0**、`未复现` **2**（§8.2 合成计数、§9 摘要；另 `open_pr_count` 不可验证） |
| `evidence_status` | 软件样本（真实命令 + 真实退出码 + 真实计数）；**实股结果 0** |
| `evidence_type` | `PROVENANCE_AND_RECONCILIATION` ＋ `ARCHIVED_REAL_MARKET_REEVAL` |

**分类计数**：`已确认错误` 3｜`待验证风险` 2｜`已经修复` 0｜`修复后回归` 0｜`未复现` 2。
**三者分开**：`程序修复` = 0（未写入仓库，仅给建议）；`任务定义变更` = 1（沙箱数字必须标 `SANDBOX-ONLY` 且不得进入验收优先级）；`真实模型增益` = **0**。

## §6 本轮实际运行过的命令与真数

| 命令 | 真实结果 |
|---|---|
| `git rev-parse HEAD` / `origin/main` | `9bb1011d46a2…` / `8287b33da269…` |
| `git diff --name-status 4a4910e..origin/main` | 4 路径，**全为 docs** |
| `git rev-parse 9bb1011^{tree}` | `20958632f92e2dee6ad03dc9190031a51f74d2a0`（= 报告声称值） |
| `python -m pytest tests -o addopts= -ra`（真实工作树） | **`15 passed in 1.50s`，退出码 0** |
| 同上（`origin/main` 完整解包副本） | **`collected 15 items` / `15 passed`，退出码 0** |
| `python scripts/audit_reports.py` | **`629 checks run, 0 problem(s)`，退出码 0** |
| `git diff --name-only HEAD origin/main -- reports/ '*.md'` | 2 路径（**唯一**进入 `Remote drift` 的内容） |
| `git fsck --unreachable` | 36 个不可达 blob；谱系匹配 `a2c6b85…`（7145 B/173 行） |
| 恢复后 `git status --porcelain` | ` M scripts/register_fixup_task.ps1` + `?? .gitattributes` |

**未运行及原因**：`docs/audits/validate_latest.py` —— 本仓**不存在**该文件（`Test-Path` = False，全历史亦无）⇒ **不声称通过该闸门**，**不编造**。训练/fit：**未运行**（本仓无 H504 代码，§8.2 无 fixture）。

## §7 1–3 条有依据的研究/改进想法

1. **把"漂移"接到判决定上，只需一行**（依据 §3 #2/#3/#4 + §4：`:108` 漏 `changed` 而 `:344` 已读）。
   改 `ok = code == 0 and not unstable and not changed` 即让 2121/2195 的不可见面与 fetch 失败**同时**变为红色。
   **验收**：在临时克隆里制造 `src/` 漂移，运行发布器，断言产出文本为 `ATTENTION`（当前为 `PASS`）。
2. **零检查必须有全局下限**（依据 §4 第 3 条：`audit_reports.py:3390` 只看 `f.problems`；`SECTION_FLOORS` 合计 620 仅 2 条余量且与检查同文件）。
   建议加 `if f.checked < 600: return 1`，并把 floors 表移出检查文件。
   **验收**：把某节置为 no-op，断言退出码 1（当前若整节被移除且 floors 同步下调则为 0）。
3. **快照范围应覆盖检查器真正依赖的路径**（依据 §4 第 4 条：`:72-73` 只哈希 `reports/**.{json,csv}`，而断言硬依赖 `README.md`/`TARGET_70PCT.md`/`RESULTS.md`/`src/*`/`data/cards_100`）。
   **验收**：审计中途改 `README.md`，断言出现 `unstable` 段（当前无）。

## §8 发布与边界

- `report_created_commit_sha`：**`05556a9a5d4d95b52605357c1c1dceedf309b0fc`**（本轮报告所在提交；`git ls-remote origin refs/heads/main` 回读 = 本地 HEAD，**EQUAL**；whitelist PASS，仅 2 个文档）。
- **本轮先做了一次 `--ff-only` 同步再提交**：因本地 HEAD `9bb1011` 是 `origin/main` `8287b33` 的**祖先**，若直接提交会把 `8287b33` 的 v5 索引条目**静默回退**。故先把**我自己的** `LATEST.md` 改动还原，`git merge --ff-only origin/main`（实测 `Updating 9bb1011..8287b33`），再在新版上重新施加我的索引段。**同步前后均校验并发写者的 ` M scripts/register_fixup_task.ps1` 与未跟踪 `.gitattributes` 原样存活**（`ff-merge` 影响的仅 2 个文档路径，与该文件无关）。`LATEST.md` 同时保留 v5 条目（3 处提及）与我的新条目。
- 索引指针：before **12** → after **13**，**丢失 0 个**；加性证明 `after.replace(inserted, anchor, 1) == before` = **PASS**。
- 门禁：本仓**无** `docs/audits/validate_latest.py` ⇒ **未运行、不声称 PASS**。
- 边界：本轮**只新增本 Markdown 与更新 `LATEST.md`**；**未**改任何产品源码/权重/配置/Actions/PR；**未**创建或修改任何定时任务/排程（未调用 `schedule_*`、`arm_self_wake`、`schtasks`、`Register-ScheduledTask`）；**未**启动训练或真实通知/交易；**未** push 代码。
- **`scripts/register_fixup_task.ps1` 的并发写者改动**：本轮**我一度误删、随后已恢复**（§0），恢复后 `git status` 与事故前逐字一致；该改动**未被我提交**，仍是他人的未提交改动。`.gitattributes`（未跟踪）**全程未触碰**。
- 所有计数、退出码、hash 均为本轮**实际运行**输出；未运行项已逐条注明原因；**无任何编造**。
