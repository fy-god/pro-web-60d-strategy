# 专家／ML：研究源码实际写入 main

- 时间：2026-09-22T03:49:55+09:00。
- 类型：`SOURCE_IMPLEMENTATION_PUBLISHED`，不是又一份未落地候选方案。
- 用户本轮明确授权：“一定要写入GitHub，随便改”。本次将范围扩展到本项目必要研究源码、测试、runner与提示词；不触碰其他项目、真实交易、付费资源或远端训练。
- 测试起点：`8287b33da26978f1d513b7880baf06a9103d2879`。发布前并发主线更新至 `bbc82825c79c7f354179f7198514ab00b96d2c5f`，差异仅两份审计文档路径，因此保留其全部变更并以前者后继为父提交。
- **源码提交：[`878cdeba5837a302004a8b3a656afee3d6a62b44`](https://github.com/fy-god/pro-web-60d-strategy/commit/878cdeba5837a302004a8b3a656afee3d6a62b44)**。
- 源码树：`ff2e1c662e206d8fa2dc93821b8dce3e79907c7c`。
- 通过 GitHub Git Data API 创建完整树、创建单个源码提交、非强制 fast-forward main；不是仅上传孤立blob或本地ZIP。

## 实际变更

源码提交共23个路径：16个 `src/ml/research_h504/*.py`、2个 `tests/research_h504/*.py`、修改 `scripts/run_fixup.py` 和 `scripts/fixup_prompt.txt`，新增根目录 `RESEARCH_QUICKSTART.md` 及2份验证文件。

研究模块树SHA `60086cf074a73c6956088c81aab5aa7ab3261b95`、测试树SHA `7b5d79c3f0f8872f11726514f54bf286ab990a72`，均与本轮实际测试的本地文件字节重新计算结果一致。

已经入库的功能：独立市场日H504 oracle；保留candidate_id；无次日Open不顺延；早结果可知时间与完整随访训练资格分离；显式FeatureSpec；实际候选支持检查；数据日历对齐；快照/KDJ路径/量压/VIR；家族ECDF/MEB模块；T0 HGB；AUX因果TCN；训练状态检查点、完整开发预测及收据。

本轮改进：真实数据CLI必须带独立 `--calendar`，不能用面板日期并集冒充交易所日历；未来排期不计入随访长度；H504 dev发布候选不先按标签是否已知过滤；源码、日历和参数进入signature；TCN默认CPU，不自动占用GPU。统一入口实际接受 `--stage all --resume --offline`。

runner改为13200秒硬上限；dry-run只读；dirty工作区停止而不是自动stash；fetch/ff-only同步失败不启动agent；增加单实例锁和本次进程树清理。实际本地提示词已替换，不再“只修P0/P1”和“同报告直接NO_NEW_REPORT”。

## 本轮实际验证

命令：`OPENBLAS_NUM_THREADS=2 OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 PYTHONPATH=. python -m pytest -q tests/research_h504 --disable-warnings`。

**50 passed in 12.66s，退出0。** 这是隔离目录的研究源代码/launcher mock测试，含合成HGB/TCN训练和重载、日历缺口、标签边界、候选ID、未知候选预测及同步失败测试。原候选先在45秒和120秒预算超时，随后将oracle改成每股一次数组对齐，重跑通过；没有把超时计作通过。

[完整验证范围与源码哈希](https://github.com/fy-god/pro-web-60d-strategy/blob/878cdeba5837a302004a8b3a656afee3d6a62b44/docs/research/h504_integration_validation.json)；[实际pytest输出](https://github.com/fy-god/pro-web-60d-strategy/blob/878cdeba5837a302004a8b3a656afee3d6a62b44/docs/research/h504_integration_pytest.txt)。

`real_market_fit_count=0`。完整既有仓库测试未重跑：本轮clone受DNS限制，不能据此声称旧15项或629项同时通过。没有声称H504精确率提升。

## 已实现与尚未完成

本轮是源码集成，不是全部30项研究槽位完成。T0和AUX base/plus可执行；完整T1—T5、MLP对照、OOF/meta、独立校准和最终发布评价还需要本地继续实现。registry暂不是完整全局预算控制器；剩余48次fit需本地执行者核对。checkpoint可恢复训练状态，不宣称中途batch级位一致恢复。PIT股票池、复权可比性及实际成交可执行性仍需真实数据认证。

## 本地接手

直接同步GitHub源码后读 [RESEARCH_QUICKSTART.md](https://github.com/fy-god/pro-web-60d-strategy/blob/878cdeba5837a302004a8b3a656afee3d6a62b44/RESEARCH_QUICKSTART.md)，不再从聊天ZIP逐个拼模块。先保护已有dirty文件，在干净副本同步，然后运行：

```text
python -m src.ml.research_h504.run --help
python -m pytest -q tests/research_h504
python scripts/run_fixup.py --dry-run
```

真实研究命令：

```text
python -m src.ml.research_h504.run --stage all --panel <实际panel> --calendar <独立calendar.csv> --candidates <因果candidate_manifest.csv> --out <本批专用目录> --resume --offline --evidence-type REAL_MARKET --source-sha <实际HEAD>
```

缺H504合法成熟折则保留 `BLOCKED_PROTOCOL_H504`，同一session继续AUX真实历史表征；不得缩短期限、随机切分或混报任务指标。一个任务完成后继续有依据的READY任务，不sleep或重复同hash凑三小时。

**本机Windows注册任务没有被本对话修改，也没有启动本机训练。** 已注册上限、任务是否存在、DSH可用性必须本地核验；旧XML模板可能仍为PT2H30M。只改runner不能延长正在运行的父进程或Windows实际上限。三小时依旧为 `LOCAL_APPLY_PENDING / NOT_VERIFIED`，但“研究源码未入库”和“远端runner仍5400秒”的旧描述现在已不适用于本源码提交。

## 交付判定

- GitHub源代码：**已写入main并回读**。
- 新测试源码：**已入库，50项在本轮隔离环境通过**。
- 旧H10数据、权重和结果：**未改**。
- 注册任务、云端排程、Actions：**未改**。
- 本机三小时运行：**未验证启动**。
- 新H504市场成绩：**无**。

历史审计继续保留，其结论必须按原source SHA解释；不能将本次新模块的测试反向认证为旧生产链已全部修复。
