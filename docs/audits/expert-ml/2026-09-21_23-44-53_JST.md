# 专家／ML 审计补充：**11/11 遗留项全部仍坏**；并更正我自己汇总里**口径最危险的一处**——去重对净值的方向**取决于统计量**（均值 +60.82%，中位数 −80.60%）

- `audit_time_jst`: `2026-09-21T23:44:53+09:00`
- `audit_time_cst`: `2026-09-21 22:44`
- `reviewed_source_sha`: `4a4910ed3ba070a8809cc6ad49ad29079564d7e1`（= `origin/main`，`ls-remote` 回读一致）
- `previous_report`: `docs/audits/expert-ml/2026-09-21_23-32-12_JST.md`（同一轮的独立审计）
- `open_pr`: 0
- 本轮 `REAL_MARKET_NEW_FIT = 0`；**不是**新的实股成绩，**未重训**。

`execution_status`: `COMPLETED_READ_ONLY_AUDIT_PLUS_INDEPENDENT_RERUN`
`research_verdict`: `ALL_11_OPEN_EML_ITEMS_STILL_BROKEN / ONE_PUBLISHED_MAGNITUDE_HAS_A_WRONG_SIGN_FOR_ITS_STATISTIC / ONE_FINDING_SCOPE_NARROWED / ONE_FINDING_LABEL_CORRECTED`
`evidence_status`: `ARCHIVED_REAL_MARKET_REEVAL / REAL_CODE_MEASUREMENT / PROVENANCE_AND_RECONCILIATION`

> **`evidence_type` 声明**：证据来自**真实代码执行**（`labels.dedupe_signals`、`live_readiness.assess` 在真实 646,718 行账本 + 真实 2,680,715 行面板上的实跑）与**真实归档产物**。
> **本轮 `real_market_fit_count = 0`**，没有新拟合、没有新收益数字。**软件测试通过数不计入真实模型增益。**

---

## 0. 为什么这一份必须写：**我上一份汇总里有一处口径会把方向搞反**

我上一份报告（`2026-09-21_23-32-12_JST.md`）的 §6 表里，对 `EML-P1-POOLED-NO-DEDUP` 只写了「已确认错误」，**沿用了早前报告的量级 −32.38%**。

本轮我**在真实数据上实跑**该去重对比，得到：

```text
账本  outputs/webpro_signals.csv   646,718 行 × 29 列   35 个 strategy_id
重复 (code,date)                    359,282 行 = 55.5547%
去重后                              287,436 行
```

**去重对净值的方向不是一个符号，而是取决于统计量**：

| 统计量 | 原样（已发布） | 去重后 | 变化 |
|---|---:|---:|---:|
| `signals` | 646,718 | 287,436 | −55.5547% |
| `resolved` | 637,499 | 283,768 | −55.4873% |
| **`gross_mean_return`** | **0.0044660826** | **0.0071822910** | **+60.8186%** |
| `gross_median_return` | −0.0054054054 | −0.0010483829 | **−80.6049%** |
| `net_mean_return` | 0.0034460826 | 0.0061622910 | +78.8202% |
| `net_median_return` | −0.0064254054 | −0.0020683829 | **−67.8093%** |
| `net_win_rate` | 0.4661575940 | 0.4870915678 | +4.4908% |
| `net_p10` | −0.1187665522 | −0.0979614804 | −17.5176% |
| `net_p90` | 0.1283141746 | 0.1166270588 | −9.1082% |

⇒ **「去重会抬高/压低成绩」这句话本身不成立**：它对**均值是抬高 +60.82%**，对**中位数是压低 −80.60%**。
⇒ 早前那个 **−32.38%** 既**不等于**均值的 +60.82%，也**不等于**中位数的 −80.60%，且**符号与均值相反** ⇒ 判定 **未复现（量级与符号都不对）**，但**缺陷本身成立**（见 §2 第 7 项）。

**⚠️ 这同时更正我上一份报告**：我在 `2026-09-21_23-32-12_JST.md` §6 沿用了 −32.38% 而未加限定。**正确写法是「缺陷成立（`:72` 合并后从不按 (code,date) 去重），但其量级必须指明统计量，且不能写成单一负数」。** 我在此明确纠正自己。

**机制**（`src/live_readiness.py` 实读）：

```text
 72|     merged = signals.merge(returns, on=["code","date"], how="left")
 78|     r = live["return_h"].to_numpy("float64")
 87|         "gross_mean_return": float(r.mean()),
```
`assess()` 在 `:72` 直接合并**多策略**账本，**全程无 `drop_duplicates`**（全文件 `grep` 只有 `:123/:126` 两处 `nunique` 是别的用途）。同一 (code,date) 可被最多 35 个策略各计一次 ⇒ **一条经济信号按策略数重复计权**。均值被**重复行占比高的小额正样本**抬起来、中位数被**去重后暴露的负中位数**压下去，两者方向相反**是数学上自洽的，不是矛盾**。

---

## 1. 11/11 遗留项**全部仍坏**（子 agent B 单列回归）

子 agent B 在冻结副本内逐条复跑，结论 **已确认错误 11 · 已经修复 0 · 未复现 0**：

| # | 项 | 判定 | 真实 file:line | 关键实数 |
|---|---|---|---|---|
| 1 | `EML-P0-FIXUP-SYNC-FAILOPEN` | 已确认错误 | `scripts/run_fixup.py:180`（fetch 返回码丢弃）、`:183-184`（rebase 返回码仅记日志）、`:233-238`（仍起 agent） | 远程不可达时 **exit 0**，打印 `fix-up ok`，日志 `rebase exit: 128` 后紧跟 `result: OK` |
| 2 | `EML-P0-ARR-ARTIFACTS-ABSENT` | 已确认错误（**范围收窄**） | `2026-09-20_03-06-03_JST.md:187` | `git rev-list --objects --all \| grep arr_candidate` = **0 / 3,296** 对象；CLI `exit 1 ModuleNotFoundError` |
| 3 | `EML-P0-AUDIT-FETCH-FAIL-PASS` | 已确认错误（**范围收窄**） | `scripts/scheduled_report_audit.py:84-86`、`:108`、`:344` | 真实模块 + 不可达 origin ⇒ 已发布 `Verdict **PASS**`，同时 NOTE 写 `git fetch failed (exit 128)` |
| 4 | `EML-P1-AUDIT-DRIFT-PASS` | 已确认错误 | `scripts/audit_reports.py:249-262` `TOP_LEVEL_FIELDS`、`:185-211` `ARTIFACT_SHAPES` | `exit 0`，`629 checks, 0 problems`，**同时** 4 个 producer 键缺失 |
| 5 | `EML-P0-COOLDOWN-INDEPENDENT-CLOCK` | 已确认错误（**标签纠正**） | `src/labels.py:233`、`:242` | 97 会话间隔的 2 信号 → 保留 **1/2**；加入填充股票 → 保留 **2/2** |
| 6 | `EML-P1-META-BLACKLIST-H10-NAMES` | 已确认错误 | `src/ml/walkforward.py:58-61`、`:108-109` | 注入 21 个未来/标签列 → **20 个被收进特征**；`forward_outcomes()` 的 8 列 **8/8 全被收进** |
| 7 | `EML-P1-POOLED-NO-DEDUP` | 已确认错误（**量级未复现，见 §0**） | `src/live_readiness.py:72`、`:87`（我实测；B 报 `:155` 调用点） | 646,718 行；重复 55.5547%；均值 **+60.82%**、中位数 **−80.60%** |
| 8 | `EML-P0-AUDIT-STALE-ATTEST` | 已确认错误 | `scheduled_report_audit.py:88-94`、`:108`、`:241-267` | 漂移清单 2 个路径 + checker `exit 0` ⇒ 仍发布 **PASS** |
| 9 | `EML-P1-HOLDOUT-ARTIFACT-SCHEMA-DRIFT` | 已确认错误 | `src/ml/final_holdout.py:294`、`:312-321` | 字面量 **25** 键 vs tracked **27**；**恰缺 4 个** `date_clustered_*`/`holdout_calendar_sessions`；真实重跑产出 **31** |
| 10 | `EML-P1-SEARCH-REPORT-SCHEMA-DRIFT` | 已确认错误 | `src/ml/search.py:410-433` | producer 字面量 **15** 键；tracked 8/8/11（缺 8/8/4）；`ranked_unfiltered` 在 **0 / 15** 个 tracked `reports/*.json` 中出现 |
| 11 | date-clustered CI 陈旧 | 已确认错误 | `final_holdout.py:251`/`:254`；`README.md:550`、`TARGET_70PCT.md:344,546`、`docs/REVIEW_RESPONSE.md:162` | 重跑：`block=5` 宽 `0.12296923779128671` vs `block=1` 宽 `0.07349438854487804`；tracked 宽 **恰等于 block1**（等值判定 True）；比值 `1.673179` ⇒ 已发布区间比当前定义**窄 40.2335%** |

### 1.1 我独立复核了三个「承重」断言（不只采信子 agent）

| 复核对象 | 我的命令与结果 | 结论 |
|---|---|---|
| **B2 冷却时钟是「帧局部」而非墙钟** | 实跑 `labels.dedupe_signals`：单股票 97 会话间隔 → 保留 **1/2**；同一两信号 + 每会话插入填充股票 → 保留 **2/2**（`exit 0`） | **证实**，且**标签确实错了** |
| **B3 仓库 checker 其实是 fail-closed** | 在副本内破坏 `ml_final_holdout.json`（写成非法 JSON）→ `exit 1`，`590 checks, 6 problems`；删除该文件 → `exit 1`，`590 checks, 5 problems` | **证实**：checker **会**失败关闭；fail-open 只在 git-fetch 外层 |
| **B1 去重量级与符号** | 用 `main()` 的**精确预处理**（`:143-149` `dtype`/`usecols`/`parse_dates`/`zfill`）实跑 `assess` → 我得到 `0.0044660826` / `0.0071822910` / `resolved 283,768` / **+60.8186%** | **与 B 逐个数字一致**；但方向**取决于统计量**（§0） |

### 1.2 我**更正两处标签/范围**（不让错误标签继续传播）

1. **`EML-P0-COOLDOWN-INDEPENDENT-CLOCK` 的名字不准确。** 缺陷成立，但机制**不是**「用了墙钟/独立时钟」，而是 **`labels.py:233` 的 `session_index` 由 `out["date"].unique()` 构造** —— 即**只在传入的那一帧内编号**。我的实测：**保留与否取决于帧里还有哪些其他股票**。⇒ 正确表述是 **`FRAME-LOCAL cooldown clock`**。**（此项我改判为「已确认错误，标签需更正」。）**
2. **`EML-P0-ARR-ARTIFACTS-ABSENT` 范围收窄。** 只有 `2026-09-20_03-06-03_JST.md:187` 声称仓库内**存在**该适配器；17:31 那份**明确写了「不冒称已加入仓库 src」**。⇒ 缺陷是**单一文档的越界断言**，不是「多份报告都声称有」。**（同样：缺陷成立、范围收窄。）**
3. **`EML-P0-AUDIT-FETCH-FAIL-PASS` 范围收窄**（见上表）：fail-open **只在 git-fetch 包装层**；`audit_reports.py` 本体对读入/形状错误**会** fail closed。B 用注入测试证明：损坏 JSON → `623/3/exit 1`；截断对象 → `623/3/exit 1`；删文件 → `623/2/exit 1`；删 `ml_search_wide.json` → `564/7/exit 1`；剥掉 `per_fold_signals` → `576/7/exit 1`。**我独立复现了前两条。**

---

## 2. 分类与计数

| 项 | 判定 |
|---|---|
| 11 个遗留项 | **已确认错误 = 11**（我在 §1.1 独立复核了其中 3 个的承重断言） |
| `EML-P1-POOLED-NO-DEDUP` 早前发布的 **−32.38%** | **未复现**（量级错、符号对均值反）—— 我**并更正自己上一份报告的沿用** |
| `EML-P0-COOLDOWN-INDEPENDENT-CLOCK` 的 **标签** | **标签错误，缺陷成立**（应为 frame-local） |
| `EML-P0-ARR-ARTIFACTS-ABSENT` / `EML-P0-AUDIT-FETCH-FAIL-PASS` 的 **范围** | **范围收窄**（单一文档越界；fail-open 仅在 fetch 包装层） |

**`程序修复`：0**（本轮为纯复核，未改任何源码）。
**`任务定义变更`：1** —— `live_readiness` 的输出契约必须声明**每个统计量在去重下是否口径一致**；当前 `gross_mean_return` 与 `gross_median_return` 对去重的**方向相反**，一个只公布均值的接口会把整份报告的方向讲反。
**`真实模型增益`：0** —— 无拟合、无新预测。**归档重分析不得冒充实股新训练。**

**未复现 / 未运行清单（如实）**：`docs/audits/validate_latest.py` 在本仓库**不存在**（`Test-Path` False）⇒ 未运行、**不编造通过**；真实 `dsh` fixup agent（第 1 项）以 `DSH_BIN` 桩替代，**未启动真实 agent**（超出本线范围）；`arr_candidate` 包在所有 git 对象中都不存在 ⇒ 无可运行；**未跑全量测试套件**（本项不要求，且不宣称零回归）。

---

## 3. 2 条有依据的研究/改进想法

1. **给 `assess()` 加一条「统计量方向一致性」断言。** 依据：§0 的实测表 —— 同一份去重对比里均值 `+60.82%` 而中位数 `−80.60%`，**符号相反**。做法：在 `live_readiness` 的输出里同时落**去重前/去重后的每一个统计量**，并对「均值与中位数方向相反」的情形**显式标注**，使报告无法只挑一个方向叙述。这条断言在**今天的代码上失败**（无去重列），在补齐后通过。
2. **把「遗留项的标签必须由代码机制命名」变成可执行检查。** 依据：§1.2 的两处更正 —— `EML-P0-COOLDOWN-INDEPENDENT-CLOCK` 的实际机制是**帧局部**而非墙钟；照错误标签去修会**修错地方**（去改时钟源，而真因是 `:233` 在帧内构造索引）。做法：要求每个缺陷 id 的机制词必须能在源码里对应到一处 `file:line`，否则降级为 `待验证风险`。本轮的 `EML-P0-ARR-ARTIFACTS-ABSENT` 就是这一条的正例（范围收窄到单一文档一行）。

---

## 4. 声明

- `execution_status = COMPLETED_READ_ONLY_AUDIT_PLUS_INDEPENDENT_RERUN`；三轴见文首。
- 本报告 `docs-only`：只新增本文件 + 追加式更新 `LATEST.md`。
- **未改任何源码/权重/配置/Actions/PR。**
- **未创建、修改或删除任何定时任务或排程。**
- **未触碰他人未提交改动**：`scripts/register_fixup_task.ps1`（` M`）与 `.gitattributes`（`??`）全程原样。
- 所有实跑**只读**仓库数据（`outputs/webpro_signals.csv`、`data/panel_daily.parquet`）；**破坏性注入测试只在 `%TEMP%` 副本内**进行。
- 本仓库**不存在** `docs/audits/validate_latest.py` ⇒ 未运行、**不编造通过**。
- **未跑全量测试套件** ⇒ 不宣称任何零回归。
- `程序修复 / 任务定义变更 / 真实模型增益` 分开计（§2）。

---

## 固定来源

```text
仓库                  D:\ccc\pro-web-60d-strategy
HEAD = origin/main    4a4910ed3ba070a8809cc6ad49ad29079564d7e1
本行上一份报告        docs/audits/expert-ml/2026-09-21_23-32-12_JST.md
去重实测              outputs/webpro_signals.csv 646,718 行; 重复(code,date) 359,282 = 55.5547%
                      gross_mean_return 0.0044660826 -> 0.0071822910 (+60.8186%)
                      gross_median_return -0.0054054054 -> -0.0010483829 (-80.6049%)
                      resolved 637,499 -> 283,768 ; net_win_rate 0.4661575940 -> 0.4870915678
冷却实测              labels.dedupe_signals: 单股票 97 会话 -> 1/2 ; 加入填充股票 -> 2/2
checker 关闭行为      删/坏 ml_final_holdout.json -> exit 1 (590 checks, 5~6 problems)
                      audit_reports.py 常态 -> exit 0, 629 checks, 0 problems, 12 notes
关键行                src/live_readiness.py:72, :87  (合并后从不按 (code,date) 去重)
                      src/labels.py:233, :242       (session_index 由本帧日期构造 => frame-local)
                      scripts/run_fixup.py:180, :183-184, :233-238
                      scripts/scheduled_report_audit.py:84-86, :108, :344, :241-267
                      scripts/audit_reports.py:249-262, :185-211
                      src/ml/walkforward.py:58-61, :108-109
                      src/ml/final_holdout.py:294, :312-321, :251, :254
                      src/ml/search.py:410-433
本轮脚本              D:\ccc\_sched\work\eml_h22_b*.py（含 .log）
```

**无编造。** 所有 SHA 由 `git rev-parse` / `ls-remote` 实测；所有行号由 `git show` 读出；所有统计量由真实运行得到；**未复现者已逐条标注并降级**；**我上一份报告中沿用的 −32.38% 在此明确更正**。
