# 专家／ML 线独立审计补充：以真实 CLI 反例推翻「dedupe fail-closed 恒不可达」的 P1

- 审计时间：2026-09-22 21:35 JST（= 20:35 CST）
- 被审仓库：`fy-god/pro-web-60d-strategy`
- `reviewed_source_sha`: `8163db84e10f52057aab1216e969f2ee121ad54f`
- 本轮开始时 `origin/main`: `92cbb3d479479ba92d128d8cdb9e472331c90dfb`
- 前一份报告：[`2026-09-22_21-05-00_JST.md`](./2026-09-22_21-05-00_JST.md)
- 本轮 `real_market_fit_count` 增量：**0**；无实股证据
- 类型：`EXECUTED_COUNTEREXAMPLE + CLAIM_REFUTATION + LINE_COUNT_CORRECTION`

---

## 0. 本轮结论

仓库内既有的 `EML-P1-H504-DEDUPE-FAILCLOSED-UNREACHABLE`（15:40 轮）断言 `session_clock.py` 的 fail-closed 分支在缺 `--calendar` 时**恒不可达 / 永不可达**（理由是「无 `--calendar` 时时钟只能取 panel 并集，故 `unknown` 恒为空」）。

**本轮以真实 CLI 反例推翻该断言**：`--stage dedupe` 的时钟取自 **panel**，但它校验的是 **signals 文件**；signals 与 panel 相互独立，因此只要 signal 日期不在 panel 日期集合内，分支即触发。

我用真实 CLI、**不带** `--calendar`、**不带** `--evidence-type` 跑四臂：

| 臂 | signal 日期 | 结果 |
|---|---|---|
| C1 是 panel 日期 | `2020-01-03` | **rc=0**，写出 1 行 |
| C2 周六 | `2020-01-04` | **rc=1** `ValueError: signal dates absent from market calendar; first=[Timestamp('2020-01-04 00:00:00')]` |
| C3 早于 panel 区间 | `2019-12-31` | **rc=1** 同一 `ValueError`，`first=[Timestamp('2019-12-31 …')]` |
| C4 晚于 panel 区间 | `2020-05-01` | **rc=1** 同一 `ValueError`，`first=[Timestamp('2020-05-01 …')]` |

⇒ **分支可达**。该 P1 的「不可达」表述**予以推翻**。

## 1. 更正为可辩护的弱表述（缺陷本身仍成立）

被推翻的只是「不可达」。该处真实缺陷应改述为：

- 无 `--calendar` 时时钟**只能**取 panel 并集，因此「signal 日期恰是 panel 日期」的情形**无法**对照独立交易所日历校验 —— 即**校验源不足**（self-report fail-open），而不是「分支死代码」；
- 若真实交易日缺失于 panel，位置会被**静默压缩**（实测 `2020-01-07 → pos 4`、`2020-01-09 → pos 5` 变为相邻，`cooldown=1` 仍保留 3/3 行、rc=0、无异常）。

**因此本项的责任归属**：`'unreachable'` 措辞删除；缺陷保留在原条目 `EML-P1-H504-REAL-CALENDAR-GUARD-SELF-REPORT-FAILOPEN` 下。

## 2. 另一处行数更正

15:40 轮 §6 写 `registry.py（26 行）`。实测为 **25 行**（`LATEST.md:20` 的 11:56 轮写 25 行是对的）。此系文档数字错误，不影响缺陷判定。

## 3. 复核方式

- 四臂均调用真实 CLI `python -m src.ml.research_h504.run --stage dedupe --panel … --signals … --out …`，工作目录为纯净导出树 `D:\eml_h18_tip`（`git diff 8163db84..origin/main -- src tests` = **空**，故行号有效）；
- 未传 `--calendar`、未传 `--evidence-type`，即被 PEER 认定为「必然不可达」的确切配置；
- 每臂独立写出 signal 文件与 panel，互不共享。

## 4. 本轮实际执行的命令 / 退出码

| 命令 | 结果 |
|---|---|
| `--stage dedupe` signal ∈ panel | **rc=0**，1 行 |
| `--stage dedupe` signal = 周六 | **rc=1**，`ValueError: signal dates absent from market calendar` |
| `--stage dedupe` signal < panel 区间 | **rc=1**，同一异常 |
| `--stage dedupe` signal > panel 区间 | **rc=1**，同一异常 |
| `git diff 8163db84..origin/main -- src tests` | **空** |

**未跑及原因**：本仓库**无** `docs/audits/validate_latest.py`，手册第 5 步门禁在本线不适用；真实 H504 fit 未跑（无授权日历）。

## 5. 三轴状态

- `execution_status`：`SOFTWARE_SAMPLE`（真实 CLI 调用 + 合成输入）。
- `research_verdict`：`DEDUPE_UNREACHABLE_CLAIM_REFUTED_BY_REAL_CLI / SELF_REPORT_FAILOPEN_AND_SILENT_POSITION_COMPRESSION_SURVIVE / REGISTRY_LINE_COUNT_25_NOT_26`。
- `evidence_status`：软件样本证据；**实股证据 0 条**；`evidence_type=SOFTWARE_SAMPLE`，不与 `REAL_MARKET_*` 合并计数。

## 6. 分开写三件事

- **程序修复**：无（未在本轮实施）。建议为 `session_clock.py:54-56` 增加「无独立日历」的显式告警，使 self-report fail-open 不再静默。
- **任务定义变更**：无。
- **真实模型增益**：**无**。
