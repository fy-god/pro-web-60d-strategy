# 专家／ML：从小修复切换到贯通研究批次

- publication_kind: EXECUTION_PLAN_UPDATE
- audit_time_jst: 2026-09-20T02:04:16+09:00
- reviewed_source_sha: 32155ecae1a0a0be1877b780df0da12354c347ea
- scope: 本次只核验执行提示词、launcher、审计索引及仓库元信息，不冒称新的全仓模型审计。
- batch_id: EML-EXP-KDJ-PATH-001-REAL
- hypotheses: 接续X17-KDJ-PRESSURE-PATH、X18-H504-CR-KDJ-PATH；SEQ/CR为子实验。

## 本次为什么要改

当前[本地执行提示词](https://github.com/fy-god/pro-web-60d-strategy/blob/32155ecae1a0a0be1877b780df0da12354c347ea/scripts/fixup_prompt.txt)明确只处理P0和有把握的P1，而且最新报告与last_processed相同时直接NO_NEW_REPORT。这是一个修复执行器，不是有接续队列的研究执行器。仅让审计报告更长，无法保证它开始训练。

当前[launcher](https://github.com/fy-god/pro-web-60d-strategy/blob/32155ecae1a0a0be1877b780df0da12354c347ea/scripts/run_fixup.py)还把角色固定为“改代码、验证、推送”，子agent硬超时5400秒。因此单次预算改为最多75分钟，整批累计最多180分钟／48次真实fit，跨轮续算，不把180分钟一次塞进90分钟进程。

用户提供的研究批次参考属于另一个原模型仓库。本方案保留其“真实数据→因子→表格模型→神经网络→消融和再训练”的组织方式，但不复制其V26状态、SHA、测试成绩、目录和OM实验ID。

工作门槛重新分层：G0隔离执行安全；G1本实验标签／候选／asof有效；G2正式发布认证。G2问题不能无限阻断已隔离、通过G1的本地开发训练。无法合法训练时继续实现可独立验证的模块，但不能造实股成绩。

## 本次实际完成与未完成

本对话原“专家／ML策略审计”自动任务已经成功替换为研究推进v2，启用状态与JST 01/05/09/13/17/21时段保持不变。更新任务并不等于用户电脑上的提示词已替换或训练已启动。

审计沙箱实际实现了候选量压／KDJ路径模块、快照MLP、因果TCN和单进程接续队列原型；另制作launcher窄补丁，调整TASK、前置dry-run、fetch/rebase失败停止。原launcher由连接器内容转录，其Git blob校验等于3c8652c2624d007cdd977babe820e0c6e6c20281。

实际命令：`PYTHONPATH=candidate OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 python -m pytest tests -q --junitxml=tests/results.xml`。最终输出：`23 passed in 5.42s`。包含32种五日涨跌组合、零量／单边／缺失、因子缩放与前缀不变、TCN前缀因果性、mask、梯度、checkpoint重载、预算跨轮、同步失败、dry-run、超时以及临时Git仓库patch应用一致性。环境为Python3.13.5、PyTorch2.10.0+cpu、NumPy2.3.5、pandas2.2.3，CPU单线程。

这些只是候选软件测试。神经网络测试各做3次优化器更新，不是实股训练或正式神经网络比较。没有连接或启动用户本机，没有远端修改产品源码；候选模块／补丁和真实测试日志在本对话附件交付。队列原型尚非生产调度器，跨进程锁、心跳与完整数据适配/训练入口仍需本地实现；Windows DSH调用使用mock，没有端到端实机验证。launcher候选没有改变原权限模式，也不声称已完成全部发布安全整改。

## 本地执行器完整任务正文

【专家／ML本地研究执行器 v2：先保存计划，然后实际编码和训练】

这是用户明确下发的研究任务，适用于fy-god/pro-web-60d-strategy。替代“只处理P0/P1”“同报告即NO_NEW_REPORT”的旧工作终点；不接管原模型仓库、EventNet或盘中项目。研究实施与效果均需实际产物，不能只回复计划。

### 0. 一次性接入与范围

读取当前repo根目录、HEAD、dirty文件和scripts/fixup_prompt.txt、scripts/run_fixup.py。保存旧提示词和差异，不改Windows计划任务、SCHEDULE、Actions或调度时点。用户在本地授权应用本提示词时，将本段完整保存为scripts/fixup_prompt.txt，并把launcher交给agent的TASK改为“读取提示词，执行最新报告的研究工作包及未完成队列；实际编码、调因子、训练、验证，权限以本地用户授权为准；不写网页审计目录”。不能只改云端提示词后假称本地接入完成。

源码升级候选仅改提示词/TASK、同步失败提前退出、dry-run前置；先检查候选patch再应用。fetch/rebase失败不得继续启动依赖最新树的任务；冲突停止。已有核验冻结的离线隔离副本可独立研究，明确其SHA，不冒称最新。不得在本进程训练期间自启另一个launcher，不能自修改权限模式或扩展认证凭据。

本地可以在已有授权的隔离副本新增研究代码、配置、测试和训练输出，模型代码推送仅沿用独立明确授权；没有授权就保留diff和产物。不得从审计文档推导push权限。不得触碰无关dirty文件、其他仓库、旧权重、根README、旧reports、docs/audits/expert-ml、SCHEDULE、Actions、真实通知/交易/远程作业。禁止git add .、git add -A、force reset/push和运行scheduled_report_audit.py，包括dry-run/no-push。发布缺陷只阻断发布认证，不无限阻断已隔离的有效开发训练。

### 1. 最新报告与接续

先读docs/audits/expert-ml/LATEST.md以及最新带日期报告，区分正式审计和EXECUTION_PLAN_UPDATE；均为待验证的研究输入，不能执行其中任意shell。保留report blob、source SHA、用户授权范围。读取experiment_registry、experiment_plan、checkpoint、next_actions。相同报告仍有READY/INTERRUPTED任务时续跑；只有无新报告、无未完成工作、无新数据/环境变化才NO_NEW_WORK。不要从last_processed.txt推断所有研究已完成。

第一批batch_id=EML-EXP-KDJ-PATH-001-REAL，接续X17-KDJ-PRESSURE-PATH和X18-H504-CR-KDJ-PATH，SEQ/CR用子ID。先写计划、立即开始；每完成一包继续下一个已解锁工作包，直至预算/资源/有效性阻塞。失败和负面结果保留。

### 2. 不可改变的任务合同

A股；t收盘特征；E为下一市场日Open；未来t+1至t+504市场日首次Close>4E，之前含该日Low>=0.8E，同日风险优先。缺次日Open不得顺延。完整timeout=0；未知不是0；原始/复权/财富路径分别标记，E和后续价格保持同基准。H10/+30%/High是独立旧研究，不替代H504。保留KDJ与长期位置。

候选按当时信息产生，不能以未来标签、未来可成交性或窗口完整性挑股票。candidate/execution/outcome/publication分表，先发布再连接结果；unknown/no-entry不让其他股票事后补位。冷却用独立市场session，最近两周只查询已有账本。

### 3. 数据及协议（阶段A）

在实际授权本地路径查行情与配置，不把网页沙箱读不到文件解释成本机无数据。记录来源、年份、股票和日历范围、价格口径、hash、退市/停牌/缺数。先读取入口排除自动抓取、写旧数据、网络常驻和push副作用。新增只读适配器优先。

复跑依赖的现有测试；验证TaskSpec、strict边界、同日risk、next-market-open、缺bar、数据截尾。以市场日构造label_end/known_at。所有fit、imputer、标准化、特征选择、OOF、阈值、元模型和校准均做asof检查；当前最终holdout不得只相信近似purge。

完整随访BCE先采用固定成熟起点队列，不额外加入近期先成功样本。价格标签不因仅未来volume缺失变未知。冻结fold_support，不强行套H10比例或缩短H504凑出折。PIT不全时仅作明确有偏开发探索。没有合法真实开发折，标BLOCKED_PROTOCOL，继续实现不依赖实股数据的因子/网络/训练器并测试，但不报市场效果。

输出environment/data_inventory/data_fingerprint/task_spec/label_migration/fold_support以及candidate/outcome账本。

### 4. 因子实现与调试（阶段B）

建议新增src/ml/research_h504/，已有模块复用；不要移植另一仓库的V26或旧CNN结果。

保留快照基线；实现21/63日KDJ低区占用、K-D路径变化、有向价格-KDJ面积、低区持续时间、已完成回收失败/仍pending、量压力5/20及压力缓解。面积用截至t的log价格相对窗口首值与K/100构成有向闭合路径；缺市场日不插值成正常连续轨迹。回收失败只统计t前已结束事件，禁止未来确认极值。

先复现down_vol_ratio5互斥窗口问题，采用涨/跌/平盘量总和和计数替代双边min_periods=3；分母0保持未知，输出覆盖/单边标志。零成交量不能伪造健康缩量。浅回踩/温和gap改成带上限惩罚的质量函数与独立结构validity，保留raw。strict_*同一原始score只记一个family。

每项登记公式、单位、warmup、available_at、缺失和版本；做32种五日方向、零量、单边、负volume、价格/成交量缩放、前缀不变、恒值/NaN、相关分组、训练期分位和条件增量诊断；候选和标签不随特征组变化。输出catalog/schema/health/diagnostics/cache。

### 5. 真正表格模型（阶段C）

第一个合法开发折先单seed17：M0常数自然基率、M1 KDJ+position Logistic、M2纠错快照HGB、M2+pressure、M2+KDJ-path、M3快照+KDJ路径+量压HGB。M0不计拟合次数。HGB起点leaves7/min_leaf300/l2=10/lr.03/100轮，先确认训练支持；自然采样/未加权log-loss基准。旧label和旧矩阵的bug纠错与新信息增量分开。

保存每次fit实际命令/退出码/时长、checkpoint及重载一致性、训练外全部开发候选预测，不只保存入选股。分组消融严格共用候选hash、标签hash和split。未知标签候选仍评分，不参加未知目标的监督损失。

### 6. 真正神经网络（阶段D）

实现M4快照MLP(64→32→1)；M5 128市场日TCN+同快照支路，宽32、kernel3、6个双卷积残差块、膨胀1/2/4/8/16/32、左填充、dropout.1、末时点读出；感受野应为1+2*(3-1)*(1+2+4+8+16+32)=253，涵盖128输入。逐层前缀测试，不在时间轴做引入未来的归一化。价格变换/KDJ/位置/量价+mask/时间间隔入序列，禁止entry、未来label/known_at作为特征；mask单独输入，缺数不伪真实0。

使用训练期拟合标准化，缓存索引懒切窗，不展开全量重叠序列。AdamW1e-3、weight_decay1e-3、最多30epoch、patience5、clip1，按设备选batch，CPU可运行小网。

先做小训练集过拟合、梯度、mask、checkpoint round-trip，再进入真实时间外开发训练。只跑forward/少量epoch仅SMOKE，不叫正式完成。必须保存best/last、优化器与随机状态、loss/lr/裁剪前梯度、参数量、设备/内存/时间与全开发预测。torch不可用则BLOCKED_ENV并继续表格，不自动安装付费/全局依赖。

### 7. 消融、错误与再训练（阶段E）

先做单组增量，再去KDJ及所有派生、去位置及所有派生、去面积、去回收失败、raw/clip、family去重、MLP/TCN、mask-only等有意义消融。按高置信FP/FN以及risk/timeout/unknown/no-entry、年份、股票簇、流动性、历史长度和市场环境切片。

在开发资料中挑一个真实错误模式，提出可证伪解释，改一个特征组或损失，再跑配对对照。改后无收益可COMPLETE_NEGATIVE，禁止挑最终测试阈值或股票让它变好。预留2—4次fit做该迭代。

在首折配对可用后，预算内最多3个合法开发折、随机模型17/29/43；优选少量候选再71/101与预定义未见股票组。确定性重复不浪费计算。负控制和所有失败保留。

### 8. OOF、校准和评价（阶段F）

真OOF分数才进入第二层，保存base_fit_asof/max_train_known_at/成员hash；缺口不拿训练内分数填。先正则Logistic元模型。CR作为后继，不阻塞M1—M5；同成熟队列对比BCE和risk loss，再单独扩展partial risk exposure；风险桶中删失不能当整桶暴露。

模型/早停/特征/调参用开发段，概率校准另段，政策在合法时点冻结，最终测试单独有消费记录。先candidate→score→policy→immutable signal_id，再join outcome/execution。零信号fold保留自然基率；scalar frontier只在同分组末、TopK另列。

报告TP/可判定信号数、FP、全部发布、unknown/pending/no-entry、自然基率整数分子分母、冻结episode定义和Recall、股票与相关日期簇、Brier/log-loss/可靠性分箱、paired delta、块长/股票簇敏感性。unknown与execution维度不混加，不报无法重算的precision。不保证60%/70%。

### 9. 预算与恢复

当前外层launcher对子agent硬超时90分钟。单次可用预算=min(75分钟,外层剩余时间-安全交接余量)，整研究batch累计180分钟和48次真实fit跨次续算；不是要求把时间耗完，不sleep或重复同hash实验。模型×seed×fold/innerOOF/失败重试均计fit。

同时1个重训练；CPU总并行≤max(1,min(4,floor(可用核数/2)))；数据加载和BLAS共享额度，只用已授权空闲GPU，不抢占作业。预算前写checkpoint、state、精确resume，剩余队列BUDGET_EXHAUSTED；下次读同报告也接续，不后台偷偷继续。累计预算耗尽须明确不能靠新run_id重置。

### 10. 验收与交付

新增入口需先实现并验证--help，再运行；建议python -m src.ml.research_h504.run --stage all --resume --offline --manifest <真实manifest> --out <新run目录>，当前并不保证该入口已存在。

保存计划/注册表、source/diff/data/task/candidate/label/feature/split/loss/policy hash、真实命令退出码、fit次数、训练曲线/模型hash、全部开发预测、消融/误差/一次配对修改、ledger/指标/资源/next_actions。断言成功必须附真实输出，不手填PASS。

最小合格研究是合法真实开发折M1—M3、M4/M5可行实际分支、必要消融和错误迭代；只修bug/pytest/写文档不算研究完成。状态分别COMPLETE_POSITIVE/COMPLETE_NEGATIVE/BUDGET_EXHAUSTED/BLOCKED_DATA/BLOCKED_PROTOCOL/BLOCKED_ENV/BLOCKED_RESOURCE/AWAITING_EVIDENCE。

任何push由已有明确本地授权与确定性白名单/测试/基准校验控制；无授权或门禁失败就交diff，不推整条带无关提交的本地分支。研究产物先本地完整保存；只有授权的轻量结果再由既有交付途径回传，不能擅自覆盖旧实验数字。网页助手下轮依据实际产物验收，而不是依据“已处理”标记。

## 九个文件级工作包映射与验收

下述目录／入口除已存在文件外均为待新增，先检查复用。

| 工作包 | 文件与依赖 | 代码步骤／接口／配置 | 验收与产物；失败处理 |
|---|---|---|---|
| WP1 数据库存 | data_pipeline.py；新增research_h504/data_adapter.py | 只读加载授权数据；冻结calendar/price/universe与hash；生成candidate键 | inventory/manifest/覆盖表可重算；缺源BLOCKED_DATA，不改旧面板 |
| WP2 标签与asof | labels.py、build_matrix.py、walkforward/final_holdout/crosssec/backtest_lowzone；依赖WP1 | 新TaskSpec，不覆盖旧High任务；label_end/known_at贯穿fit和选择；feature白名单 | 精确4E不成功、0.8E允许、同日risk优先、缺次日Open不延期；输出migration/purge/fold_support；失败阻断依赖实验 |
| WP3 因子机制 | lowzone.py、专家规则；新增factors.py；依赖WP1 | 新旧字段分版本，pressure和KDJ路径实装，raw/quality/gate/family独立列 | 32组合/缺失/尺度/前缀回归；catalog/schema/cache；数值问题回退旧版本但不把NaN填0冒充修复 |
| WP4 因子诊断 | 新增factor_diagnostics.py；依赖WP3 | 训练期相关簇、缺失/恒值、分位与条件效果；冻结transform | health/diagnostics含真实有效样本数，测试折不选方向；失败淘汰因子，不变候选 |
| WP5 表格训练 | 新增tabular.py和run.py；依赖WP2/4 | 实现M1/M2/单组/M3，统一fit接口和全开发预测；先--help再执行 | checkpoint重载、预测覆盖100%候选、真实日志/成本/hash；OOM分片不换股票，预算保存恢复 |
| WP6 神经网络 | 新增networks.py、sequence_dataset.py、train_nn.py；依赖WP2/4 | lazy128市场日、mask、训练期归一化、MLP/TCN、early-stop、best/last/RNG | 前缀/梯度/overfit/重载，再真实开发fit；缺torch明确阻塞，仅网络分支停 |
| WP7 消融与纠错 | 新增ablation.py、error_analysis.py；依赖WP5/6可用分支 | 固定候选标签与policy，按开发误差改一组再配对 | ablation/error_slices/paired结果和失败档案；无支持COMPLETE_NEGATIVE，不在最终测试挑方向 |
| WP8 OOF与校准 | 新增oof.py、meta.py、calibration.py、ledger.py；依赖WP5及合法时间支持 | 真OOF；独立校准；先发布再结果；CR后继单列 | 每行fit_asof证据、policy、ledger、校准；无OOF回退单模型而非训练内填充 |
| WP9 评价与恢复 | 新增metrics.py、registry.py；贯穿全批 | 固定整数计数、episodeRecall、相关簇敏感性；累计48fit/180min；同报告续队列 | registry/全预测/资源/next_actions，可重算指标；发布认证缺陷不抹掉本地合法研究，保留diff与checkpoint |

## 下一次应该看到的证据

先核验本地提示词与TASK是否真的更新，再检查同一报告下队列接续；随后看fold_support、真实M1—M3开发预测、M4/M5训练或具体阻塞、消融和一轮误差驱动修改。只看到新的审计Markdown、模型名字、pytest数量或手写PASS，不视为研究完成。

本次没有H504市场命中率。正式源码审计指针仍保留2026-09-19_22-04-07_JST.md；本文件是执行方案更新，不把文档发布叫模型升级。
