# 专家／ML 独立审计 · 2026-09-21 15:48 JST

- 审计线：专家／ML（`D:\ccc\pro-web-60d-strategy`）
- 被审提交：`5d1b365cd6c2a80a74b18fbf12e47e766203eb93`（`main`，`HEAD == origin/main`）
- 上一份专家／ML 报告：`2026-09-21_14-06-49_JST.md`（`reviewed_source_sha` = `74430df12f60b6b30b06baa02111feb0d4d0bcee`）
- 本轮待审增量：`74430df..5d1b365` 共 **2 次提交，全部 docs-only**
- `evidence_type`：`PROVENANCE_AND_RECONCILIATION` + 源码审计（**不含**任何真实市场拟合；`real_market_fit_count = 0`）
- 证据可得性：本机仓库工作树 + 真实 `data/panel_daily.parquet` + 真实 `outputs/lowzone_signals.csv` + 实时 `schtasks` 只读查询。所有数字本轮实测。
- 未触碰：`scripts/register_fixup_task.ps1`（他人 `M`）、`.gitattributes`（他人 `??`）。

---

## §0 本轮最重要的自我更正：我的两个"发现"其实早已发表

我在初稿里把"entry/horizon 按行计数"和"cooldown 用帧秩"当作本轮新发现。**这是错的。** 复核后：

| 我初稿的说法 | 事实 | 处置 |
|---|---|---|
| §2 entry/horizon 行空间"从未被任何一轮审计报出" | **错**。`2026-09-21_03-50-40_JST.md:42` 第 1 节标题即"【已确认错误 · P0】`low504` 标签在真实停牌面板上**窗口错位**"，并给出 12.2237% / 最长 590 / 停牌股 002656 反例 | 降级为**回归确认**，只保留我**新增**的量化（见下） |
| §3 cooldown 帧秩"从未被报出" | **错**。同报告 `:116` 第 2 节即该缺陷，`LATEST.md:43` 亦已记录 | 降级为**回归确认**，并**更正方向表述** |

`git grep` 证据：

```
$ git grep -l -E "12\.2237|135,108|span>504" -- docs/audits/expert-ml/
  docs/audits/expert-ml/2026-09-21_03-50-40_JST.md
  docs/audits/expert-ml/2026-09-21_06-03-05_JST.md
  docs/audits/expert-ml/LATEST.md
```

这正是手册警戒的"为了每次有新发现而编造错误"。**我收回这两条的"新发现"定位。**

**本轮真正新的东西**（经 `git grep` 确认从未出现在任何审计报告中）：

| 编号 | 内容 | 新意证据 |
|---|---|---|
| §1 | `ProWeb60d-Fixup` 计划任务**根本没有注册**，fixup 链自 2026-09-20 01:15 已死 | `git grep -rn "未注册\|not registered" -- docs/ scripts/` → **0 命中** |
| §6 | 报告声称的 `33 passed` 在本仓库**不存在**对应测试集（全部可收集 = 15） | 全部报告的 test-count 从未与 `--collect-only` 对账 |
| §7 | 候选无界增殖的**结构性成因**（两侧契约把"能写结论的"与"能改代码的"切反了） | 无报告做过该契约对偶分析 |
| §9 | `output_86_real` 是过期标签，实为 3,193 只主板股票 | `git grep "output_86_real..." -- docs/` → **0 命中** |

---

## §1 【已确认错误 · P0 · 任务定义变更 · 本轮新发现】自动修复链已死：`ProWeb60d-Fixup` 从未注册

**文件／行**

- `scripts/run_fixup.task.xml:16` `<ExecutionTimeLimit>PT2H30M</ExecutionTimeLimit>`，`:29 <Interval>PT4H</Interval>`
- `scripts/register_fixup_task.ps1:102-104`（注释 + `$timeLimit = "PT2H30M"`）、`:123`（插值）
- `scripts/run_fixup.py:237 timeout=5400`、`:239 except subprocess.TimeoutExpired`、`:240`（写 `TIMEOUT:`）
- `logs/fixup/last_processed.txt`；`logs/fixup/fixup_*.log`

**触发／反例（本轮实时只读实测）**

```
$ schtasks /query /tn "ProWeb60d-Fixup"
ERROR: The system cannot find the file specified.
  exit = 1

$ schtasks /query /fo csv /nh     # 全表
  注册的 ProWeb60d* 任务 = ['ProWeb60d-ReportAudit']       # 仅此一个

$ Get-Content logs\fixup\last_processed.txt
  2026-09-19_04-05-29_JST.md        mtime = 2026-09-19 14:27:50

$ 最新 fixup 日志 = logs\fixup\fixup_2026-09-20_011545.log
  末行 = "dry run: not starting the agent"
```

对照组（确实存在的那个）：

```
TaskName:      \ProWeb60d-ReportAudit
Next Run Time: 2026/9/21 15:15:00
Last Run Time: 2026/9/21 11:15:01
Last Result:   0
Task To Run:   ...\scripts\run_report_audit.cmd
```

**机制**

这条链是两半：`ProWeb60d-ReportAudit` 每 4 小时跑一致性审计并提交 `reports/AUDIT_STATUS.md`；`ProWeb60d-Fixup` 每 4 小时跑 `run_fixup.py`，读 `docs/audits/expert-ml/` 的**最新网页审计报告**，按其中 P0/P1 改代码并推送。

**前一半活着，后一半不存在。** `register_fixup_task.ps1` 是一个**从未被执行的注册脚本**；`run_fixup.task.xml` 是一份**从未被注册的模板**。上一轮报告 `2026-09-21_03-50-40_JST.md:372` 说 `PT2H30M` "未生效"——结论碰巧正确，但 `:357` 只核对了**模板文件**，从未查过**实际注册状态**，因此推理链是错的（对错误的原因说了对的话）。

**影响（量化）**

- `last_processed.txt` 停在 `2026-09-19_04-05-29_JST.md`；最新 fixup 日志是 2026-09-20 01:15:45，且为 dry run。
- 自最后源码提交 `1dd9732`（2026-09-19 01:58:38）起：**74 次提交 / 26 份审计报告 / 0 次 fixup**。
- 结论：**26 轮专家／ML 审计写下的任何 P0/P1，一条都没有被自动消费过。** 这直接解释"审计越写越多、`src/` 一行不动"——不是审计质量不够，而是**执行端最后一环从未接线**。
- 叠加 §4：`AUDIT_STATUS.md` 的 `PASS` 也**没有任何机器消费者**。两端皆惰性：**无人对 PASS 行动，也无人对 findings 行动。**

**修复建议（属任务定义变更，我无权执行）**

我受硬约束禁止创建／修改任何计划任务（`schtasks` 写操作、`Register-ScheduledTask`、`schedule_*` 一律禁止），故只上报事实与两个可选处置：

- **(a) 接线**：由人类执行仓库自带的 `scripts\register_fixup_task.ps1`。
- **(b) 明确放弃**：若不打算再接，应把 `SCHEDULE.md`／`LATEST.md` 中"本机执行链"表述改为"该链未启用"，并停止以"下一轮核验 receipt"作为推进形式（否则每轮重复一份无人消费的文档，见 §7）。

**验收测试**

```powershell
schtasks /query /tn "ProWeb60d-Fixup" /fo LIST | Select-String 'TaskName|Execution Time Limit'
# 必须 exit 0 且命中；
Get-Content D:\ccc\pro-web-60d-strategy\logs\fixup\last_processed.txt
# 下次运行后必须前进
```

---

## §2 【已确认错误 · P0 · 程序修复 · 回归确认（非本轮新发现）】地平线按"行"而非交易日计数

> **状态更正**：本条**已由 `2026-09-21_03-50-40_JST.md:42` 第 1 节报出**（12.2237%、最长 590、停牌股 002656 反例），并在 `LATEST.md:42` 记录。我初稿误标为新发现，此处更正。以下只保留我**新增**的量。

**文件／行**：`src/labels.py:123-128`（`forward_outcomes`）、`:150-161`、`:176`、`:196`；`src/ml/build_matrix.py:389-400`

```python
123:    # Entry price: the next session's open, but only within the same stock.
125:    entry[:-1] = opens[1:]
126:    same_next = np.zeros(n, dtype=bool)
127:    same_next[:-1] = codes[1:] == codes[:-1]
128:    entry[~same_next] = np.nan
161:    for d in range(1, horizon + 1):        # 行偏移
196:    resolved = (future_bars >= horizon) & np.isfinite(entry)
```

**我新增的量化（与既有报告的差异已说明）**

既有报告的输入面板来自两份源 CSV（**2,677,525 行 / 886 session**），给出 `H=504 span>504 = 135,108 (12.2237%)`。我改用**当前仓库 parquet**（**2,680,715 行 / 887 session**）重算，并**首次给出全部制度的横向扫描**：

```
输入：data/panel_daily.parquet — 2,680,715 行 / 3,193 codes / 887 sessions
同 code 相邻行对                  = 2,677,522
其中下一行不是下一 session 的       = 943   (0.0352%)
相邻行之间最大 session 间隔         = 46

horizon 按行计数 → 窗口真实跨越的 session 数
   H     成熟行数       跨>H 的行      占比        最大跨度  超出
  10   2,648,785          9,219    0.3480%           55      45
  20   2,616,857         18,132    0.6929%           68      48
  60   2,489,247         51,357    2.0632%          108      48
 120   2,298,224         88,351    3.8443%          169      49
 250   1,886,160        134,370    7.1240%          300      50
 504   1,108,219        135,544   12.2308%          590      86
```

- **新增价值 1**：既有报告只给了 `low504`(H504) 与 `low60`(H60)。我补出 **H10 = 0.3480%（9,219 / 2,648,785）**，这是**产品实际矩阵用的 horizon**（`matrix_h10_t30_s1_meta.json` 实测 `horizon=10, target=0.3, features=82`），因此是把该缺陷连到**已发布数字**的关键一环。
- **新增价值 2**：数字与既有报告**同向、同量级**，但绝对数不同（943 vs 941；135,544 vs 135,108），原因是输入面板不同（887 vs 886 session，parquet vs 源 CSV）。这**不是矛盾**，但既有报告的绝对数**不应被当作 parquet 上的现值和引用**。
- 机制、方向（窗口被单向**拉长**）、影响面（集中在有停牌的股票）与既有报告一致，我不重复。

**修复建议**：见既有报告 §1；我补充一点：`build_matrix.py:389-400` 与 `labels.py` 是**两条独立实现同一语义**的路径，修复必须同时覆盖，否则矩阵层与标签层会再次分叉。

**验收测试**：`max(span - H) == 0`；停牌构造下 `entry_open` 必须为 NaN 或显式 `no_entry`；上述六行扫描全部归零。

---

## §3 【已确认错误 · P1 · 程序修复 · 回归确认 + 方向更正】cooldown 用"信号帧秩"当交易日时钟

> **状态更正**：本条**已由 `2026-09-21_03-50-40_JST.md:116` 第 2 节报出**（标题"冷却时钟不是交易日历，**过度抑制**"），`LATEST.md:43` 亦已记录。我初稿误标为新发现，且**方向表述写反了**，此处更正。

**文件／行**：`src/labels.py:222-246`，关键 `:233-234`

```python
226:    holidays cannot silently shorten a cooldown.     # 注释声称按交易日
233:    session_index = {d: i for i, d in enumerate(sorted(out["date"].unique()))}
234:    positions = out["date"].map(session_index).to_numpy()
242:                if last is None or (positions[j] - last) >= cooldown:
```

**我初稿的方向错误（必须更正）**

我写的是"cooldown 过度宽松 / over-permissive 0.66%"。**实测净效应相反**：

```
真实 outputs/lowzone_signals.csv（1,409,536 行 / 634 distinct date / 覆盖 71.48% session）
  帧时钟保留 = 21,193
  真时钟保留 = 21,198
  净差       = -5          → 帧时钟保留更少 = 过度抑制（OVER-SUPPRESSION）

  帧保留而真应剔除（帧过宽）= 139
  真保留而帧剔除（帧过严）  = 144
  => 净方向 = 过度抑制，与 2026-09-21_03-50-40_JST.md 的表述一致，与我的初稿相反

机制核验：
  同 code 相邻对 = 1,406,350
  帧秩差 <= 真 session 差 : 100.00% 的对
  帧秩差 <  真 session 差 :   0.21% 的对
  → 帧秩系统性偏小 ⇒ `>= cooldown` 更难满足 ⇒ 过度抑制。方向确认为"过严"。
```

另据同批独立复核子代理在**真实生产调用点** `src/backtest_lowzone.py:206`（`cooldown=horizon`）上测得：**327 / 273,634 = 0.12%** 的过度抑制，而非既有报告"24→3"的合成稀疏夹具量级。子代理同时声明其信号 CSV 是**陈旧产物**（早于 `1dd9732` 的标签修复），故 0.12% 是**dedupe 步骤上的代理值，非调用点精确值**——我照录该限制。

**判定**：机制是真实的规格违背（`:226` 的 docstring 说按交易日，实现按帧秩），**仍 OPEN**；但影响量级应**从 P0 降级**，且既有报告"24→3"的合成量级**不代表生产**。**建议在有人投入修复前先重新界定量级。**

**修复建议**：`dedupe_signals` 接受市场日历；拿不到日历应**拒绝运行**而非静默退化为帧秩。

**验收测试**：帧只含 session {0, 61}，cooldown=60 → 真日历距离 61 ≥ 60 保留 2 条；帧秩距离 1 只保留 1 条。断言修复后保留 2 条，且未提供日历时抛错。

---

## §4 【已确认错误 · P1 · 程序修复 · 回归确认】`PASS` 不覆盖 drift，且 `git fetch` 失败也照样 `PASS`

> 本条已在 `LATEST.md:12`、`:29` 与多份报告记录；本轮**双重独立复现**，状态 STILL_OPEN。

**文件／行**：`scripts/scheduled_report_audit.py:80-94`、`:108-109`、`:141`、`:158-161`、`:344`

```python
 84:    code, out = run(["git", "fetch", "origin", "main", "--quiet"], timeout=300)
 85:    if code != 0:
 86:        return [], f"git fetch failed (exit {code}): {out.strip()[:300]}"
108:    ok = code == 0 and not unstable
109:    verdict = "PASS" if ok else "ATTENTION"
```

verdict 的输入只有 `code` 与 `unstable`。`changed`／`note` **只用于显示**（`:141`、`:159`），从不进入 `:108`，也不进入 `:344 needs_attention`。因此：

1. 工作树已与 `origin/main` 分叉仍 publish `PASS`。现网 `reports/AUDIT_STATUS.md` 同时写着 `| Verdict | **PASS** |` 与 `| Remote drift | 2 path(s) differ from origin/main |`。
2. `:84-86` fetch 失败时 `changed` 退化为 `[]` → `:108` 仍真 → **`PASS`**，失败只作为表格文字存活。历史先例：提交 `32155ec` 内含 `Verdict **PASS**` 与 `git fetch failed (exit 1)`。

**独立复现**：子代理直接调用**真实的** `write_status`（导入仓库副本，`STATUS` 重定向到 scratch）：fetch-fail note → 输出 `| Verdict | **PASS** |` 且 `needs_attention=False`；对照组 `code=1` → ATTENTION，纯 drift → PASS。

**影响（blast radius 实测）**：`reports/AUDIT_STATUS.md` 唯一写入点是 `:202`；**无任何 `.py` 读取它**（`audit_reports.py:3380`、`run_fixup.py:182` 仅是注释）；`tests/` 命中 0；`.github/` **不存在**，无 CI。读者是人类与 29 份审计报告 ⇒ 这是**可信度**缺陷：下游把"树已同步"当既成事实。与 §1 叠加后两者共同构成本项目的**双重惰性**。

**修复建议**：`ok = code == 0 and not unstable and not changed and "failed" not in note`；并在 `:129-144` 落 `| Audited commit | <sha> |` 与 `| Working tree clean | ... |`。

**验收测试**：`changed=["x"], code=0, unstable=[]` → 断言 `verdict != "PASS"`；`note="git fetch failed (exit 1)"` → 断言 `verdict != "PASS"`。

---

## §5 【待验证风险 · P1 · 程序修复 · 回归确认（今日未触发）】`META_COLUMNS` 黑名单式特征选择，无白名单兜底

> 已在 `2026-09-20_23-50-06_JST.md:158`、`2026-09-21_03-50-40_JST.md:180` 等记录。本轮我补的是**"今天恰好安全"的实测边界**。

**文件／行**：`src/ml/walkforward.py:58-61`、`:108-109`；**手工重复的第二份拷贝** `src/ml/build_matrix.py:520-524`

```python
 58: META_COLUMNS = {
 59:     "code", "date", "entry_open", "fwd_max_high", "fwd_min_low",
 60:     "fwd_max_close", "label_high", "label_close", "resolved",
 61: }
109:     return [c for c in frame.columns if c not in META_COLUMNS]
300:     if not groups: return list(columns)          # 空分组 = 放行全部
```

**用仓库自己的函数实测**

```
labels.forward_outcomes(...) 真实产出 17 列
feature_columns(该输出) → 14 列，全部不在 META_COLUMNS：
    open, high, low, close, volume, amount,
    future_bars, bars_to_target, forward_max_return, forward_min_return,
    label_bull, label_strict_low, label_joint, label_resolved
含未来列的构造帧（29 列）→ 15 列泄漏（含 known_at, deadline, outcome, terminal）
仓库内无白名单：git grep -E "FeatureSpec|WHITELIST|ALLOWED_FEATURES|allowlist|ALLOWED_COLUMNS" -- src/
    → exit 1，0 命中
```

**必须同时说清的反面（我实测确认，避免被误读为已发生泄漏）**

```
outputs/ml/matrix_h10_t30_s1.parquet  →  columns = 91
非 META_COLUMNS 的                     →  82        (9 + 82 = 91，无多余列)
meta['feature_names'] 声明             →  82
声明特征命中 META_COLUMNS              →  0
声明特征名字含 label/fwd/forward/resolved →  0
```

`build_matrix.build()` 是**显式**逐列赋值（`:346-363`），今天列集合恰好 9+82。故这是**潜在陷阱**，不是当前泄漏。**状态维持 STILL_OPEN（latent）**。

**机制**：默认允许。任何新增结果/时钟列（`known_at`/`deadline`）或 `forward_outcomes` 未来新增列，只要名字不在那 9 个里就自动进 X，**且不会有任何测试失败**。`FEATURE_GROUPS`（82 名）也不被 `feature_columns()` 使用；`build_matrix.py:520-524` 是第二份需手工同步的拷贝。

**修复建议**：改为白名单 + 语义禁止（`FeatureSpec`），未知列**默认拒绝并报错**；消灭 `build_matrix.py:520-524` 的第二份拷贝，共用同一真相源。

**验收测试**：真实矩阵加一列 `known_at`，断言 `feature_columns()` **抛异常**；91→92 列时断言 `len(feature_names) == 91`。

---

## §6 【未复现 / 待验证风险 · 本轮新发现】报告的 `33 passed` 在本仓库不存在对应测试集

`2026-09-21_14-06-49_JST.md` 声称 v4 有 `33 passed in 5.02s`、v3 有 `29 passed in 2.45s`，并给 pytest 日志 SHA-256 `041bece8...`。本机实测：

```
$ pytest -o addopts="" -p no:cacheprovider --rootdir=<repo> --collect-only -q <repo>
  15 tests collected
$ pytest -o addopts="" -p no:cacheprovider --rootdir=<repo> -q <repo>\tests
  15 passed in 5.81s        exit = 0
$ pytest --collect-only -q <repo>\tests <repo>\scripts\scratch
  15 tests collected        # scripts/scratch 下 3 个 *_test.py 不被收集
```

- 仓库**全部**可收集测试 = **15**；唯一测试根 `tests/`（`tests/__init__.py`、`tests/test_engine.py`）。
- `pyproject.toml` / `pytest.ini` / `setup.cfg` / `tox.ini` **全部不存在**。
- tracked `*.zip`=0、`*.patch`=0、`*.pt`=0、`*.pth`=0、`*.jsonl`=0、`*.receipt`=0；`src/ml/research_h504/` **不存在**；全盘（含 ignore）`*.zip/*.patch/*.pt/*.pth` = **0**。
- 4 个候选 SHA-256 只出现在 **2 个 `.md` 的散文**里（`2026-09-21_14-06-49_JST.md`、`LATEST.md`），仓库无对应文件。
- 报告自承候选仅在对话附件交付（`LATEST.md:18`、`2026-09-21_14-06-49_JST.md:144`）。

**判定**：**不指控伪造**——候选可能存在于审计沙箱。但**在本仓库范围内**，`33 passed`、四个哈希、v3→v4 patch 一律记 **`未复现`**，不得作为本项目已验收证据。手册要求"未证实的标为待验证风险"；反向亦然：**不得把未复现当作已通过**。

---

## §7 【已确认错误 · P1 · 任务定义变更 · 本轮新发现】候选无界增殖的结构性成因

**文件／行**

- `scripts/fixup_prompt.txt:11`：**绝对不要**自己写审计报告、不要往 `docs/audits/expert-ml/` 里写任何东西。那个目录由 GPT Pro 网页版负责产出。
- `scripts/fixup_prompt.txt:27`：若最新报告 == `last_processed.txt` → 直接结束并输出 `NO_NEW_REPORT`。
- `docs/audits/expert-ml/SCHEDULE.md:20`：只写本审计目录，不更改现有策略、模型参数、权重、结果文件、自动审计脚本、PR 或 Actions，不自动交易或启动远程训练。

**机制（契约对偶分析）**

两条契约把角色切成两半，而且**切反了位置**：

- **能写结论的**（网页审计者，`SCHEDULE.md:20`）**不能改代码**；
- **能改代码的**（fixup agent，`fixup_prompt.txt:11`）**不能写结论**，且按 §1 **它根本没被调度**。

于是每 4 小时一轮的唯一可交付物就是**再写一份文档**。`SCHEDULE.md:24,30` 又把这份文档推向"1—3 项有依据的新思路 / 新实验"，即**一份新产物**，而非"把上一份没落地的产物落地"。结果候选代号 v1→v2→v3→v4 持续增殖，`real_market_fit_count` 一直是 0。

**实测规模**

| 项 | 值 |
|---|---|
| 候选代次（`git log` candidate/候选 提交） | 8 次，代号 `ceaa994`(v1) / `ffa0bd1`(v2) / `2dbd67e`(v3) / `844ea93`(v4) |
| 其中在仓库落地的代码 | **0** |
| 专家／ML 报告文件 | **29** 份 / **734,335 B** |
| 自最后源码提交 `1dd9732` 后的提交 | **74**（触碰 `src/` **0** 次） |
| 自那以后的报告 | **26** |
| `real_market_fit_count = 0` | 4 个文件 |

`2026-09-21_14-06-49_JST.md:182` 自己写出了正确解法，然后 `:186` 立刻又提出下一代候选。**契约在奖励"新产物"、惩罚"原地收敛"。**

**影响**：这是 §1 之外的第二重惰性。**即使把 fixup 接上**，只要契约仍要求每轮新产物，候选仍会增殖。产出/推进比目前 **26 : 0**。

**修复建议（任务定义变更）**

1. **先落地再增殖**：上一份候选未在仓库出现对应产物（receipt/registry/checkpoint）前，不得提出下一代候选。只改 `SCHEDULE.md`。
2. **让候选可验证**：候选产物必须提交到仓库**非 `docs/`** 路径（哪怕只是清单+哈希），否则其数值自动记 `未复现`（与 §6 一致）。
3. **打通读回路**：见 §1。

---

## §8 【回归确认 · 更正我自己】"887 session 不足以支撑 H504 时间外折"是**正确结论**

本轮我一度怀疑这条长期结论是误判。**验算后否证了我的怀疑**，如实记录：

```
N = 887, H = 504, warmup = 60
最早训练起点 index = 59                 (需 60 根 warmup)
该训练标签成熟于   = 59 + 504 = 563
最早开发起点 index = 564
最后可完整评价 H504 的起点 = 886 - 504 = 382
564 > 382  →  无交集，差 182 个 session
容纳"一行训练 + 一行完整开发"至少需要 1,069 个 session
```

故 `2026-09-20_03-41-46_JST.md:100`、`2026-09-20_06-02-02_JST.md:183` 成立，**我此前的怀疑不成立**。

同时确认另一半（**新增**量化）：887 个 session **确实**提供大量成熟起点 —— **1,108,219** 行具备完整 504 行后续，**2,930 / 3,193** 只股票 session 数 ≥ 505。两件事不矛盾：

- 阻塞是**缺少候选清单／实验注册表**（`git ls-files` 中 `research_h504` / `receipts` / `experiment_registry` / `local_start_receipt` / `execution_receipt` 全部 **0 路径**），
- **不是** session 数不够（不够的是"严格 train→dev 时间外折"，另一件事）。

**判定：回归确认（PASS），非缺陷。** 写成缺陷会是指控错误。行动项仍是 §7。

---

## §9 【待验证风险 · provenance · 本轮新发现】`output_86_real` 是过期标签，实为 3,193 只主板股票

`data/panel_manifest.json` 的 `source_root` 写作 `...\output_86_real\mainboard_tencent_daily_*`。实测：

```
data/panel_daily.parquet : 2,680,715 行 / 3,193 codes / 887 sessions / 2023-01-03..2026-08-31
data/panel_manifest.json : codes = 3193, rows = 2680715, duplicate_rows_dropped = 0
前缀普查（按行）: 002xxx 813,560 / 600xxx 661,022 / 603xxx 476,031 / 000xxx 364,890
落在 600/601/603/605/000/001/002/003 的比例 = 100.00%；非主板代码 = 0
```

`data/*.parquet` 被 `.gitignore:2` 忽略，`data/panel_daily.parquet` **未跟踪**；`data/` 下被跟踪的只有 `data/cards_100/{cards,outcomes,scores}.json`。

**判定**：`86` 是旧目录名残留，**不是**"86 只股票"。下游若据此读样本量会把 3,193 误读为 86。属命名 provenance 风险，建议 manifest 增 `universe_label_stale: true`。本轮不改数据、不改 manifest（不在授权内）。

---

## §10 开放项回归汇总

| 开放项 | 本轮实测 | 状态 |
|---|---|---|
| `EML-P0-FIXUP-SYNC-FAILOPEN` | `ProWeb60d-Fixup` **未注册** → 比"fail-open"更严重：整链不存在 | **升级为 §1（新）** |
| `EML-P0-ARR-ARTIFACTS-ABSENT` | tracked zip/patch/pt/jsonl/receipt = 0；`research_h504/` 不存在；3216 objects 扫描 0 命中 | **STILL_OPEN** |
| `EML-P0-AUDIT-FETCH-FAIL-PASS` | `:108` 只看 `code`/`unstable`；子代理用**真实** `write_status` 复现 PASS | **STILL_OPEN** |
| `EML-P1-AUDIT-DRIFT-PASS` | 同上 | **STILL_OPEN** |
| `EML-P0-ENTRY-ROW-ADJACENCY-ON-REAL-PANEL` | `:126-128` 仍只有 code 守卫；我补 H10=0.3480% | **STILL_OPEN（非新）** |
| `EML-P0-COOLDOWN-INDEPENDENT-CLOCK` | `:233` 仍用帧内 `unique()`；净 −5，**方向=过度抑制** | **STILL_OPEN，建议降级量级** |
| `EML-P1-META-BLACKLIST-H10-NAMES` | `:58-61` 仍 9 名；`:109` 仍黑名单；无白名单 | **STILL_OPEN（latent）** |
| `EML-P1-RSI-META-LEAK` | `git grep -i rsi -- src/ml/` = 0；**无 stacking/meta 层** | **NOT_REPRODUCED（按名）→ 并入上一行** |
| `EML-P1-POOLED-NO-DEDUP` | `git grep dedupe\|duplicated -- src/ml/` = 0 命中 | **STILL_OPEN** |
| `EML-P2-TEST-KDJ-NESTED` | `def test_kdj_continuous_across_year_boundary` **已被 `6125ac4` 删除**，函数体留在 Wilson 测试内（`tests/test_engine.py:304-319`），断言**仍活但无 test id** | **已修复/描述更正**（既有报告 `2026-09-20_23-50-06_JST.md:465` 已提修复建议） |
| `EML-P0-AUDIT-STALE-ATTEST` | `LATEST.md:8` 绑 `74430df`，HEAD 已 `5d1b365` | **STILL_OPEN（轻）** |

回归汇总：**FIXED 1 / STILL_OPEN 8 / NOT_REPRODUCED 1 / 描述更正 1**。

---

## §11 有依据的研究思路（1—3 项）

### 思路 A：把"审计是否被消费"变成可核对的数（直接消灭 §1）
- **证据**：`schtasks` 实测任务不存在；`last_processed.txt` 停在 09-19；26 份报告 0 消费；`local_start_receipt` 被 **4** 份报告重复要求。
- **新意**：不再靠每轮文档里写"下一轮核验 receipt"，而是让 `run_fixup.py` 每轮写一份**被 git 跟踪**的 `logs/fixup/receipts.jsonl`：`(report_name, report_sha, commit_before, commit_after, fixed_ids[])`。审计报告只需读它即可判定上轮 findings 是否落地。
- **数据需求**：无。**前提**：fixup 必须真被接线（§1）。
- **基准组**：当前"无回执"。
- **消融**：同一开放项被重复提出的次数。
- **评价**：重复提出次数应单调下降。
- **淘汰条件**：若 fixup 仍不接线，本思路自动失效。

### 思路 B：`FeatureSpec` 白名单（消灭 §5 的沉默未来缺陷）
- **证据**：用仓库自己的 `feature_columns()` 实测 15 列泄漏（构造帧）；真实矩阵今日恰好 9+82；`git grep` 无白名单 0 命中。
- **新意**：把"默认允许"翻转为"默认拒绝"并用**测试**锁死；今日安全是**巧合**（列集合恰好相等）而非设计。
- **模块**：`FeatureSpec = {allowed: frozenset, forbidden_patterns: (...)}`；未知列 `raise`；消灭 `build_matrix.py:520-524` 第二份拷贝。
- **评价**：未知列零容忍；82 列不变。
- **淘汰条件**：若 `feature_columns()` 在真实路径无调用者，降级 P2 卫生项。

### 思路 C：session-aware 标签引擎（一次性消灭 §2+§3 的共同根源）
- **证据**：§2 实测 H10 0.3480% / H60 2.0632% / H504 12.2308%；§3 净 −5（过度抑制）；`labels.py:59` 明写 `horizon_sessions`。
- **新意**：不是修 off-by-one，而是把**市场日历**提升为标签层**必需输入**（`forward_outcomes(..., calendar=)`），使"行"不再是任何语义单位。`labels.py` 与 `build_matrix.py:389-400` 两条独立实现共用同一 session 邻接掩码。
- **数据需求**：市场日历（`panel_daily.parquet` 的 887 session 自洽）。
- **基准组**：当前 HEAD 的 `labels.py`。
- **消融**：同一股票池、H∈{10,60,504}，比较正类率与命中率变化；必须同时报告**受影响行占比**与**受影响股票集中度**。
- **评价**：跨窗行数降为 0；正类率变化与方向可解释。
- **淘汰条件**：若正类率变化 < 0.05 个百分点且跨窗行集中在最后几个 session，降级 P2。

---

## §12 三类产出分离（手册要求）

### A. 程序修复
1. `src/labels.py:126-128` + `src/ml/build_matrix.py:389-400`：session 邻接掩码 + 按 session 推进（§2，P0）
2. `src/labels.py:233-234`：`dedupe_signals` 接受日历，缺失时抛错（§3，P1，**先重量级**）
3. `scripts/scheduled_report_audit.py:108`：verdict 纳入 `changed`/`note`，落 `Audited commit` 行（§4，P1）
4. `src/ml/walkforward.py:58-61,108-109`：黑名单 → `FeatureSpec` 白名单（§5，P1，latent）

### B. 任务定义变更（我无权执行）
5. **注册 `ProWeb60d-Fixup`，或明确宣告该链未启用**（§1，P0，**最高优先**）
6. `SCHEDULE.md`：加"上一份候选未落地前不得提下一代"（§7，P1）
7. 候选产物必须提交到仓库非 `docs/` 路径，否则自动记 `未复现`（§6/§7，P1）

### C. 真实模型增益
**无。** `real_market_fit_count = 0`，与上轮持平。本轮没有、也不能声称任何实股增益。§2/§3 修好后，**已有** H10/H60 结果会小幅变动（0.35%—2.06% 成熟行受影响），届时需重跑才知方向与幅度——那才是第一次真正的"真实模型增益"机会。

---

## §13 明确记为未复现 / 不指控

| 事项 | 原因 | 标记 |
|---|---|---|
| 候选 v4 的 `33 passed in 5.02s` | 本仓库全部可收集测试 = 15；无 `research_h504/` | **未复现** |
| 4 个候选 SHA-256（含 pytest 日志哈希） | 只存在于 2 个 `.md` 散文 | **未复现** |
| v3→v4 patch 内容 | 仓库无 `*.patch` | **未复现** |
| `2026-09-21_14-06-49_JST.md` 的本地 3 小时执行 | 无 start receipt / registry / checkpoint | **未复现** |
| 候选是否伪造 | 无证据 | **不指控** |

---

## §14 授权与纪律声明

- 本轮**只读**审计；除本报告与 `LATEST.md` 外未改动任何文件。
- 未创建／修改任何计划任务（只做 `schtasks /query` 只读查询）。
- 未触碰 `scripts/register_fixup_task.ps1`（他人 `M`）与 `.gitattributes`（他人 `??`）。
- 未 force push、未合并 PR、未启动远程训练、未自动交易。
- 上传范围仅限专家／ML 审计目录与索引。
- **本报告更正了我自己初稿的两处错误**：§0/§2/§3 的"新发现"定位（实为既有报告已发表）与 §3 的方向表述（实为过度抑制，非过度宽松）。

`reviewed_source_sha`: `5d1b365cd6c2a80a74b18fbf12e47e766203eb93`
