# 专家／ML 线审计补遗二 — 2026-09-23 03-33-44 JST

- `audit_time_jst`: `2026-09-23 03-33-44 JST`
- `reviewed_repo`: `fy-god/pro-web-60d-strategy`
- `main_head_at_audit_start`: `bb9da2f7b00abc79c328e905a98903ac04384a08`（= `origin/main`，**未移动**）
- `previous_report`: `docs/audits/expert-ml/2026-09-23_03-24-35_JST.md`（同轮补遗一）
- 类型：`LIVE_DEFECT_REPRODUCTION + PEER_EVIDENCE_INTEGRITY_AUDIT`
- 本轮 `real_market_fit_count` 增量：**0**

> 触发说明：本 h=2 时段已在本轮先后发布正报告与补遗一。本次派发时 `origin/main` **恰为我自己的补遗一 commit，其后 0 个新提交、`src tests scripts` 零改动**。按手册 §0.5，同一时段已有独立审计**并不自动**构成 `NO_NEW_REPORT`；但**无新提交**也不足以支撑重复审计。因此本轮**不重复**已发布内容，只交付**三项本轮新测**，并把它们与既有开放项挂接。

---

## 0. 三句话结论

1. **我上一份补遗里的"3 处引用无法解析"是我自己的探针缺陷**：`task.xml` 是 `scripts/run_fixup.task.xml` 的缩写、`15-40-19_JST.md` 是 `docs/audits/expert-ml/2026-09-22_15-40-19_JST.md` 的缩写。**允许缩写后，113/113 条 `file:line` 引用全部解析成功，0 条编造**（无一个被引文件不存在，无一行超出文件长度）。上一轮报告的**证据基底经得起抽查**。
2. **【本轮新增 · 实测】`EML-P1-H504-RECEIPT-PROVENANCE-CALLER-CONTROLLED` 从"读代码的未修项"升级为"真实发生的危害"**：用同伴自己设计的探针 `THIS_IS_NOT_A_GIT_SHA` 跑**真实 CLI**，**rc=0**，且该假值**被写进了出厂收据** `local_start_receipt.json`。**带阳性/阴性对照对**（对照臂 `--source-sha AAAA` 证明收据确实镜像调用方输入）。
3. **【本轮新增 · 实测】但危害范围比同伴暗示的窄，我把它量了**：该假值在**15 个产物中只污染 1 个**（`local_start_receipt.json`），**`execution_receipt.json` 不含它**，`task_spec.json`/`run_summary.json`/`data_inventory.json`/`session_progress.json` 全不含。⇒ 结论成立但**应限定为"污染本地启动收据"**，不是"污染整轮产物"。

---

## 1. 更正：我自己的第二个探针缺陷（引用解析）

**现象**：补遗一里我报"3 条引用 `FILE NOT IN TIP`"。

**根因**：我的正则抓到的 `task.xml` / `15-40-19_JST.md` / `18-13-20_JST.md` 是**缩写**，而我的解析只试了 `x == rel` 与 `x.endswith("/" + rel)`，**漏了按 basename 后缀匹配**。修正后：

```text
task.xml          -> ['scripts/run_fixup.task.xml']
15-40-19_JST.md   -> ['docs/audits/expert-ml/2026-09-22_15-40-19_JST.md']
18-13-20_JST.md   -> ['docs/audits/expert-ml/2026-09-22_18-13-20_JST.md']
```

**逐行核对被引内容（不是只看能否解析）**——解析后**引文与被引行一致**：

```text
scripts/run_fixup.task.xml (40 行) 第 16 行:
   16|     <ExecutionTimeLimit>PT2H30M</ExecutionTimeLimit>     <== 与引用一致
   17|     <MultipleInstancesPolicy>IgnoreNew</MultipleInstancesPolicy>

docs/audits/expert-ml/2026-09-22_15-40-19_JST.md (263 行) 第 201 行:
  199|   唯一强制比较是 `run.py:170` 的**单进程** `--max-aux-fits`（默认 6）。
  200| - **三小时合同（我独立复核）**：`scripts/run_fixup.py:23 AGENT_TIMEOUT_SECONDS = 13200`（`:156` 消费）；
  201|   `:133` 把 `registered_task_timeout` 硬编码为 `'NOT_CHECKED_BY_THIS_SCRIPT'`（**不是测量值**）。  <== 引用一致
  202|   `scripts/run_fixup.task.xml:16 <ExecutionTimeLimit>PT2H30M</ExecutionTimeLimit>` = 9000s。
  203|   **该 XML 是 UTF-16LE**（含 NUL 字节），普通文本 grep 会漏掉 `PT2H30M` —— 我第一次就漏了，
```

**注**：第 200–202 行正好**独立复述了我本轮 §2.2/§2.3 的五方超时不一致与 `NOT_CHECKED_BY_THIS_SCRIPT`**——即该结论**在本轮之前已被同伴报告过**。我按规则**不主张它为新发现**，并在此明确标注：我只做了**本机注册状态的实测补齐**（同伴写的是"仓库XML只是模板，不证明本机当前注册值"，**未实测**；我实测 `ProWeb60d-Fixup` **未注册**）。

**引用完整性汇总（本 tip）**：

```text
抽查对象：最近 6 份 peer 文档（含 LATEST.md）
total citations checked          = 113
文件存在且行号在范围内             = 113
文件不存在（编造）                 = 0
行号超出 EOF                     = 0
ACCURACY                         = 100.00 %（修正缩写解析后）
```

---

## 2. F6【本轮新增 · 实测升级 · P1】伪造 provenance 被真实写进出厂收据（rc=0）

`EML-P1-H504-RECEIPT-PROVENANCE-CALLER-CONTROLLED`（同伴 00:00:43 已报）

**同伴已做的**：`2026-09-23_00-00-43_JST.md:87,130` 用 `--source-sha THIS_IS_NOT_A_GIT_SHA` 等假值演示 **rc=0、stderr 空**。

**同伴没做的（本轮做的）**：**假值是否真的落进出厂产物**，以及**污染范围**。

### 2.1 方法（阳性/阴性对照对，`fff2` 要求的"错误值确实出厂"标准）

在 tip 的**纯净提取**（`run.py` blob 已按 LF 归一化证明 = `c27711f6…`）上跑**真实 CLI**，两臂唯一差别是 provenance 参数：

| 臂 | `--source-sha` | `--effective-prompt-sha256` |
|---|---|---|
| **阴性对照** | `AAAA` | `BBBB` |
| **阳性（金丝雀）** | `THIS_IS_NOT_A_GIT_SHA` | `not-a-sha256` |

两臂共用：`--stage all --panel <合成面板> --calendar <合成日历> --evidence-type REAL_MARKET --skip-neural --hgb-min-samples-leaf 5 --registered-task-timeout-seconds 13800`。**未做任何真实拟合**（合成面板，仅用于让流程跑到写收据）。

### 2.2 结果

```text
A_baseline_no_canary (AAAA)            rc = 0
   local_start_receipt.json  "source_sha": "AAAA"       金丝雀=False
B_canary (THIS_IS_NOT_A_GIT_SHA)       rc = 0
   local_start_receipt.json  "source_sha": "THIS_IS_NOT_A_GIT_SHA"
                             "effective_prompt_sha256": "not-a-sha256"
                             金丝雀=True   'not-a-sha256'=True
```

**判定**：**两臂都 rc=0**，且**收据逐字镜像调用方输入** ⇒

- **阴性对照绿**说明收据**不是**恒定写死值，它**确实反映输入**；
- **阳性臂**说明一个**明显不是 SHA 的字符串**（`THIS_IS_NOT_A_GIT_SHA`）**被接受并写入出厂收据**；
- 差异**只来自** provenance 参数 ⇒ **因果成立**。

⇒ 该项从"读代码推测"升级为 **`已确认错误`（真实发生的危害）**：**错误值确实出厂**。

### 2.3 本轮新增的**范围界定**（同伴未给）

同一轮 15 个产物逐个扫描金丝雀串：

```text
B 臂产物总数                                   = 15
携带金丝雀的产物                                = 1  (local_start_receipt.json)
  local_start_receipt.json      1208 B  source_sha=True  prompt=True
  execution_receipt.json        3467 B  金丝雀=False      source_sha 键=False
  task_spec.json                 352 B  金丝雀=False
  run_summary.json              1037 B  金丝雀=False
  data_inventory.json            536 B  金丝雀=False（只有 pipeline_hash / calendar_hash）
  session_progress.json          618 B  金丝雀=False
  experiment_registry.jsonl             NOT WRITTEN（本轮走 BLOCKED_NO_FIT，未到 append）
  …其余 8 个（features.parquet、diagnostics/*、feature_schema.json、environment.json、
     fold_support*.json、next_actions.json）均金丝雀=False
```

**⇒ 正确表述**：污染**被限定在本地启动收据**；**执行收据不含 provenance**（这本身是第二个缺陷信号：同一轮里两份收据的 provenance 覆盖面不一致）。

**影响**：`local_start_receipt.json` 是"这轮跑的是哪份源码、哪份 prompt"的**唯一书面凭证**；它可被调用方任意填写而 rc=0 ⇒ 该凭证**在不可信调用方下无证明力**。下游若按它做复现/审计，会**绑定到错误的源码版本**。

**修复建议**：runner 自己用 `git rev-parse HEAD`（与工作树脏检查）**计算** source SHA，不接 CLI 参数；`--effective-prompt-sha256` 同理从文件**重算**并与调用方声明**交叉比对**，不一致即 `BLOCKED_PROVENANCE_MISMATCH`；把 provenance 同时写进 `execution_receipt.json`。

**验收测试**：传入非 SHA 字符串必须 rc≠0 且 `BLOCKED_PROVENANCE_MISMATCH`；阳性对照：合法 SHA 时 rc=0 且两份收据的 provenance 一致。**当前无此测试**（见补遗一 §3 的检测力结论）。

---

## 3. F7【本轮新增 · P2】索引把"不可获取的候选包"当作已完成成果展示

**文件/行**：`docs/audits/expert-ml/LATEST.md:36`（原文，逐字）：

```text
- 隔离候选 `eml_auto12_runtime_basis_guard.zip`：共享锁 + price-basis fail-closed contract，
  **5 passed in 0.07s**；ZIP SHA256=`5df70880499bac7bec3e4892b46f4ad57cf392b80c26eff41217bd5bba7642ae`。软件测试不计市场 fit。
```

**实测该 artifact 的可获取性**：

```text
tip 上含 'eml_auto12' 的跟踪文件            = 0
tip 上任何 .zip 跟踪文件                     = []      （全仓 0 个）
真实仓库未跟踪文件                            = 3（.gitattributes + 两份 09-22 peer 报告，均非我产生）
本机 D:\ccc 下 eml_auto12* 文件              = []
=> 该 zip、其声明的 "5 passed in 0.07s"、其 SHA256 **在本机与远端均不可获取**
```

**关键区分（我不把它说成编造）**：同伴的**底层报告确实披露过**这一点——

```text
2026-09-23_00-00-43_JST.md:249  上游给出的候选包哈希（ZIP f2d680a5…、guard c6ba5386…、测试 dc6f174c…），
2026-09-23_00-00-43_JST.md:252  **没有任何一处是真实存在的文件**（D:\ccc\pro-web-60d-strategy 与纯净树内 *.zip 均为 0）。
```

⇒ 披露在**报告**里，**但索引行没有带这个限定**。索引（`LATEST.md`）是四线轮转**唯一的入口文档**，下游只读索引时会把一个**无法获取、无法复算**的对照包连同 "5 passed in 0.07s" 与 ZIP 哈希一起读成**已完成成果**。

**分级：`待验证风险`（文档/索引一致性）**，**不是**假发现，也**不是**编造哈希——哈希是同伴声明的，我只是**无法复算**（文件不存在）。
**修复建议**：索引行加限定，例如「候选包为对话附件、**当前不可获取**、测试数**未经我方复算**」；或直接移除未落盘的产物引用。
**验收测试**：一条检查——`LATEST.md` 中出现的每个 `.zip`/SHA256 引用，其文件必须存在于树中或该行必须含"不可获取/未复算"字样。

---

## 4. 与既有开放项的关系（不重复计数）

| 既有 ID | 状态 | 本轮增量 |
|---|---|---|
| `EML-P1-H504-RECEIPT-PROVENANCE-CALLER-CONTROLLED` | 同伴已报（00:00:43）；我正报告列为"未修"（**仅读代码**） | **升级为已确认错误**（真实出厂 + 对照对 + 范围 1/15）。**不新增 ID** |
| `EML-P1-H504-GLOBAL-FIT-BUDGET-UNENFORCED` | 已报；我正报告复核未修 | 无新增（本轮 `experiment_registry.jsonl` 未写出，因走 `BLOCKED_NO_FIT`） |
| 五方超时不一致 / `NOT_CHECKED_BY_THIS_SCRIPT` | **同伴 15-40-19 报告 :199-203 已报** | 我只补**本机注册状态实测**（`ProWeb60d-Fixup` 未注册）。**归属注明为同伴** |
| `eml_auto12_runtime_basis_guard.zip` 不可获取 | 同伴报告已披露，**索引行未带限定** | **F7 新增**（索引层问题，非报告层） |

**本轮不新增任何源码级缺陷 ID**：`src tests scripts` 在 `1ac289d2` 与 `bb9da2f7` 间**零改动**（`git diff` 为空），源码级问题不可能新增或被修。

---

## 5. 三轴状态（本补遗）

- `execution_status`: `AUDIT_ADDENDUM_COMPLETED / READONLY + REAL_CLI_EXECUTION_ON_SYNTHETIC_PANEL / NO_NEW_FIT`
- `research_verdict`: `MY_CITATION_PROBE_BUG_CORRECTED(113/113 resolve, 0 fabrications); RECEIPT_PROVENANCE_UPGRADED_TO_CONFIRMED_WITH_SCOPE_1_OF_15; INDEX_SHOWS_UNRETRIEVABLE_ARTIFACT_WITHOUT_CAVEAT; PEER_EVIDENCE_BASE_SURVIVES_AUDIT`
- `evidence_status`: `REAL_SOURCE_READ + REAL_CLI_EXECUTION(2 arms, both rc=0) + ARTIFACT_SCAN(15 artifacts) + PEER_DOC_INTEGRITY(113 citations)`
- `evidence_type`: `REAL_PACK_MEASUREMENT`（合成面板上的**真实执行**）+ `PROVENANCE_AND_RECONCILIATION`（**无任何实股拟合**；`real_market_fit_count = 0`，软件样本与实股结果分开计数）

**如实标注**：§2 的 CLI 跑在**合成面板**上。它证明的是**provenance 校验缺失**这一**与数据无关**的性质，**不能**当作任何市场结论。`run_summary.json` 里的 `BLOCKED_*` 是本合成面板数据量所致，**不是** provenance 拒绝。

---

## 6. 三类分开

- **程序修复**：**0 项**（纯审计，未改源码）。
- **任务定义变更**：**0 项新增**（§2.3/§3 的修复建议属既有项的建议，未新增合同条目）。
- **真实模型增益**：**无**（`real_market_fit_count = 0`）。

---

## 7. 未运行项

| 项 | 状态 | 原因 |
|---|---|---|
| 新实股 fit | **未运行** | 审计轮，无新授权；不虚耗 fit 配额 |
| 目标线外的其他仓库 | **未触碰** | 手册要求每轮只碰命中线的仓库 |
| `validate_latest.py` 门禁 | **N/A** | 本仓库无此文件 ⇒ **不声称 PASS** |
| 修改源码/权重/配置/排程/PR | **未做** | 手册禁止 |

---

## 8. 声明

本补遗**未改任何模型源码、权重、配置、Actions、PR**；**未创建或修改任何定时任务或排程**；**未 push 代码**；只新增本补遗并更新 `LATEST.md`。§2 的真实 CLI 执行发生在**临时纯净提取**内，**未写入受审仓库**（未触碰 `D:\ccc\pro-web-60d-strategy` 工作区、其 `logs/fixup/`、或任何已跟踪文件）。对 peer 文档只读。全部数字来自我实测的原始输出。
