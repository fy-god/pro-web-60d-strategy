# 专家／ML 线审计报告 — 2026-09-24 03:48:00 JST（h=2 CST）

- `audit_time_jst`: `2026-09-24 03:48:00 JST`
- `audit_time_cst`: `2026-09-24 02:48:00 +08:00`
- `round_h_cst`: **2**（路由：`dispatch.md` L21 → 专家/ML ✔）
- `reviewed_repo`: `D:\ccc\pro-web-60d-strategy`（origin `https://github.com/fy-god/pro-web-60d-strategy.git`）
- `reviewed_source_sha`：`3110d22e04a9c740d1dae7135db518d7402fe9b0`（本轮 `git ls-remote` 实读）
- `previous_audit_head` / `reviewed_head`：`e72c7258537b0e5c70bee6d5253faaa45fcf6b73`
- `open_pr`: 0
- `product_source_change_since_previous_audit`: **NO**（实测 `git diff --stat e72c7258 3110d22e -- . ':(exclude)docs'` = **EMPTY**）
- `real_market_h504_fit_delta`: **0**
- `TARGET50_REACHED`: **NO**　`TARGET70_REACHED`: **NO**
- `evidence_type`: `SOFTWARE_SAMPLE`（**本轮无新实股结果、无联网取数**）

---

## §0 结论先行

本轮**没有新产品源码可审**（`e72c7258..3110d22e` 非 docs diff 为空；新增 2 个 commit 全是 `[skip ci]`
审计文档）。所以我**没有**再做一遍"再确认云端结论"，而是去**独立复算云端最新报告的核心实证**，
并因此拿到一条**云端报告自己没写、但仓库里早已有数据的结论**。

**一句话结论**：云端报告把「训练内 50.6% → 时间外 15.98%」当作**拟合能力存在、缺口只在泛化**的证据；
但仓库**自己的零假设电池**（同一张 stride-5 网格、同样 281,227 行）里，一个**完全没有信号**
的双重零假设（`feat_gauss_auc0`：特征 N(0,1) + 标签 Bernoulli，AUC **0.4976**）在同一口径下也拿到
**29.8416% 训练内 precision**，并同样塌到 **3.2673% 时间外**（gap **+26.574pp**，占真实 gap 的 **76.8%**）。
⇒ **"训练内能到 50.6%"这句话本身不是拟合能力的证据**，云端那一句推断**不被它引用的证据支持**。

同时**确认一条 P2 缺陷**并**用执行实验证明**（不是复述）：`EML-P2-EVIDENCE-TYPE-NOT-IN-REUSE-SIGNATURE-002`
—— 复用签名不含 `evidence_type`，`SYNTHETIC` 的 `COMPLETE` 行会被后续 `REAL_MARKET` 运行**静默复用**。

---

## §1 本轮实际动作、命令与退出码

| # | 命令/动作 | 退出码 | 真实结果 |
|---|---|---|---|
| 1 | `git remote get-url origin` | 0 | `https://github.com/fy-god/pro-web-60d-strategy.git` |
| 2 | `git ls-remote origin refs/heads/main` | 0 | `3110d22e04a9c740d1dae7135db518d7402fe9b0` |
| 3 | `git fetch origin` | 0 | `e72c725..3110d22  main -> origin/main`；fetch 后 `rev-parse origin/main` == 新鲜 ls-remote ✔ |
| 4 | `git status --short` | 0 | **5 条**（` M LATEST.md`、` M scripts/register_fixup_task.ps1`、`?? .gitattributes`、`?? 2 份 2026-09-22 报告`）—— **他人未提交改动，只记录不覆盖** |
| 5 | `git rev-list --left-right --count HEAD...origin/main` | 0 | `11 48`（本地 behind 48 / ahead 11，**未做任何 checkout/reset/clean**） |
| 6 | `git diff --stat e72c7258 3110d22e -- . ':(exclude)docs'` | 0 | **EMPTY ⇒ 产品代码零变化** |
| 7 | `git archive origin/main` → 净提取 `em_h2_tree` | 0 | 绕过脏工作树，拿到与远端完全一致的树 |
| 8 | `python -m pytest -q`（在**净提取**内，非原仓库） | **0** | **65 passed in 23.81s**（见 §7） |
| 9 | `python -m pytest -v`（同上，第二次独立计数） | **0** | `65 passed in 21.87s` |
| 10 | `em_h2_recompute.py`（逐字段复算） | 0 | 云端全部 12 个数字**逐一复现** |
| 11 | `em_h2_final.py`（零假设对照表 + 对照臂） | 0 | **本轮核心新结论**（见 §3） |
| 12 | `em_h2_proof.py`（复用缺陷执行证明） | 0 | 阳性臂 + 3 个阴性对照全部成立（见 §4） |
| 13 | `git ls-tree -r --name-only origin/main -- outputs/charts_120d` | 0 | **1920** 个跟踪文件（939 hit / 980 miss / 1 `index.html`） |

---

## §2 云端报告的数字：**全部逐一复现**（我自己的值，不是抄它的）

| 云端声称 | 我复算的值 | 判定 |
|---|---|---|
| 训练内 `fam_hgb_0` signals **19,885** | 19,885 | ✔ |
| 训练内 precision **50.600955%** | `0.5060095549409103` | ✔ |
| OOS signals **1,790** | 1,790 | ✔ |
| OOS precision **15.977654%** | `0.159776536312849` | ✔ |
| OOS base rate ≈ **4.0878%** | `0.040878009579` | ✔ |
| lift ≈ **3.91x** | `3.908618` | ✔ |
| 下降 **34.6233 个百分点** | `34.623302` | ✔ |
| 保留约 **31.6%** | `31.575794%` | ✔ |
| ceiling OOS rows **1,406,181** / positives **57,505** | 一致；`57505/1406181 = 0.040894450999` | ✔ |
| raw max OOS precision **26.7790%** | `0.26779026217228463` | ✔ |
| rank @min_signals=50 **32.7273%** | `0.32727272727272727` | ✔ |
| 消融 5 个精度 + 4 个 delta | 全部一致（`+2.092063pp`/`+12.7274%` 等） | ✔ |

⇒ **§1–§3 的报告数字本身没有造假**。问题不在数字，而在**这些数字被放在一起的方式**（§3、§5）。

---

## §3 【本轮核心新发现】训练内 50.6% 不是"拟合能力"的证据（**已确认的推断错误**）

**文件/函数/行**：`outputs/ml/audit/nulls_audit.json` → `results[]`；
云端报告 `docs/audits/expert-ml/2026-09-24_02-01-50_JST.md` §1、§4。

**为什么这是可查的**：报告 §1 的原话是「当前问题不是"分类器连 50% 都拟合不了"，而是**训练内可以到约
50.6%，时间外直接掉到约 16%**」。这句话把**训练内 precision** 当作**拟合能力**的度量。
但同一个仓库里就有一份**零假设电池**，跑在**同一张网格**上。

**同一性核验（必须先证明我在比同一个东西）**：

| 项 | 值 |
|---|---|
| 电池矩阵 | `outputs/ml/matrix_h10_t30_s5.parquet` |
| 电池 `pooled_rows` | **281,227** |
| 报告 `ml_crosssec_final.json` `fam_hgb_0.n_rows` | **281,227** |
| `pooled_rows == n_rows` | **True** |
| 电池 `real` 行 `insample_precision` | `0.5060095549409103` |
| 报告引用的 `insample_global_threshold.precision` | `0.5060095549409103` |
| 十五位小数相等 | **True** |
| `null_ceiling.json` `real_baseline.insample_precision` | `0.5060095549409103`（同值） |
| 电池自证 `harness_vs_audit_precision_max_abs_diff` | **0.0**（同一测量，非相似管道） |

**14 条臂（全部同一网格、同样 281,227 行）**：

| variant | 训练内 | OOS | gap(pp) | AUC | folds>base |
|---|---:|---:|---:|---:|---:|
| **real**（对照） | **50.6010%** | **15.9777%** | **+34.623** | 0.6306 | 4/4 |
| **feat_gauss_auc0**（双重零假设） | **29.8416%** | 3.2673% | **+26.574** | **0.4976** | 4/4 |
| feat_gauss_7 | 27.0304% | 4.4467% | +22.584 | 0.4424 | 2/4 |
| feat_gauss_8 | 27.6842% | 3.6254% | +24.059 | 0.4402 | 1/4 |
| feat_permute_9 | 28.1267% | 4.0370% | +24.090 | 0.4410 | 2/4 |
| perm_global_101/202/303 | 21.98/21.81/23.15% | 3.26/3.32/2.99% | +18.7/+18.5/+20.2 | ≈0.496 | 3/3/1 of 4 |
| label_bernoulli_505 | 21.4584% | 3.3755% | +18.083 | 0.4981 | 4/4 |
| perm_within_date_404 | 20.1558% | 6.2842% | +13.872 | 0.4809 | 1/2 |
| perm_train_only_606 | 19.6681% | 4.9608% | +14.707 | 0.4351 | 3/4 |
| perm_test_only_707 | 50.6010% | 4.9721% | +45.629 | 0.4787 | 3/4 |
| oracle_strong（**阴性对照**，标签泄露） | 56.9575% | **39.0459%** | +17.912 | **0.7754** | 4/4 |
| oracle_weak（阴性对照） | 51.7626% | **19.4170%** | +32.346 | 0.6751 | 4/4 |

**对照臂（必需，否则结论没有判别力）**：

- **阳性臂** `feat_gauss_auc0`：AUC **0.4976 ≤ 0.5**（确证无信号），却拿到训练内 **29.8416%**、
  gap **+26.574pp** ⇒ 度量**对无信号构型也会报出巨大 gap**。
- **阴性臂** `oracle_strong`：AUC **0.7754 ≫ 0.5**（确证有真信号），gap 只有 **+17.912pp**
  ⇒ 我的 AUC 字段**能**区分信号与噪声（两臂 AUC 差 **+0.2778**）。
- **无信号臂中最大的 gap = +26.574pp = 真实 gap 的 76.8%**；
  无信号臂中**最高的训练内 precision = 29.8416% = 真实值 50.6010% 的 59.0%**。

**机制**：`insample_global_threshold` 是**在同一批数据上选阈值再报分数**，所以它同时吸收了
(a) 任何有限样本的**选择偏差**（HGB 在噪声上也能找到高分切片）与 (b) 真正的样本内过拟合。
零假设电池的价值正是把 (a) 单独量出来 —— 而它占了大头。

**影响**：报告据此把研究优先级从"提高拟合/换模型"改到"改候选分布、时序与硬负样本"。
**排序方向可能仍然正确**（它另有秩相关与消融证据），但**§1 这一条推断本身不被它引用的证据支持**，
且**报告全程 0 次提到"零假设/双重零假设"**（我实测 `null`/`零假设`/`nulls_audit` 出现 **0 次**）。

**修复建议**：报告在引用训练内分数时，必须同时给出**同网格零假设的同口径对照**；
不得把 in-sample precision 单独当作拟合能力证据。

**验收测试**：`nulls_audit.json` 已含 `feat_gauss_auc0`；建议在
`scripts/audit_reports.py` 增加一条 gate —— 任何报告若引用某训练内 precision，
必须引用同网格 `feat_gauss_auc0.insample_precision` 并注明二者之比。

---

## §4 `EML-P2-EVIDENCE-TYPE-NOT-IN-REUSE-SIGNATURE-002`（**已确认错误，执行级证明**）

**文件/函数/行**：

- 签名构造：`src/ml/research_h504/run.py:150`（AST 展开的键集）：
  `{'pipeline_hash','calendar_hash','task','data','cand_sha256','features','horizon','model_config'}`
  —— **不含 `evidence_type`**。
- 复用判定：`src/ml/research_h504/registry.py:19-23`
  ```python
  19     def latest_complete(self,signature):
  20         for r in reversed(self.rows()):
  21             if r.get('signature')==signature and str(r.get('status','')).startswith('COMPLETE'):
  22                 return r
  23         return None
  ```
- 写入时 `evidence_type` **是**记录进 registry 的：`run.py:155` / `:181`
  `registry.append({... 'signature':sig, 'evidence_type':args.evidence_type, ...})`
- `evidence_type` 在 `registry.py` 中出现 **0 次**（我实测计数）。

**触发条件**：同一组输入（因此同一 signature）先后用**不同 `evidence_type`** 运行。
先跑的 `SYNTHETIC` 若成功，后来标 `REAL_MARKET` 的同一配置会被判为"已完成"。

**执行证明（阳性 + 3 阴性对照，全部实跑）**：

| 步骤 | 实测 |
|---|---|
| 签名（复刻 `run.py:150` 键集） | `8dfb2f6b…9c8407`；`'evidence_type' in keys` = **False** |
| 一个 `REAL_MARKET` 运行算出的签名 | **与前一行完全相同** |
| 阳性臂：先写 `evidence_type='SYNTHETIC'`, `status='COMPLETE_H504'` | registry 1 行 |
| 再问 `latest_complete(sig)` | **返回该行**（`evidence_type='SYNTHETIC'`）⇒ 非 None |
| `run.py:158` 生成的 `REUSED_COMPLETE` 键集 | `['experiment_id','model_id','signature','source_status','status','task_scope']` —— **无 `evidence_type`** |
| 阴性对照 1：不同签名（horizon=505） | `latest_complete` → **None** ✔（判定器有判别力） |
| 阴性对照 2：同签名但 `status='INTERRUPTED_WALL_BUDGET'` | → **None** ✔ |
| 阴性对照 3：`startswith('COMPLETE')` 对真实状态串 | `COMPLETE_H504`→T、`COMPLETED_AVAILABLE_RESEARCH_NOT_CERTIFIED`→**T**、`INTERRUPTED_WALL_BUDGET`→F、`BLOCKED_NO_FIT`→F、`REUSED_COMPLETE`→F |

**机制**：把「相同输入 ⇒ 可复用」这条推理建立在一个**不含证据类别**的签名上；
`evidence_type` 只在**写入**时被存下，**判定时从不读取**。

**影响（我必须限定口径，不夸大）**：这是**来源标注（provenance）缺陷**——
被复用行的数值是 `SYNTHETIC` 运行的数，却会被当作 `REAL_MARKET` 结果返回并写进
`run_summary.json`/`execution_receipt.json`（`run.py:190` 的 `summary` 里带
`evidence_type=args.evidence_type`，即**当次运行**的标签）。
它**不会**篡改一个正确标注的 fit，且需要签名碰撞（同输入换标签）才触发。
但它使**"实股结果"可以由"合成结果"冒名生成** —— 这正是本线三轴里
`evidence_type` 必须分开计数的原因。

**修复建议**：把 `evidence_type` 纳入签名输入，或在 `latest_complete` 增加
`and r.get('evidence_type')==ev` 的匹配；并在 `REUSED_COMPLETE` 结果里带上来源 `evidence_type`。

**验收测试**：`evidence_type=SYNTHETIC` 写一行后，同签名以 `REAL_MARKET` 查询必须**不得**复用；
并加一条 `REAL_MARKET` 与 `REAL_MARKET` 的正向复用用例，确保修复没有把复用功能整体关掉。
（现状：`tests/*.py` 共 4 个文件，**0 个**覆盖 `REUSED_COMPLETE`/`QUEUE_DRAINED`；实测计数。）

---

## §5 口径问题（**待验证风险 / 文档缺口，非数值造假**）

### 5.1 三份产物**不是同一个 OOS 总体**，报告却当成一幅连贯图景（已确认）

| 产物 | 规模字段 | base rate |
|---|---|---|
| `ml_crosssec_final.json` | `n_rows` **281,227** | `0.040878009579`（stride-5 网格） |
| `ml_precision_ceiling.json` | `oos_rows` **1,406,181**；`_matrix_rows` 2,680,715；`stride=1` | `0.040894450999`（dense stride-1） |
| `ml_search_ablation.json` | 无行数 | `0.040894450999` |

- ceiling 与 ablation 的 base rate **十五位小数相同** ⇒ 同一总体；
- crosssec 的 base rate **不同**，行数约为 ceiling 的 **1/5.000164** ⇒ **另一个总体**。

而仓库**自己**在 `TARGET_70PCT.md:382-396` 有一张**明确的"哪一节用哪张网格"表**，
并写明「**No cross-grid subtraction is made anywhere**」；更有一条 gate
（`scripts/audit_reports.py:3001-3030`，`EML-P0-CROSSSEC-POP`）**要求**在 stride-5 运行上
`pooled_baseline_oos_precision` 必须**等于**该运行自己的 `global_threshold.precision`，
理由是「不要留一个 15.98% 被读成本轮基线」，否则须显式声明总体。

**我实测**：云端报告全文 **0 次**提到 `stride`/`dense`/`grid`/`281,227`/`population`/`网格`，
**0 次**引用 `TARGET_70PCT`、`nulls_audit`、`null_ceiling`（我逐串计数）。
⇒ 报告把 **stride-5** 的 §1 与 **dense** 的 §2/§3 并列成一个"三条新实证"，
**没有做它自己仓库的 gate 所要求的总体声明**。

**我撤回了一次误读（如实记录）**：我最初把「约占训练 rows 的 2%」读成"分母是 `n_rows`"，
那样应得 7.07%。但 `n_rows=281,227` 是**测试行数**（与 `n_test_sessions=462` 并列，
`n_folds=4` 而配置 `folds=5`），所以报告的 2% 与产物自己的 `share_of_rows` 一致，
而 `share_of_rows` 的分母 `19,885/0.01999884098608814 = 994,307.62` 是**未标注**的第三个数。
⇒ **不是缺陷，是分母未标注的文档缺口**。我**撤回**原读法。

### 5.2 `ml_precision_ceiling.json` 的 `recall` 字段由 **raw** 精度推出，却被用于 **rank** 上限

`at_signals`/`recall` 只对应 `max_precision_raw` 那一行。用整数性检验（精度×信号数必为整数个正例）：

| min_signals | at_signals | raw×N 整数? | rank×N 整数? |
|---|---:|---|---|
| 50 | 534 | **143（是）** | 174.7636（否） |
| 100 | 534 | 143（是） | 169.9091（否） |
| 250 | 534 | 143（是） | 152.9296（否） |
| 500 | 534 | 143（是） | 152.9296（否） |
| 1000 | 1464 | 345（是） | 390.4（否） |
| 2000 | 2000 | 447（是） | 482（**是**） |
| 5000 | 9056 | 1680（是） | 1773（**是**） |
| 10000 | 16112 | 2685（是） | 2822（**是**） |

**raw 侧 8/8 都是整数（阳性对照成立），rank 侧 5/8 不是整数** ⇒ `at_signals` 是 raw 行的信号数，
rank 上限**不可能**恰好在那 534 个信号上取得，产物里**没有**单独的 rank 信号数。
报告 §2 却把 rank 精度、534 signals、0.2487% recall 写在**同一句**里。

**影响**：两个数都极小（<1%），**不改变任何决策**；但该字段不能用于 rank 上限，
且这句话内部不自洽。**修复**：为 rank 行单独记录 `at_signals_rank`/`recall_rank`。

---

## §6 已确认 vs 降级（逐条）

| 项 | 判定 | 依据 |
|---|---|---|
| 云端 §1–§3 的 12 个数字 | **全部复现**（无造假） | §2 |
| 「训练内 50.6% ⇒ 拟合能力存在」这一推断 | **已确认的推断错误** | §3，阳性+阴性对照 |
| `EML-P2-EVIDENCE-TYPE-NOT-IN-REUSE-SIGNATURE-002` | **已确认错误**（执行级证明） | §4 |
| 三份产物非同一总体 / 未做总体声明 | **已确认（口径）** | §5.1 + 仓库自己的 gate |
| `share_of_rows` 分母未标注 | **我撤回原读法**，降为**文档缺口** | §5.1 |
| ceiling 的 `recall` 与 rank 精度不同轴 | **已确认（轻微）** | §5.2 整数性证明 |
| `scripts/run_fixup.py` 退出码契约 | **已确认（代码事实）** | `:23 AGENT_TIMEOUT_SECONDS=13200`；`:162 return 0 if rc==0 else 1`；文件内 `target_met` 出现 **0 次**；`receipts.py:173` 仅**记录** `target_met`；`register_fixup_task.ps1` 的 `PT2H30M`(=9000s) < 13200s（task.xml 经 **utf-16** 解码才可见，UTF-8 读会误判为缺失） |
| `outputs/charts_120d` 不得当作命中率 | **已确认** | 实测 1920 文件（939/980/1）；**每个策略目录 40 个文件（20 hit+20 miss = 50.00%）**；`src/make_charts.py:319/382-383/417` 源码自称 balanced sample、"this is NOT the hit rate"。（**修正**：我最初按"每目录 20 个"写，实测是 **40** 个 = 20+20） |
| 测试覆盖 `REUSED_COMPLETE`/`QUEUE_DRAINED` | **已确认 0** | `tests/*.py` 共 4 个；`REUSED_COMPLETE`/`QUEUE_DRAINED`/`has_complete` 均 **0** 个文件命中 |
| 本机 runtime / 单次连续三小时 | **NOT_VERIFIED**（本轮无本机证据） | 无用户执行通道 |

---

## §7 测试真数

- `python -m pytest -q`（净提取 `em_h2_tree` 内）：**65 passed in 23.81s，exit 0**
- `python -m pytest -v`（第二次独立计数）：**65 passed in 21.87s，exit 0**
- 测试文件 3 个（`tests/test_engine.py`、`tests/research_h504/test_candidate.py`、
  `tests/research_h504/test_github_delivery.py`）。`test_candidate.py` 有 **40** 个 test 函数，
  其中 `:327-328` 断言 `target_met is False` 与 `evidence_type == 'SYNTHETIC'`。
- **未跑**：`scripts/audit_reports.py` 的 gate 套件（需完整 `outputs/` 与矩阵；本轮只做只读复算）。
- **口径提醒**：本仓库 `pyproject.toml`/`pytest.ini` 均**不存在**，所以 `-q` **会**打印汇总行（与盘中预警不同）。

---

## §8 子 agent 执行情况（如实记录）

| 子 agent | 任务 | 结果 |
|---|---|---|
| A `74b3ea56` | 独立复算 3 份产物的全部数字 + 交叉一致性 | **失败**（收尾消息为一句未完成的自问）；`REPORT.md` **未产出**（我核实磁盘） |
| B `ca3707ff` | 6 项历史未修项回归 + 测试真数 | **失败**（空收尾）；`REPORT.md` **未产出** |

**但两者都留下了可用的原始输出**，我**没有**因此丢弃它们的证据，而是**亲自复核**：
- B 留下 `p1b_out.txt`：复用签名的真实运行输出（`signature = 59a69c41…`、
  `latest_complete` 返回 `SYNTHETIC` 行、`REUSED_COMPLETE` 键集无 `evidence_type`）——
  我**自己重做**了同一实验（§4），独立得到相同结论与相同键集。
- B 留下 `p3_out.txt`：CLI 真实退出码（A1 `UNSPECIFIED` rc=0、A3 `SYNTHETIC` rc=0、
  A2 `REAL_MARKET` 无 calendar → `SystemExit BLOCKED_CALENDAR`；A4/A5 亦 rc=0 且
  `BLOCKED_CANDIDATE_MANIFEST`）—— 我引用时标明**这是子 agent 的实测记录**，非我本轮亲跑。
- B 的 `scratch/` 是一个**完整仓库副本**（含 1920 个 chart png），占用较大；
  已在 §9 声明，未清理（属本轮审计痕迹，不影响任何仓库）。

**教训**：子 agent 失败不等于零产出；**必须去磁盘读原始输出**再决定是否重做。

---

## §9 研究/改进想法（依据注明来自本轮哪条证据）

1. **把"零假设同口径对照"列为引用训练内分数的前置条件。**
   依据：§3 —— 无信号构型在同一网格拿到真实值 59.0% 的训练内 precision、76.8% 的 gap。
   这是一条**可自动化的 gate**，且能防止"把过拟合当能力"这类反复出现的误读。

2. **`evidence_type` 必须进入复用签名（或进入复用判定）。**
   依据：§4 的执行证明。这是**一处改动同时封掉"合成冒充实股"** 的路径，
   并让 `REUSED_COMPLETE` 结果自带来源标签。

3. **让"三轴同总体"成为产物字段的自带属性。**
   依据：§5.1 —— 三份产物规模差 5 倍、base rate 十五位不同，而报告并列使用。
   建议在每份产物里写入 `_population_id`（网格 + stride + 行数哈希），
   报告引用时自动校验一致；这比"靠人记得 stride-5 与 dense 不可相减"更可靠。

---

## §10 三轴状态与三类增益（分开写，不混算）

| 轴 | 值 |
|---|---|
| `execution_status` | `COMPLETED: 产品代码零变化确认 + 净提取内 65 passed/exit 0（两次）+ 12 个数字逐一复现 + 14 臂零假设对照（含阳性/阴性对照）+ 1 项缺陷执行级证明 + 4 项口径核验` |
| `research_verdict` | 云端报告**数字为真**但 **§1 的推断不被其证据支持**（`INCONCLUSIVE`）；`EML-P2-EVIDENCE-TYPE-NOT-IN-REUSE-SIGNATURE-002` **CONFIRMED** |
| `evidence_status` | `VERIFIED`（§3/§4 均有阳性 + 阴性对照）；**唯一未亲跑项**（CLI 退出码）已标为子 agent 实测记录 |
| `evidence_type` | `SOFTWARE_SAMPLE` |

- **程序修复**：本轮 **0**（审计轮不改源码）。
- **任务定义变更**：本轮 **0**。
- **真实模型增益**：本轮 **0**；`real_market_h504_fit_delta = 0`；
  **本轮没有新增实股结果**，亦无联网取数。`TARGET50_REACHED=NO`、`TARGET70_REACHED=NO`。

---

## §11 声明

- 本轮**未**修改任何模型源码、权重、配置、Actions、PR、根 `README.md`、其他实验归档。
- 本轮**未**创建/修改/删除任何定时任务或排程（无 `schedule_*`、`arm_self_wake`、`schtasks`、`Register-ScheduledTask`）。
- 本轮**只碰** `D:\ccc\pro-web-60d-strategy` 这一个仓库；**未** push 任何代码，只上传本审计文档与索引。
- **未**触碰 `D:\xm\60日预测\reference\eventnet_test_results.json`。
- 原仓库工作树的 **5 条他人未提交改动一字未动**（未 `checkout`/`reset`/`clean`/`stash`）；
  一切读取走 `git show origin/main:<path>` 或净提取。
- 测试**只在净提取内**运行，原仓库零写入。
- 所有 SHA/hash/通过数/退出码来自本轮**实际命令输出**；未亲跑者已标注来源；
  不可复现/我误读者（§5.1）**已如实撤回并降级**。
