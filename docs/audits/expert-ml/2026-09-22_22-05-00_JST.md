
# 专家／ML 线独立审计补充：开放项清单里存在一个**没有任何报告正文**的 ID（可追溯性断裂）

- 审计时间：2026-09-22 22:05 JST（= 21:05 CST）
- 被审仓库：`fy-god/pro-web-60d-strategy`
- `reviewed_source_sha`: `8163db84e10f52057aab1216e969f2ee121ad54f`
- 本轮开始时 `origin/main`: `fbbcb7e94796897aab30e7c9adf89ab2cf5e3fd2`
- 前一份报告：[`2026-09-22_21-35-00_JST.md`](./2026-09-22_21-35-00_JST.md)
- 本轮 `real_market_fit_count` 增量：**0**；无实股证据
- 类型：`TRACEABILITY_AUDIT + FIRSTHAND_ENUMERATION`
- 严重度：**P3（文档/可追溯性）**，不改变任何科学结论

---

## 0. 本轮结论

18:13 轮的 §6 标题为「与上一轮开放项的关系」，正文写 **「以下仍按已有 ID 开放」** —— 即明确断言下列 ID 均为**既有**（早前报告定义过）的开放项。

我对该断言做了全树与全历史枚举，发现其中**一个 ID 从来没有正文**：

```
EML-P1-H504-FOLD-SUPPORT-USES-CACHED-TIMEOUTS-BEFORE-DEV-START
```

- 在纯净 tip 全树中**恰好出现 1 次**：`docs/audits/expert-ml/2026-09-22_18-13-20_JST.md:242` —— 就是那行「仍按已有 ID 开放」的清单本身；
- `git log --all -S "<ID>"` 在全历史中只命中 **1 个提交**（`d347c26`，即 18:13 那份报告自身）；**没有任何更早或更晚的报告**定义它。

**对照组（同一份清单里的其余三项都有真实正文）**：

| ID | 全树出现次数 | 定义它的报告 |
|---|---|---|
| `EML-P1-H504-REAL-CALENDAR-GUARD-SELF-REPORT-FAILOPEN` | 5 | 13:57、15:40、18:13 |
| `EML-P1-H504-DEDUPE-FAILCLOSED-UNREACHABLE` | 3 | 15:40（§3.3 有正文章节）、18:13 |
| `EML-P1-H504-STOCK-CODE-KEY-NORMALIZATION-MISMATCH` | 2 | 18:13 |
| **`…FOLD-SUPPORT-USES-CACHED-TIMEOUTS-BEFORE-DEV-START`** | **1** | **无** |

## 1. 为什么这值得记一条（而不是「文档瑕疵无所谓」）

清单自称是「**已有 ID** 的延续」，读者（含后续审计轮）据此会认为该开放项的**判据、触发条件与反例**已在前文登记、可直接回归核对。实际上该 ID 的**具体断言**（"fold support 在 dev 启动前使用了缓存 timeout"）在发布记录中**无处可查**：

- 无法从已发布文档判断它说的是 `fold_support.py` 的哪个量、哪个 timeout、什么条件下取缓存；
- 因此它**不可被回归核对**，只能被反复转抄 —— 这正是本轮我在别的条目上能给出 `rc=1` 反例、而在这条上**无法给出任何反例**的原因。

**如实限定**：其**底层静态缺陷是真的，且另有正文**（`fold_support.py:63` 的签名不接受 label/resolved 输入，结构上是 41 个 structural split，而真实 dev 块的 `resolved=0`）。因此本项**不**推翻该缺陷，只指出**该 ID 自身的可追溯性断裂**。

## 2. 建议修法（文档修复，非程序修复）

1. 在下一份报告里**正式定义**该 ID（判据/触发/反例/修复验收），或
2. 将其**改名为已有正文所覆盖的 ID**（resolved-aware support），并在 §6 注明「旧 ID 从未有正文、此处更正」。

## 3. 本轮执行的命令与判据

| 判据 | 结果 |
|---|---|
| 全树逐文件子串枚举（2,225 文件） | 该 ID 命中 **1** 处 |
| `git log --all -S "<ID>"` | 命中 **1** 个提交（`d347c26`，报告自身） |
| 对照三项 ID 的出现次数 | 5 / 3 / 2，均有正文 |
| 全树 `EML-*` ID 去重枚举 | 88 个 ID；仅此 1 个呈「只有索引行、无正文」形态 |

**未跑及原因**：本仓库**无** `docs/audits/validate_latest.py`，手册第 5 步门禁不适用；真实 H504 fit 未跑（无授权日历）。

## 4. 三轴状态

- `execution_status`：`TRACEABILITY_AUDIT`（枚举 + git 历史检索，无执行）。
- `research_verdict`：`ONE_OPEN_ITEM_ID_HAS_NO_PUBLISHED_BODY / CLAIM_UNREGRESSABLE_AS_WRITTEN / UNDERLYING_STATIC_DEFECT_REAL_AND_SEPARATELY_DOCUMENTED`。
- `evidence_status`：源码树与 git 历史事实；**实股证据 0 条**；`evidence_type=SOFTWARE_SAMPLE`。

## 5. 分开写三件事

- **程序修复**：无。
- **任务定义变更**：无。
- **真实模型增益**：**无**。
