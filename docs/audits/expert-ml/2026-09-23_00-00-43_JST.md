# 专家／ML 线独立审计：门禁机制**已被上游报过**，但**没有人量过测试套件能否发现它**——实测 5 个机制中 **4 个删除后套件仍 65 passed**

- `audit_time_jst`: `2026-09-23 00:00 JST`
- `reviewed_repo`: `D:\ccc\pro-web-60d-strategy` (origin `https://github.com/fy-god/pro-web-60d-strategy.git`)
- `reviewed_source_sha`: `8163db84e10f52057aab1216e969f2ee121ad54f`
- `main_head_at_audit_start`: `146c583a39b0b5576d5df26a0671bddca5baa604`
- `main_head_at_audit_end`: `2a2ad874353d533b17e145912cfb699b869cb89e`
- `previous_report`: `docs/audits/expert-ml/2026-09-22_22-10-00_JST.md`
- `report_created_commit_sha`: `82f616872851a2c7d43ccff18af52aa82ded3cbd`
- 类型：`INDEPENDENT_VERIFICATION + MUTATION_DETECTION_POWER_MEASUREMENT + REGRESSION`
- 本轮 `real_market_fit_count` 增量：**0**；**无新增实股结果**

> 本报告是**独立审计**。本轮全部数字来自**我本人或只读子 agent 在纯净尖端实测**；
> 同行报告的数字一律标为「复核」而非采信。**所有证据均为软件/合成数据**，不得当作市场成绩。

---

## 0. 一句话结论

上游 `2026-09-22_22-10-00_JST.md` 报的 `EML-P1-H504-RECEIPT-PROVENANCE-CALLER-CONTROLLED` **我独立复现成功**
（真 CLI `rc=0`，假 provenance 原样落盘），但其中 `registered_task_timeout_seconds` 那一条**不是新发现**——
它早在 `2026-09-22_13-57-18_JST.md:129` 就已发表。

本轮**真正新的东西**不在这条缺陷本身，而在**它的可发现性**：
我把上游报过的 5 个机制**逐个注入纯净树**再跑全仓套件，结果是
**4 个机制（dev-support 支持门、horizon 守卫、`pipeline_hash` 来源盲区、全 NaN 静默填补）
在被删除/削弱后，套件仍然是 `65 passed`**。我**亲自独立复跑**了其中最关键的 horizon 守卫，
得到同样的 `65 passed`。
⇒ **这套测试对着这些机制零检测力**：它们可以被静默删除而不触发任何红灯。
这才是本轮最该上报的东西，因为它解释了「为什么同一批缺陷能在 52 份报告里反复出现却从未被测试拦住」。

---

## 1. 本轮实际执行的动作、命令、退出码与真数

### 1.1 靶子（真实命令读出）

```
git -C "D:\ccc\pro-web-60d-strategy" fetch origin
git -C "D:\ccc\pro-web-60d-strategy" ls-remote origin refs/heads/main
      -> 2a2ad874353d533b17e145912cfb699b869cb89e   refs/heads/main
git -C "D:\ccc\pro-web-60d-strategy" rev-parse origin/main   -> 2a2ad874353d533b17e145912cfb699b869cb89e   （与 ls-remote 一致）
```

审计起点读到的 `origin/main` 是 `146c583a39b0b5576d5df26a0671bddca5baa604`；取证期间远端推进到 `2a2ad874353d533b17e145912cfb699b869cb89e`。
`git rev-list --count 146c583a39b0b5576d5df26a0671bddca5baa604..2a2ad874353d533b17e145912cfb699b869cb89e` = **1**（该提交为 `docs` 索引提交）。

**本地 HEAD 不是基线**：本地 `main` HEAD = `0689c0a6b7decbff2288677e26117ef12d3b8df3`，
`merge-base --is-ancestor 0689c0a <tip>` = **False**，且 `branch -r --contains 0689c0a` 为**空**
⇒ 本地 HEAD **不在任何远端 ref 的历史中**。按纪律**不以其为基线**，一切以 `origin/main` 为准。

工作树实况（`git status --short`，共 5 行）：` M docs/audits/expert-ml/LATEST.md`、
` M scripts/register_fixup_task.ps1`、`?? .gitattributes`、`?? docs/audits/expert-ml/2026-09-22_07-41-01_JST.md`、
`?? docs/audits/expert-ml/2026-09-22_15-40-19_JST.md`。
**按手册只记录、不覆盖**；我全程未 checkout/stash/改动主工作树任何文件。

### 1.2 待审提交与源码变化（实测，不是转抄）

`146c583a39b0b5576d5df26a0671bddca5baa604..2a2ad874353d533b17e145912cfb699b869cb89e` 共 **11 个提交**，逐条 `--name-status` 后**全部落在 `docs/audits/expert-ml/`**：

```
2a2ad87 docs(audit): index 22:10 measured provenance gap [skip ci]
1d38fac docs(audit): 专家/ML 2026-09-22 22:10 JST measured provenance gap [skip ci]
146c583 docs(audit): 专家/ML 2026-09-22 22:05 JST dangling open-item ID with no published body [skip ci]
fbbcb7e docs(audit): 专家/ML 2026-09-22 21:35 JST refute the dedupe-unreachable P1 with a real CLI counterexample [skip ci]
92cbb3d docs(audit): 专家/ML 2026-09-22 21:05 JST alias key-space-split independently confirmed [skip ci]
9d83c85 docs(audit): 专家/ML 2026-09-22 20:30 JST review of the code-key P1 -- downgraded to P2 ... [skip ci]
84528cb docs(audit): index 18:13 H504 stock-code key normalization finding [skip ci]
d347c26 docs(audit): expert-ml 2026-09-22 18:13 JST stock-code key normalization audit [skip ci]
6e11203 docs(audit): expert-ml 2026-09-22 15:40 JST audit ... [skip ci]
74857e8 docs(audit): expert-ml 2026-09-22 13:57 JST index [skip ci]
05225aa docs(audit): expert-ml 2026-09-22 13:57 JST audit [skip ci]
```

`git diff --name-status 8163db84..2a2ad874353d533b17e145912cfb699b869cb89e` 只有 8 个 `A docs/.../*.md` + 1 个 `M docs/.../LATEST.md`；
**`src` / `tests` / `scripts` 的 diff = 空**。⇒ 被审产品源码与上游 22:10 报告所审的
`8163db84e10f52057aab1216e969f2ee121ad54f` **一致**，**本轮没有任何「已修复」结论**。

### 1.3 我实际跑过的命令、退出码、真数

| 动作 | 命令 | 退出码 | 真实输出 |
|---|---|---|---|
| 纯净树提取 | `git archive --format=zip 2a2ad874353d533b17e145912cfb699b869cb89e` + 解压 | **0** | 2230 文件；`.git` 存在 = **False** |
| 全仓测试基线 | `python -m pytest -q -o addopts= -p no:cacheprovider` | **0** | **65 passed in 27.13s** |
| 我本人变异复跑（horizon 守卫） | 同上，注入后 | **0** | **65 passed in 23.24s**（无任何 FAILED） |
| 我本人变异复跑（还原后） | 同上 | **0** | **65 passed in 23.27s**（字节还原 = **True**） |
| 真 CLI 假 provenance | `python -m src.ml.research_h504.run --stage all ... --evidence-type REAL_MARKET --source-sha THIS_IS_NOT_A_GIT_SHA --effective-prompt-sha256 not-a-sha256 --registered-task-timeout-seconds 1` | **0** | 假值**原样落盘**，无异常 |
| 组件级构造（双向对照） | 直接实例化 `ResearchSessionReceipt`，fabricated 与 truthful 两臂 | **0 / 0** | 两臂均写盘；fabricated 无拒绝 |
| 本仓库门禁 | `docs/audits/validate_latest.py` 存在性 | **128** | **不存在** ⇒ 手册第 5 步门禁在本线**不适用**，本轮**不声称**任何 gate PASS |

**未跑及原因**：真实 A 股 H504 fit（无合法本机 panel/calendar/candidates 回传）、
本机 Windows ScheduledTask 回读（无机器通道）、三小时连续 session（无 receipt）——三者均**未运行**。

### 1.4 字节同一性：我先测错了一次，必须留痕

组件级复现前我核对字节身份，**第一次得到「不相等」**：

```
GitHub blob  receipts.py @TIP = cdac56d80f1aba6ad964cf1525e69feced16fbf0
extract hash-object          = f279727c6da41c9365526ed6238829f0ed9e5ee4   -> 不相等
```

**这不是缺陷，是我的探针错**：该仓库 `core.autocrlf = true`，
`git archive` 落盘时把 LF 转成了 CRLF（实测 blob 7784 B / LF=190，
盘上 7974 B / CRLF=190），而我对**盘上文件**直接 `hash-object`，比较被 EOL 污染。
按规则（**纯 CRLF/LF 差异也会造成哈希不等**）我先做了 EOL 归因再下结论：

```
receipts.py  blob 7784 B CRLF=0 LF=190  |  disk 7974 B CRLF=190 LF=190  |  去 CR 后相等 = True
run.py       blob 17345 B CRLF=0 LF=197 |  disk 17542 B CRLF=197 LF=197 |  去 CR 后相等 = True
权威校验（blob 头 + 原始字节 sha1）:
   receipts.py  sha1 = cdac56d80f1aba6ad964cf1525e69feced16fbf0  == rev-parse  MATCH = True
   run.py       sha1 = c27711f6a0b95a8e03e50ae9e333d445ab00ea73  == rev-parse  MATCH = True
```

⇒ **被审组件与 GitHub 逐字节一致，我审的是真产品代码。** 若不做这一步归因，
我会把一个 EOL 假象写成「上游 blob 造假」的假发现。

---

## 2. 发现清单

### F1【已确认错误 · P1，但归属需更正】`EML-P1-H504-RECEIPT-PROVENANCE-CALLER-CONTROLLED`

**文件/函数/行**：`src/ml/research_h504/run.py:71`（三个参数默认值）、`run.py:109`（唯一构造点）；
`src/ml/research_h504/receipts.py:53`（类）、`:58-61`（字段）、`:70-76`（`__post_init__`）、
**`:129-131`**（原样写入）、`:135`（`evidence_type`）、`:138`（落盘）。

**触发条件/反例（实测，决定性）**：真 CLI 传 `--evidence-type REAL_MARKET` 加三个假值
（`THIS_IS_NOT_A_GIT_SHA` / `not-a-sha256` / `1`），**退出码 0、stderr 空**，
`local_start_receipt.json` 中三项**原样落盘**。全 `src/` 树对三个字段名共 **8 处**出现，
**全部是写入或透传，0 处比较/断言/正则/`git rev-parse`/prompt 哈希/ScheduledTask 查询**。

**机制**：收据记录的是**调用者声称**的东西，不是**运行时实测**的源码与 prompt。
`evidence_type` 与这三个字段之间**没有任何交叉校验**（`src/` 内仅 11 行提到 `evidence_type`，
其中唯一同时提到 provenance 字段的就是 `run.py:109` 那次透传）。

**影响**：一旦产生第一个真实 fit，收据**无法证明**它由哪份源码/prompt/父进程上限产生。
**按当前仓库自己的文档路径走也会缺**：`scripts/fixup_prompt.txt:31` 那一条真实 `--stage all` 命令
**完全没有**这三个 flag；`RESEARCH_QUICKSTART.md:38` 只补了 `--source-sha`。
⇒ 不是「只有恶意调用者才会」的边缘情况。

**归属更正（必须分开说）**：该缺陷的 `registered_task_timeout_seconds` 那一条**不是新发现**——
`2026-09-22_13-57-18_JST.md:129` 已发表
（"`run_fixup.py` 自己把 `registered_task_timeout` 写成 `NOT_CHECKED_BY_THIS_SCRIPT`，所以仓库源码不能证明实际 Windows 任务已扩到 PT3H50M"），
`15-40-19_JST.md:201`、`18-13-20_JST.md:256`、`LATEST.md:73` 重复；
且属**仓库自述限制**（`RESEARCH_QUICKSTART.md:57`、`h504_integration_validation.json:31/33`）。
**真正新的是 `source_sha` + `effective_prompt_sha256` 这一半**：它既未被上游报过，也未被仓库披露。
我因此**不把整条记作本轮新发现**，只记：**该 P1 成立；其中一半为旧项，一半为新**。

**修复建议（程序修复 · 需授权，我未改任何源码）**：见 F2 之后一并给出（出处同上游 §5）。

**验收测试**：`REAL_MARKET` 下若 `source_sha` 不是 40-hex、或与 `git rev-parse HEAD` 不符，
或 prompt hash 与磁盘文件不符 → **必须 fail closed**。当前该测试**必然失败**（实测 exit 0），此即缺陷证据。

---

### F2【已确认错误 · P1 · 本轮新发现】`EML-P1-H504-GUARDS-HAVE-ZERO-TEST-DETECTION-POWER-001`

**文件/函数/行**：`tests/research_h504/test_candidate.py`、`tests/research_h504/test_github_delivery.py`、
`tests/test_engine.py`；被守护的代码：`src/ml/research_h504/run.py:88-91`、`run.py:94`、
`src/ml/research_h504/train_h504.py:13-15,43`、`src/ml/research_h504/registry.py:19-23`。

**触发条件/反例（实测）**：把上游**已发表**的机制逐个注入**纯净树**，再跑全仓套件：

| 变异 | 位置 | 套件结果 | 有测试失败？ | 检测力 |
|---|---|---|---|---|
| M1 dev-support 支持门放松为恒真 | `train_h504.py:43` | **65 passed** | 无 | **零** |
| M2 `registry.latest_complete` 去掉 signature 相等 | `registry.py:21` | 1 failed / 64 passed | 有 | 有 |
| M3b **只**删除 horizon 守卫 | `run.py:90` | **65 passed** | 无 | **零** |
| M3c **只**删除 calendar 守卫 | `run.py:88` | 1 failed / 64 passed | 有 | 有 |
| M4 `pipeline_hash` 改为哈希常量 | `run.py:94` | **65 passed** | 无 | **零** |
| M5 `_safe_impute` 完全不填补 | `train_h504.py:15` | **65 passed** | 无 | **零** |
| M5b `_safe_impute` 一律 `fillna(0.0)`（忽略 train 中位数、无指示列） | `train_h504.py:15` | **65 passed** | 无 | **零** |

**我本人独立复跑**（用**另一份**新提取，避免与并行取证共用同一棵树）：
基线 `exit=0 / 65 passed in 25.80s` → 把 `args.horizon != 504` 改为 `False` →
**`exit=0 / 65 passed in 23.24s`，无任何 FAILED** → 字节还原 `True` → `exit=0 / 65 passed`。
⇒ **horizon 守卫可以被静默删除而测试全绿**，与子 agent 结论一致。

**这不是「不可达变异」**：运行时探针显示这些代码**确实被执行**——
`_safe_impute` 在携带 **864 个 NaN 单元**的 108×82 / 156×82 帧上**被调用 1 次/run**，
仍返回 `COMPLETE_H504_DEV`；M1 的支持门也被求值并通过（`n_train=108 > 100`、`n_dev=156 > 50`）。
⇒ 这是**假阴性（测试没断言），不是死代码**。

**机制**：套件**确实**调用了真实生产入口（`main --stage all` 两处、`run_h504_hgb`、
`prepare_panel`、`label_h504_candidates`），所以**不是整体绕过**；
但它**只用合成 fixture**：`_panel(n=510)` 单股票 / `_long_h504_panel_and_layers(1180, 12)`
12 只确定性走势股票；且唯一驱动完整链的那条测试
`test_candidate.py:460` 传 `--evidence-type SYNTHETIC` 且**不传 `--calendar`**，
**恰好绕开 `run.py:88` 那条 REAL_MARKET 守卫**，然后断言 `COMPLETE_H504_DEV`。
`test_github_delivery.py:60` 更直接 `monkeypatch` 掉 `first_legal_split`。
⇒ **覆盖率存在，但对这些机制的断言力不存在。**

**影响**：这解释了本线的一个反复出现的现象——同一族缺陷在 **52 份报告**里被反复报告、
上游反复「修」，却**没有任何一次修复被测试锁定**。任何一次重构、合并或「清理未使用守卫」
都可以把这些保护**静默删除**，而 CI（本仓 `.github/` 不存在）与本地套件都不会发声。
影响面是**全部真实 fit 的前置保护**，不是某个数值。

**修复建议（程序修复 · 需授权）**：为每个守卫各加一条**负向**验收测试：
构造能触发该守卫的输入，断言**必须**非零退出且错误码正确。具体：
1. horizon：`REAL_MARKET` + `--horizon 200` + 合法 `--calendar` → 必须 raise；
2. calendar：`REAL_MARKET` 不传 `--calendar` → 必须 raise（已有，但只在 `test_github_delivery.py`）；
3. `pipeline_hash`：改动 `src/ml/build_matrix.py` 一个字节 → 签名**必须变**；
4. 支持门：`n_dev_resolved = 0` → 必须**不得**返回 `COMPLETE_H504_DEV`；
5. 填补：全 NaN 列 → 必须留痕（指示列或 fail closed）。
并把变异测试纳入日常：每个守卫至少有一条测试会因删除它而变红。

**验收测试**：上述 5 条加入后，重跑 M1/M3b/M4/M5/M5b 五个变异 → **每一条都必须产生至少一个 FAILED**。
当前 5 条中 **4 条为 65 passed**，此即缺陷证据。

---

### F3【修复后回归 · 全部维持】9 个未修项在尖端逐条复测，**0 个被修复、0 个被证伪**

被审源码 diff 为空，按纪律逐条**重新打开文件实测**（不是转抄）：

| 未修项 | 结论 | 关键落点 |
|---|---|---|
| `EML-P1-H504-REAL-CALENDAR-GUARD-SELF-REPORT-FAILOPEN` | **STILL_PRESENT** | `run.py:88-89`；默认 `UNSPECIFIED`（`:72`）不在阻断集合内；实测默认+无日历 → **exit 0**，`REAL_MARKET` → exit 1 |
| `EML-P1-H504-HORIZON-GUARD-SAME-FAILOPEN` | **STILL_PRESENT**（含更正） | `run.py:88` vs `:90`；集合字面量是**复制粘贴**而非共享变量（子 agent 更正），但「一个默认值同时打穿两个守卫」成立 |
| `EML-P1-H504-AUX-FALLBACK-RUNS-WHEN-PRIMARY-READY` | **STILL_PRESENT** | `run.py:162` 只查 `not args.skip_neural`；默认上限 6 = `min(6, 2 reps × 3 seeds)`；主任务 fit（`:154`）**不计入** `nfit` |
| `EML-P1-H504-AUX-RUNS-WHEN-PRIMARY-NEVER-RAN` | **STILL_PRESENT** | `run.py:144` 与 `:162` **同级**（兄弟 if，非嵌套）；实测序列 `['BLOCKED_CANDIDATE_MANIFEST','COMPLETE_AUX'×6]` |
| `EML-P1-H504-REUSE-TRUSTS-REGISTRY-WITHOUT-ARTIFACT-VERIFICATION` | **STILL_PRESENT** | `registry.py` 全文 **25 行**；`latest_complete()` `:19-23` 只比 signature + status，**零 artifact 校验** |
| `EML-P1-H504-SIGNATURE-BLIND-TO-FEATURE-SOURCE` | **STILL_PRESENT** | `run.py:94` 非递归 `glob('*.py')` 只覆盖 16 文件；`snapshot_features.py:78` 导入父包 `src/ml/build_matrix.py`（546 行）**不在**哈希集 |
| `EML-P1-H504-DEV-RESOLVED-SUPPORT-MISSING` | **STILL_PRESENT** | `train_h504.py:42` 定义 `resolved`，`:43` 只守 `train/dev`；实测 `n_dev_resolved=0` 仍报 `COMPLETE_H504_DEV` |
| `EML-P1-H504-GLOBAL-FIT-BUDGET-UNENFORCED` | **STILL_PRESENT** | `src/` 内独立的 `48` 字面量 **0 处**；`fits_*` 12 处**全是写入**；唯一上限是单进程 `run.py:170` `nfit >= args.max_aux_fits` |
| `EML-P2-H504-ALL-NAN-FEATURES-SILENTLY-IMPUTED` | **STILL_PRESENT** | `train_h504.py:13-15`；`:34` 的 `merge(how='left')` 全仓 `indicator=` **0 处**，无匹配率断言 |

---

### F4【已确认错误 · P3】同行报告把 **4 键子集**当作「实际写出的」收据全文

**落点**：`docs/audits/expert-ml/2026-09-22_22-10-00_JST.md:121-130`。

**实测**：真实 `local_start_receipt.json` 有 **14 个键**
（`schema_version, session_id, started_at, pid, python, platform, source_sha,
effective_prompt_sha256, registered_task_timeout_seconds, target_active_seconds,
normal_wall_limit_seconds, hard_runner_timeout_seconds, evidence_type, argv`），
报告只贴了 4 个并标注为「实际写出的」。四个值**逐字节正确**，名称与类型也正确，
**故这是表述问题而非事实错误**；但它会让读者以为收据只有 provenance 四项。
另有小误：报告引用 parser 时漏了 `type=int`（真实为 `run.py:71` 带 `type=int`）。
我按纪律如实记录，**不把它写成缺陷**，仅作 **P3 表述更正**。

---

### F5【已核实 · 不构成缺陷】候选产物的可复现性

上游给出的候选包哈希（ZIP `f2d680a5…`、guard `c6ba5386…`、测试 `dc6f174c…`），
我在**所有 ref 的全部 blob**、受跟踪树、纯净树、以及 `_sched/work` 全盘做了内容与文件名搜索：
这些 token **只出现在文档提交里**（`1d38fac` / `2a2ad87`），
**没有任何一处是真实存在的文件**（`D:\ccc\pro-web-60d-strategy` 与纯净树内 `*.zip` 均为 **0**）。
**这与上游自述的「隔离沙箱候选、未远端改产品源码」一致，属诚实的范围限定**，
故**不算缺陷**。搜索路径已如实记录，以免被误读为「未找到」的推断。

---

## 3. 状态三轴

- `execution_status`: **COMPLETE**（取证、变异测量、回归、对照实验均已执行完毕，退出码见 §1.3）。
- `research_verdict`: **NO_NEW_REAL_MARKET_RESULT**（`real_market_fit_count` 增量 **0**；0 次合法 H504 fit）。
- `evidence_status`: **MEASURED_FIRST_HAND**（全部数字由我或只读子 agent 在同一尖端实测；
  同行数字逐条标为「复核」；我自己的第一次字节比较错误已留痕于 §1.4）。
- `evidence_type`: **SOFTWARE_SAMPLE**（合成面板 + 真实 CLI/组件执行）。
  **本轮无 REAL_MARKET_NEW_FIT，也无 SYNTHETIC 拟合结果。**

---

## 4. 三类必须分开的结论

- **程序修复**：F1 的 measured-provenance（出处同上游 §5 的 P1-A~P1-D）；
  **F2 的 5 条负向守卫测试**是本轮最该先做的事——它比再修一个守卫更根本。
  **以上全部需要授权；本轮我未改任何源码、测试或配置。**
- **任务定义变更**：把「守卫必须有会因删除它而变红的测试」写进门禁/评审要求
  （当前 52 份报告无一处做此测量）。
- **真实模型增益**：**0**。被审源码 `8163db84…` 未变；`v12` 类改动 0；
  65 个测试全部来自既有套件（**不是**本轮新增）。

---

## 5. 研究/改进想法（1–3 条，注明依据）

1. **把「守卫的可检测性」变成一等公民。** 依据：F2 的 M1/M3b/M4/M5/M5b
   在删除后仍 **65 passed**（含我本人独立复跑）。做法：对每个 fail-closed 守卫强制配一条
   **会因删除它而变红**的负向测试，并把变异测试纳入日常。
   预测：F2 的 5 条验收测试加入后，5 个变异**全部**产生 FAILED。
2. **补一条「合成数据不得绕开守卫」的架构测试。** 依据：唯一驱动完整链的
   `test_candidate.py:460` 用 `SYNTHETIC` 且不传 `--calendar`，
   **结构性绕开** `run.py:88` 并断言 `COMPLETE_H504_DEV`。做法：要求完整链测试至少有**一条**
   在 `REAL_MARKET` + 独立 calendar 下跑通（可用合成 calendar 文件）。
3. **把 provenance 从「调用者填写」改为「代码自测」并 fail closed。** 依据：F1 的真 CLI 反例
   （exit 0 落盘假值）+ 文档路径本身就缺这三个 flag。这也能顺带修掉
   旧项 `…RECEIPT-PROVENANCE` 的 timeout 那一半。

---

## 6. 本轮的自我更正（方法论留痕）

- **我第一次的字节同一性检查得出「不相等」，是错的三次同类错误之一。**
  根因：仓库 `core.autocrlf=true` 使 `git archive` 把 LF 转 CRLF，而我对盘上文件做 `hash-object`。
  我按「纯 CRLF 差异也会造成哈希不等」先归因 EOL，再做权威校验（blob 头 + 原始字节 sha1），
  确认**逐字节一致**。**若跳过这步，我会写出一个「上游 blob 与 GitHub 不符」的假发现。**
- **我先后两次在脚本结尾引用未定义的变量 `W` 而崩溃**（`NameError: name 'W' is not defined`），
  两次都发生在**所有测量完成之后**、仅写结果文件那一步，因此没有污染任何数据；
  已修正并重跑，最终结果文件与退出码均为重新读出的真值。
- **我最初想报的「timeout 契约不自洽」被我自己的检索否掉**：全 52 份同行报告中
  `2026-09-22_15-40-19_JST.md:202-204` 已明确写出
  「模板 9000s / runner 13200s / 目标 10800s 三者互不自洽」。
  按「宣称『新』之前先 grep 同行全部文档树」的规则，**该条不计作本轮新发现**，仅在此说明我确实复算过
  （tracked XML `PT2H30M` = **9000 s**；契约要求注册值 **13800 s**；代码 `10800/12600/13200`）。
- **我最初想报的 `src/render_results.py` 缺陷不成立**：它是 52 份报告**唯一未引用**的源文件，
  含 2 处 `except: pass`，且它生成的 `RESULTS.md` 自称数字「不会与数据漂移」。
  但实读后确认那些 handler **是刻意的**，会在输出中写出明确的读者提示
  （`:198-199` 与 `:216-217` 分别对应「stride 无法恢复」与「min_history 回退到常量」两条注记）
  ⇒ **诚实负结果，不是缺陷**，不上报。

---

## 7. 下一 READY（不因本轮而改变）

真实 A 股面板 + 独立版本化 exchange calendar → 合法 candidates/labels/fold →
T0 HGB，若主任务阻塞则同 session `AUX_REAL_HISTORY`。
**门禁侧优先顺序**：F2 的 5 条负向守卫测试 → F1 的 measured provenance（含 timeout 腿）
→ F3 中 48-fit 跨 session 预算与 resolved-aware 支持门。
