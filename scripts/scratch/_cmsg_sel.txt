Make selection_ceiling.json's practical_note compute itself instead of asserting

The strategies audit found `practical_note` in outputs/ml/audit/selection_ceiling.json
contradicts the payload it sits inside, in four separate places at once:

  claim                                    payload in the SAME dict
  3-config `models` preset                  n_configs = 7
  mean ~3.9%                                null_precision_mean = 0.037493 (3.75%)
  SD ~0.7%                                  null_precision_sd   = 0.012487 (1.25%)
  best-of-3 ~4.6%                           best_of_K["3"]      = 0.048030 (4.80%)

The SD is wrong by a factor of 1.8, and it is the number the sentence's whole
argument rests on ("selection cannot manufacture 15.98%" is only persuasive if the
null spread is small). A reader could see the contradiction without leaving the
file, which is exactly what hardcoding guarantees and interpolation cannot
reproduce.

The note is now formatted from `mu`, `sd`, `sel["3"]` and the models preset's own
`n_configs`, so it cannot drift again. Regenerated: 16.3 s, and a full flat diff of
the artifact shows exactly ONE field changed -- `.selection_budget.practical_note`
-- with all 40 other fields bit-identical.

The audit now checks the note arithmetically against its own object: it must quote
its own mean, SD, best-of-3, and the models preset's real config count. Bound by
substring on interpolated values, so a re-hardcoded stale number fails.

564 checks, 0 problems.
